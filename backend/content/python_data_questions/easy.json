[
  {
    "id": 31001,
    "order": 1,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Select Columns and Drop Nulls",
    "description": "You have a DataFrame `df_users` loaded from the users dataset.\n\n**Columns:** `user_id`, `name`, `email`, `signup_date`, `country`, `is_active`\n\nSelect only the `name` and `email` columns, then drop any rows where `email` is null. Return the resulting DataFrame.",
    "dataframes": {
      "df_users": "users.csv"
    },
    "schema": {
      "df_users": [
        "user_id",
        "name",
        "email",
        "signup_date",
        "country",
        "acquisition_channel",
        "plan_tier",
        "is_active"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_users):\n    # Select name and email columns, drop rows with null email\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_users):\n    return df_users[['name', 'email']].dropna(subset=['email']).reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_users):\n    return df_users[['name', 'email']].dropna(subset=['email']).reset_index(drop=True)",
    "explanation": "Passing a list of column names to `df[['col1', 'col2']]` selects those columns. `.dropna(subset=['email'])` removes rows where `email` is NaN. Using `subset` is more precise than plain `.dropna()` which would drop rows with any null.",
    "hints": [
      "Start by narrowing the frame to the columns the output actually needs.",
      "Use `.dropna(subset=['email'])` to remove rows where only email is null"
    ],
    "concepts": [
      "COLUMN SELECTION & PROJECTION",
      "BOOLEAN INDEXING & FILTERING",
      "MISSING VALUE STRATEGY"
    ]
  },
  {
    "id": 31002,
    "order": 2,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Count Orders by Status",
    "description": "You have a DataFrame `df_orders` loaded from the orders dataset.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `net_amount`\n\nCount the number of orders for each `status`. Return a DataFrame with columns `['status', 'count']`, sorted by `count` descending.",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Count orders per status, return DataFrame with columns [status, count]\n    # sorted by count descending\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    result = df_orders.groupby('status', as_index=False).size().rename(columns={'size': 'count'})\n    return result.sort_values('count', ascending=False).reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    result = df_orders.groupby('status', as_index=False).size().rename(columns={'size': 'count'})\n    return result.sort_values('count', ascending=False).reset_index(drop=True)",
    "explanation": "`groupby('status').size()` counts rows per group. Using `as_index=False` keeps `status` as a column rather than the index. `.rename(columns={'size': 'count'})` gives the count column its required name. Finally, sorting by `count` descending puts the most common statuses first.",
    "hints": [
      "Reduce the frame to the requested grain and count how many records fall into each bucket.",
      "The result of `.groupby().size()` has the groupby key as the index by default — decide whether you want it as a column instead."
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "OUTPUT SHAPE & ORDERING"
    ]
  },
  {
    "id": 31003,
    "order": 3,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Fill Missing Salaries with Mean",
    "description": "You have a DataFrame `df_employees` loaded from the employees dataset.\n\n**Columns:** `employee_id`, `employee_name`, `email`, `salary`, `department_id`, `hire_date`, `country`\n\nSome employees have a null `salary`. Fill those null values with the mean salary of all employees (computed from non-null values). Return the full DataFrame with the filled values.",
    "dataframes": {
      "df_employees": "employees.csv"
    },
    "schema": {
      "df_employees": [
        "employee_id",
        "employee_name",
        "email",
        "salary",
        "department_id",
        "hire_date",
        "country"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_employees):\n    # Fill null salary values with the mean salary, return the full DataFrame\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_employees):\n    mean_salary = df_employees['salary'].mean()\n    result = df_employees.copy()\n    result['salary'] = result['salary'].fillna(mean_salary)\n    return result",
    "solution_code": "import pandas as pd\n\ndef solve(df_employees):\n    mean_salary = df_employees['salary'].mean()\n    result = df_employees.copy()\n    result['salary'] = result['salary'].fillna(mean_salary)\n    return result",
    "explanation": "`.mean()` on a Series automatically skips NaN values, giving the mean of non-null salaries. `.fillna(value)` replaces NaN entries with that value. Working on a `.copy()` of the DataFrame avoids mutating the original.",
    "hints": [
      "You need a single number that summarises the non-null salaries, then use it to fill in the gaps.",
      "Use `.fillna(mean_salary)` to replace null values in the salary column"
    ],
    "concepts": [
      "MISSING VALUE STRATEGY",
      "COLUMN SELECTION & PROJECTION"
    ]
  },
  {
    "id": 31004,
    "order": 4,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Extract Email Domain",
    "description": "You have a DataFrame `df_users` loaded from the users dataset.\n\n**Columns:** `user_id`, `name`, `email`, `signup_date`, `country`, `is_active`\n\nSome rows have null `email`. First drop those rows, then add a new column `email_domain` that contains the domain part of the email address (everything after the `@` symbol — e.g. `'gmail.com'` from `'user@gmail.com'`). Return the full DataFrame with the new column.",
    "dataframes": {
      "df_users": "users.csv"
    },
    "schema": {
      "df_users": [
        "user_id",
        "name",
        "email",
        "signup_date",
        "country",
        "acquisition_channel",
        "plan_tier",
        "is_active"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_users):\n    # Drop rows with null email, then add 'email_domain' column\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_users):\n    result = df_users.dropna(subset=['email']).copy()\n    result['email_domain'] = result['email'].str.split('@').str[1]\n    return result.reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_users):\n    result = df_users.dropna(subset=['email']).copy()\n    result['email_domain'] = result['email'].str.split('@').str[1]\n    return result.reset_index(drop=True)",
    "explanation": "`.dropna(subset=['email'])` removes rows with a null email before the string operation. `.str.split('@')` splits each email string into a list at the `@` sign. `.str[1]` then extracts the second element (index 1), which is the domain. The `.copy()` prevents a SettingWithCopyWarning.",
    "hints": [
      "Decide which rows remain valid before applying the rest of the transformation.",
      "Split the string at the `@` separator; the domain is the element after it."
    ],
    "concepts": [
      "BOOLEAN INDEXING & FILTERING",
      "COLUMN SELECTION & PROJECTION"
    ]
  },
  {
    "id": 31005,
    "order": 5,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Total Order Amount by Status",
    "description": "You have a DataFrame `df_orders` loaded from the orders dataset.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `net_amount`\n\nCompute the sum of `net_amount` for each `status`. Return a DataFrame with columns `['status', 'net_amount']`, sorted by `status` ascending.",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Return net_amount summed by status, columns: [status, net_amount]\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    result = df_orders.groupby('status', as_index=False)['net_amount'].sum()\n    return result.sort_values('status').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    result = df_orders.groupby('status', as_index=False)['net_amount'].sum()\n    return result.sort_values('status').reset_index(drop=True)",
    "explanation": "`groupby('status', as_index=False)['net_amount'].sum()` groups rows by status, selects the `net_amount` column, and sums each group. `as_index=False` keeps `status` as a plain column in the output rather than the DataFrame index.",
    "hints": [
      "Collapse the data to the requested grain and total the numeric measure at that level.",
      "Decide whether the groupby key should appear as the DataFrame index or as a plain column in the output."
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "COLUMN SELECTION & PROJECTION"
    ]
  },
  {
    "id": 31006,
    "order": 6,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "First Event Type per User",
    "description": "You have a DataFrame `df_events` loaded from the events dataset.\n\n**Columns:** `event_id`, `session_id`, `user_id`, `event_time`, `event_name`, `product_id`\n\nUsers often perform the same event type (e.g. `view_product`) multiple times across sessions. Return the **first occurrence** of each event type per user — one row per `(user_id, event_name)` pair using the earliest `event_time`. Output columns: `user_id`, `event_name`, `event_time`. Sort by `user_id` ascending, then `event_name` ascending.",
    "dataframes": {
      "df_events": "events.csv"
    },
    "schema": {
      "df_events": [
        "event_id",
        "session_id",
        "user_id",
        "event_time",
        "event_name",
        "product_id"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_events):\n    # Return first occurrence of each event type per user\n    # Columns: user_id, event_name, event_time — sorted by user_id, then event_name\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_events):\n    df = df_events.copy()\n    df['event_time'] = pd.to_datetime(df['event_time'])\n    result = (\n        df.sort_values('event_time')\n        .drop_duplicates(subset=['user_id', 'event_name'], keep='first')\n        [['user_id', 'event_name', 'event_time']]\n        .sort_values(['user_id', 'event_name'])\n        .reset_index(drop=True)\n    )\n    return result",
    "solution_code": "import pandas as pd\n\ndef solve(df_events):\n    df = df_events.copy()\n    df['event_time'] = pd.to_datetime(df['event_time'])\n    result = (\n        df.sort_values('event_time')\n        .drop_duplicates(subset=['user_id', 'event_name'], keep='first')\n        [['user_id', 'event_name', 'event_time']]\n        .sort_values(['user_id', 'event_name'])\n        .reset_index(drop=True)\n    )\n    return result",
    "explanation": "`.sort_values('event_time')` puts events in chronological order so the first occurrence has the smallest timestamp. `.drop_duplicates(subset=['user_id', 'event_name'], keep='first')` then retains only the first row for each unique `(user_id, event_name)` pair — every later repeat is discarded. Specifying a multi-column `subset` is the common real-world case: uniqueness on a *combination* of keys, not a single column.",
    "hints": [
      "Define what makes two rows equivalent — here it is the combination of user and event type — then keep one representative row for each pair.",
      "Sort by the timestamp before deduplicating so that `keep='first'` selects the chronologically earliest event, not an arbitrary one."
    ],
    "concepts": [
      "DEDUPLICATION LOGIC",
      "DATETIME OPERATIONS"
    ]
  },
  {
    "id": 31007,
    "order": 7,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Rename a Column",
    "description": "You have a DataFrame `df_employees` loaded from the employees dataset.\n\n**Columns:** `employee_id`, `employee_name`, `email`, `salary`, `department_id`, `hire_date`, `country`\n\nRename the column `salary` to `annual_salary`. Return the full DataFrame with the renamed column.",
    "dataframes": {
      "df_employees": "employees.csv"
    },
    "schema": {
      "df_employees": [
        "employee_id",
        "employee_name",
        "email",
        "salary",
        "department_id",
        "hire_date",
        "country"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_employees):\n    # Rename 'salary' to 'annual_salary', return the full DataFrame\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_employees):\n    return df_employees.rename(columns={'salary': 'annual_salary'})",
    "solution_code": "import pandas as pd\n\ndef solve(df_employees):\n    return df_employees.rename(columns={'salary': 'annual_salary'})",
    "explanation": "`.rename(columns={'old_name': 'new_name'})` returns a new DataFrame with the specified column(s) renamed. The original DataFrame is not modified. You can rename multiple columns at once by adding more key-value pairs to the dict.",
    "hints": [
      "This is an output-schema task: keep the data unchanged and only reshape the column labels.",
      "Return the full DataFrame after the label changes; no filtering or aggregation is required."
    ],
    "concepts": [
      "OUTPUT SHAPE & ORDERING",
      "COLUMN SELECTION & PROJECTION"
    ]
  },
  {
    "id": 31008,
    "order": 8,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Top Products by Order Quantity",
    "description": "You have a DataFrame `df_order_items` loaded from the order_items dataset.\n\n**Columns:** `order_item_id`, `order_id`, `product_id`, `quantity`, `unit_price`, `line_amount`\n\nReturn the top 5 `product_id`s by total quantity ordered. Output columns: `product_id`, `total_quantity`. Sort by `total_quantity` descending.",
    "dataframes": {
      "df_order_items": "order_items.csv"
    },
    "schema": {
      "df_order_items": [
        "order_item_id",
        "order_id",
        "product_id",
        "quantity",
        "unit_price",
        "line_amount"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_order_items):\n    # Return top 5 products by total quantity ordered\n    # Columns: product_id, total_quantity — sorted by total_quantity descending\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_order_items):\n    result = df_order_items.groupby('product_id', as_index=False)['quantity'].sum().rename(columns={'quantity': 'total_quantity'})\n    return result.sort_values('total_quantity', ascending=False).head(5).reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_order_items):\n    result = df_order_items.groupby('product_id', as_index=False)['quantity'].sum().rename(columns={'quantity': 'total_quantity'})\n    return result.sort_values('total_quantity', ascending=False).head(5).reset_index(drop=True)",
    "explanation": "`groupby('product_id')['quantity'].sum()` totals up the ordered quantity per product. After renaming the aggregated column to `total_quantity`, sorting descending and calling `.head(5)` limits the result to the five best-selling products.",
    "hints": [
      "Collapse the data to the requested grain and total the numeric measure at that level.",
      "Order the per-product totals from largest to smallest and keep only the top 5 rows."
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "OUTPUT SHAPE & ORDERING",
      "RANKING & TOP-N PER GROUP"
    ]
  },
  {
    "id": 31009,
    "order": 9,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "High-Salary Employees",
    "description": "You have a DataFrame `df_employees` loaded from the employees dataset.\n\n**Columns:** `employee_id`, `employee_name`, `email`, `salary`, `department_id`, `hire_date`, `country`\n\nReturn employees with `salary` greater than 60000. Include only columns `employee_id`, `employee_name`, `salary`. Sort by `salary` descending.",
    "dataframes": {
      "df_employees": "employees.csv"
    },
    "schema": {
      "df_employees": [
        "employee_id",
        "employee_name",
        "email",
        "salary",
        "department_id",
        "hire_date",
        "country"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_employees):\n    # Return employees with salary > 60000\n    # Columns: employee_id, employee_name, salary — sorted by salary descending\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_employees):\n    result = df_employees[df_employees['salary'] > 60000][['employee_id', 'employee_name', 'salary']]\n    return result.sort_values('salary', ascending=False).reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_employees):\n    result = df_employees[df_employees['salary'] > 60000][['employee_id', 'employee_name', 'salary']]\n    return result.sort_values('salary', ascending=False).reset_index(drop=True)",
    "explanation": "Boolean indexing filters to rows where `salary > 60000`. Column selection with `[['employee_id', 'employee_name', 'salary']]` trims to the required columns. Sorting by `salary` descending shows the highest earners first.",
    "hints": [
      "Cut the table down to just the rows that meet the salary threshold before doing anything else.",
      "Then select the three required columns and sort descending"
    ],
    "concepts": [
      "BOOLEAN INDEXING & FILTERING",
      "COLUMN SELECTION & PROJECTION",
      "OUTPUT SHAPE & ORDERING"
    ]
  },
  {
    "id": 31010,
    "order": 10,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Orders Placed in 2023",
    "description": "You have a DataFrame `df_orders` loaded from the orders dataset.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `net_amount`\n\nFilter orders where `order_date` falls in the year 2023. Return columns `order_id`, `user_id`, `order_date`, `net_amount`, sorted by `order_date` ascending.",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Filter orders from 2023, return order_id, user_id, order_date, net_amount\n    # sorted by order_date ascending\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['order_date'] = pd.to_datetime(df['order_date'])\n    result = df[df['order_date'].dt.year == 2023][['order_id', 'user_id', 'order_date', 'net_amount']]\n    return result.sort_values('order_date').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['order_date'] = pd.to_datetime(df['order_date'])\n    result = df[df['order_date'].dt.year == 2023][['order_id', 'user_id', 'order_date', 'net_amount']]\n    return result.sort_values('order_date').reset_index(drop=True)",
    "explanation": "`pd.to_datetime()` parses the string column into a proper datetime type. The `.dt` accessor then exposes date parts like `.dt.year`, making it easy to filter for a specific year with boolean indexing.",
    "hints": [
      "Normalize the date values into a true time representation before extracting calendar pieces or measuring intervals.",
      "Once the column is a proper datetime type, you can extract the year as a numeric value and use it as a filter."
    ],
    "concepts": [
      "DATETIME OPERATIONS",
      "BOOLEAN INDEXING & FILTERING"
    ]
  },
  {
    "id": 31011,
    "order": 11,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Average Order Value per User",
    "description": "You have a DataFrame `df_orders` loaded from the orders dataset.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `net_amount`\n\nCompute the average `net_amount` per `user_id`. Return a DataFrame with columns `user_id` and `avg_order_value` (rounded to 2 decimal places). Sort by `avg_order_value` descending.",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Return user_id and avg_order_value (mean net_amount, rounded to 2dp)\n    # sorted by avg_order_value descending\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    result = df_orders.groupby('user_id', as_index=False)['net_amount'].mean().rename(columns={'net_amount': 'avg_order_value'})\n    result['avg_order_value'] = result['avg_order_value'].round(2)\n    return result.sort_values('avg_order_value', ascending=False).reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    result = df_orders.groupby('user_id', as_index=False)['net_amount'].mean().rename(columns={'net_amount': 'avg_order_value'})\n    result['avg_order_value'] = result['avg_order_value'].round(2)\n    return result.sort_values('avg_order_value', ascending=False).reset_index(drop=True)",
    "explanation": "`.groupby('user_id')['net_amount'].mean()` computes the mean order value per user. Renaming and rounding give the column its correct name and precision. Sorting descending surfaces the users with the highest average spend.",
    "hints": [
      "Collapse the data to the requested grain and compute the average measure for each group.",
      "After computing the mean, limit the result to two decimal places before returning."
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "OUTPUT SHAPE & ORDERING"
    ]
  },
  {
    "id": 31012,
    "order": 12,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Latest Order per User",
    "description": "You have a DataFrame `df_orders` loaded from the orders dataset.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `net_amount`\n\nReturn each user's most recent order. Output columns: `user_id`, `order_id`, `order_date`. Sort by `user_id` ascending.",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Return each user's most recent order: user_id, order_id, order_date\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['order_date'] = pd.to_datetime(df['order_date'])\n    idx = df.groupby('user_id')['order_date'].idxmax()\n    result = df.loc[idx, ['user_id', 'order_id', 'order_date']]\n    return result.sort_values('user_id').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['order_date'] = pd.to_datetime(df['order_date'])\n    idx = df.groupby('user_id')['order_date'].idxmax()\n    result = df.loc[idx, ['user_id', 'order_id', 'order_date']]\n    return result.sort_values('user_id').reset_index(drop=True)",
    "explanation": "`groupby('user_id')['order_date'].idxmax()` returns the index label of the row with the maximum `order_date` for each user. `df.loc[idx]` then selects those rows from the original DataFrame, giving one row per user — the most recent order.",
    "hints": [
      "Within each user's orders, the answer is the row carrying the latest date — one row per user.",
      "Find the row-level index of the maximum-date row per user, then use label-based selection on the original DataFrame to retrieve those rows."
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "RANKING & TOP-N PER GROUP"
    ]
  },
  {
    "id": 31013,
    "order": 13,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Support Tickets by Status",
    "description": "You have a DataFrame `df_support_tickets` loaded from the support_tickets dataset.\n\n**Columns:** `ticket_id`, `user_id`, `created_at`, `issue_type`, `priority`, `status`, `resolution_hours`\n\nCount the number of tickets for each `status`. Return a DataFrame with columns `status` and `ticket_count`, sorted by `ticket_count` descending.",
    "dataframes": {
      "df_support_tickets": "support_tickets.csv"
    },
    "schema": {
      "df_support_tickets": [
        "ticket_id",
        "user_id",
        "created_at",
        "issue_type",
        "priority",
        "status",
        "resolution_hours"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_support_tickets):\n    # Count tickets per status, return status and ticket_count sorted descending\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_support_tickets):\n    result = df_support_tickets.groupby('status', as_index=False).size().rename(columns={'size': 'ticket_count'})\n    return result.sort_values('ticket_count', ascending=False).reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_support_tickets):\n    result = df_support_tickets.groupby('status', as_index=False).size().rename(columns={'size': 'ticket_count'})\n    return result.sort_values('ticket_count', ascending=False).reset_index(drop=True)",
    "explanation": "`groupby('status').size()` counts the rows in each status group. Renaming `size` to `ticket_count` gives the output column a meaningful name. Sorting descending puts the most common status first.",
    "hints": [
      "Reduce the frame to the requested grain and count how many records fall into each bucket.",
      "Rename the resulting `size` column to `ticket_count`"
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "OUTPUT SHAPE & ORDERING"
    ]
  },
  {
    "id": 31014,
    "order": 14,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Employee Count by Department",
    "description": "You have a DataFrame `df_employees` loaded from the employees dataset.\n\n**Columns:** `employee_id`, `employee_name`, `email`, `salary`, `department_id`, `hire_date`, `country`\n\nCount the number of employees in each `department_id`. Return a DataFrame with columns `department_id` and `employee_count`, sorted by `department_id` ascending.",
    "dataframes": {
      "df_employees": "employees.csv"
    },
    "schema": {
      "df_employees": [
        "employee_id",
        "employee_name",
        "email",
        "salary",
        "department_id",
        "hire_date",
        "country"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_employees):\n    # Count employees per department_id\n    # Return department_id and employee_count sorted by department_id ascending\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_employees):\n    result = df_employees.groupby('department_id', as_index=False).size().rename(columns={'size': 'employee_count'})\n    return result.sort_values('department_id').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_employees):\n    result = df_employees.groupby('department_id', as_index=False).size().rename(columns={'size': 'employee_count'})\n    return result.sort_values('department_id').reset_index(drop=True)",
    "explanation": "`groupby('department_id').size()` counts the number of employee rows per department. Sorting by `department_id` ascending (the default for `sort_values`) produces the expected ordered output.",
    "hints": [
      "Reduce the frame to the requested grain and count how many records fall into each bucket.",
      "Sort by `department_id` ascending (omit `ascending=False` for ascending order)"
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "OUTPUT SHAPE & ORDERING"
    ]
  },
  {
    "id": 31015,
    "order": 15,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Revenue per Product",
    "description": "You have a DataFrame `df_order_items` loaded from the order_items dataset.\n\n**Columns:** `order_item_id`, `order_id`, `product_id`, `quantity`, `unit_price`, `line_amount`\n\nCompute total revenue per `product_id` using `line_amount` (which already equals `unit_price * quantity`). Return columns `product_id` and `total_revenue` (rounded to 2 decimal places), sorted by `total_revenue` descending.",
    "dataframes": {
      "df_order_items": "order_items.csv"
    },
    "schema": {
      "df_order_items": [
        "order_item_id",
        "order_id",
        "product_id",
        "quantity",
        "unit_price",
        "line_amount"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_order_items):\n    # Sum line_amount per product_id to get total_revenue\n    # Return product_id and total_revenue (rounded to 2dp), sorted by total_revenue descending\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_order_items):\n    result = df_order_items.groupby('product_id', as_index=False)['line_amount'].sum().rename(columns={'line_amount': 'total_revenue'})\n    result['total_revenue'] = result['total_revenue'].round(2)\n    return result.sort_values('total_revenue', ascending=False).reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_order_items):\n    result = df_order_items.groupby('product_id', as_index=False)['line_amount'].sum().rename(columns={'line_amount': 'total_revenue'})\n    result['total_revenue'] = result['total_revenue'].round(2)\n    return result.sort_values('total_revenue', ascending=False).reset_index(drop=True)",
    "explanation": "`groupby('product_id')['line_amount'].sum()` sums the pre-computed line revenue per product. Renaming to `total_revenue`, rounding to 2 decimal places, and sorting descending gives the final revenue ranking.",
    "hints": [
      "Collapse the data to the requested grain and total the numeric measure at that level.",
      "After summing, limit the result to two decimal places before returning."
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "OUTPUT SHAPE & ORDERING"
    ]
  },
  {
    "id": 31016,
    "order": 16,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Orders with Missing or Zero Discount",
    "description": "You have a DataFrame `df_orders` loaded from the orders dataset.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `discount_amount`, `net_amount`, `payment_status`\n\nReturn orders where `discount_amount` is null or equal to zero (orders with no discount recorded). Output columns: `order_id`, `user_id`, `order_date`, `discount_amount`. Sort by `order_id` ascending.",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Return orders where discount_amount is null or zero\n    # Columns: order_id, user_id, order_date, discount_amount — sorted by order_id\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    mask = df_orders['discount_amount'].isna() | (df_orders['discount_amount'] == 0)\n    result = df_orders[mask][['order_id', 'user_id', 'order_date', 'discount_amount']]\n    return result.sort_values('order_id').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    mask = df_orders['discount_amount'].isna() | (df_orders['discount_amount'] == 0)\n    result = df_orders[mask][['order_id', 'user_id', 'order_date', 'discount_amount']]\n    return result.sort_values('order_id').reset_index(drop=True)",
    "explanation": "`.isna()` produces a boolean Series that is True for null values. The `|` operator combines it with a zero-value check using OR logic. Wrapping in parentheses ensures correct operator precedence. The resulting mask filters rows that meet either condition — here: orders with no discount amount recorded.",
    "hints": [
      "Build a boolean condition that catches rows where the column is absent and rows where it equals zero, then combine them.",
      "Null detection and zero detection are separate checks — use the OR operator to match either condition."
    ],
    "concepts": [
      "BOOLEAN INDEXING & FILTERING",
      "MISSING VALUE STRATEGY"
    ]
  },
  {
    "id": 31017,
    "order": 17,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Extract Order Date (Date Only)",
    "description": "You have a DataFrame `df_orders` loaded from the orders dataset.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `net_amount`\n\nAdd a new column `order_day` containing only the date part of `order_date` (no time component). Return columns `order_id`, `user_id`, `order_day`, `net_amount`, sorted by `order_day` ascending.",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Add 'order_day' column (date part only from order_date)\n    # Return order_id, user_id, order_day, net_amount sorted by order_day\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['order_date'] = pd.to_datetime(df['order_date'])\n    df['order_day'] = df['order_date'].dt.date\n    result = df[['order_id', 'user_id', 'order_day', 'net_amount']]\n    return result.sort_values('order_day').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['order_date'] = pd.to_datetime(df['order_date'])\n    df['order_day'] = df['order_date'].dt.date\n    result = df[['order_id', 'user_id', 'order_day', 'net_amount']]\n    return result.sort_values('order_day').reset_index(drop=True)",
    "explanation": "After converting to datetime with `pd.to_datetime()`, the `.dt.date` accessor extracts just the calendar date (a `datetime.date` object), stripping the time component. This is useful when you want to group or display by day without the hour/minute/second.",
    "hints": [
      "Normalize the date values into a true time representation before extracting calendar pieces or measuring intervals.",
      "Store the result in a new column called `order_day`"
    ],
    "concepts": [
      "DATETIME OPERATIONS",
      "COLUMN SELECTION & PROJECTION"
    ]
  },
  {
    "id": 31018,
    "order": 18,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Users Registered After 2022",
    "description": "You have a DataFrame `df_users` loaded from the users dataset.\n\n**Columns:** `user_id`, `name`, `email`, `signup_date`, `country`, `is_active`\n\nReturn users where `signup_date` is after 2022-12-31 (i.e. in 2023 or later). Output columns: `user_id`, `name`, `email`, `signup_date`. Sort by `signup_date` ascending.",
    "dataframes": {
      "df_users": "users.csv"
    },
    "schema": {
      "df_users": [
        "user_id",
        "name",
        "email",
        "signup_date",
        "country",
        "acquisition_channel",
        "plan_tier",
        "is_active"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_users):\n    # Return users who signed up after 2022-12-31\n    # Columns: user_id, name, email, signup_date — sorted by signup_date ascending\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_users):\n    df = df_users.copy()\n    df['signup_date'] = pd.to_datetime(df['signup_date'])\n    result = df[df['signup_date'] > '2022-12-31'].sort_values('signup_date')\n    result = result[['user_id', 'name', 'email', 'signup_date']].copy()\n    result['signup_date'] = result['signup_date'].dt.strftime('%Y-%m-%d')\n    return result.reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_users):\n    df = df_users.copy()\n    df['signup_date'] = pd.to_datetime(df['signup_date'])\n    result = df[df['signup_date'] > '2022-12-31'].sort_values('signup_date')\n    result = result[['user_id', 'name', 'email', 'signup_date']].copy()\n    result['signup_date'] = result['signup_date'].dt.strftime('%Y-%m-%d')\n    return result.reset_index(drop=True)",
    "explanation": "After parsing `signup_date` with `pd.to_datetime()`, pandas allows direct string comparisons like `> '2022-12-31'` because it auto-converts the string to a Timestamp for comparison. This is concise and readable.",
    "hints": [
      "Normalize the date values into a true time representation before extracting calendar pieces or measuring intervals.",
      "Once the column is datetime, comparison operators work directly against a date string — pandas handles the string-to-timestamp conversion automatically."
    ],
    "concepts": [
      "DATETIME OPERATIONS",
      "BOOLEAN INDEXING & FILTERING"
    ]
  },
  {
    "id": 31019,
    "order": 19,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Bin users by signup tenure",
    "description": "You have a DataFrame `df_users`.\n\n**Columns:** `user_id`, `name`, `signup_date`, `plan_tier`, `is_active`\n\n1. Convert `signup_date` to datetime and compute `days_since_signup` = days between `signup_date` and `2025-01-01`.\n2. Use `pd.cut` with bins `[0, 90, 365, 730, float('inf')]` and labels `['new', 'recent', 'established', 'veteran']` to create a `tenure_group` column.\n3. Return `user_id`, `plan_tier`, `days_since_signup`, `tenure_group`, sorted by `user_id` ascending.",
    "dataframes": {
      "df_users": "users.csv"
    },
    "schema": {
      "df_users": [
        "user_id",
        "name",
        "email",
        "signup_date",
        "country",
        "acquisition_channel",
        "plan_tier",
        "is_active"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_users):\n    # Compute days_since_signup and bin into tenure_group\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_users):\n    df = df_users.copy()\n    df['signup_date'] = pd.to_datetime(df['signup_date'])\n    ref = pd.to_datetime('2025-01-01')\n    df['days_since_signup'] = (ref - df['signup_date']).dt.days\n    df['tenure_group'] = pd.cut(\n        df['days_since_signup'],\n        bins=[0, 90, 365, 730, float('inf')],\n        labels=['new', 'recent', 'established', 'veteran']\n    )\n    return df[['user_id', 'plan_tier', 'days_since_signup', 'tenure_group']].sort_values('user_id').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_users):\n    df = df_users.copy()\n    df['signup_date'] = pd.to_datetime(df['signup_date'])\n    ref = pd.to_datetime('2025-01-01')\n    df['days_since_signup'] = (ref - df['signup_date']).dt.days\n    df['tenure_group'] = pd.cut(\n        df['days_since_signup'],\n        bins=[0, 90, 365, 730, float('inf')],\n        labels=['new', 'recent', 'established', 'veteran']\n    )\n    return df[['user_id', 'plan_tier', 'days_since_signup', 'tenure_group']].sort_values('user_id').reset_index(drop=True)",
    "explanation": "Subtracting two datetime Series returns a Timedelta Series; `.dt.days` extracts the integer day count. `pd.cut()` bins a numeric Series into labeled intervals — `bins` defines the bin edges (left-exclusive by default), `labels` names each bin. The result is a Categorical Series. This is the standard pattern for converting continuous metrics into discrete segments for cohort analysis.",
    "hints": [
      "Normalize the date values into a true time representation before extracting calendar pieces or measuring intervals.",
      "`pd.cut(series, bins=[...], labels=[...])` assigns each value to a named bin — bin edges define the boundaries"
    ],
    "concepts": [
      "CATEGORICAL & BINNING",
      "DATETIME OPERATIONS"
    ]
  },
  {
    "id": 31020,
    "order": 20,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Categorize orders by amount",
    "description": "You have a DataFrame `df_orders`.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `discount_amount`, `net_amount`, `payment_status`\n\n1. Use `pd.cut` on `net_amount` with bins `[0, 100, 500, 1000, float('inf')]` and labels `['small', 'medium', 'large', 'whale']` to create an `order_size` column.\n2. Count orders per `order_size`. Return a DataFrame with columns `order_size` and `count`, sorted by `order_size` (use the natural category order: small, medium, large, whale).",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Bin net_amount into order_size categories, count per bin\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['order_size'] = pd.cut(\n        df['net_amount'],\n        bins=[0, 100, 500, 1000, float('inf')],\n        labels=['small', 'medium', 'large', 'whale']\n    )\n    result = df.groupby('order_size', observed=True).size().reset_index(name='count')\n    return result",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['order_size'] = pd.cut(\n        df['net_amount'],\n        bins=[0, 100, 500, 1000, float('inf')],\n        labels=['small', 'medium', 'large', 'whale']\n    )\n    result = df.groupby('order_size', observed=True).size().reset_index(name='count')\n    return result",
    "explanation": "`pd.cut()` assigns each `net_amount` to a labeled bin. The result is a Categorical column with an inherent order. `groupby('order_size', observed=True)` groups by the categorical, with `observed=True` to suppress empty-bin warnings in newer pandas versions. `.size()` counts rows per group. The categorical column retains its defined order in the output without an explicit sort.",
    "hints": [
      "Assign each order amount to a labeled tier based on its magnitude, then count how many orders fall into each tier.",
      "`pd.cut` produces the labeled bins; grouping on that column and calling `.size()` gives you the per-bin count."
    ],
    "concepts": [
      "CATEGORICAL & BINNING",
      "GROUPED AGGREGATION"
    ]
  },
  {
    "id": 31021,
    "order": 21,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Filter tickets by keyword in issue type",
    "description": "You have a DataFrame `df_support_tickets`.\n\n**Columns:** `ticket_id`, `user_id`, `created_at`, `issue_type`, `priority`, `status`, `resolution_hours`\n\nReturn all tickets where `issue_type` contains the substring `'billing'` (case-insensitive). Return `ticket_id`, `user_id`, `issue_type`, `status`, `priority`, sorted by `ticket_id` ascending.",
    "dataframes": {
      "df_support_tickets": "support_tickets.csv"
    },
    "schema": {
      "df_support_tickets": [
        "ticket_id",
        "user_id",
        "created_at",
        "issue_type",
        "priority",
        "status",
        "resolution_hours"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_support_tickets):\n    # Filter tickets where issue_type contains 'billing' (case-insensitive)\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_support_tickets):\n    mask = df_support_tickets['issue_type'].str.contains('billing', case=False, na=False)\n    result = df_support_tickets[mask][['ticket_id', 'user_id', 'issue_type', 'status', 'priority']]\n    return result.sort_values('ticket_id').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_support_tickets):\n    mask = df_support_tickets['issue_type'].str.contains('billing', case=False, na=False)\n    result = df_support_tickets[mask][['ticket_id', 'user_id', 'issue_type', 'status', 'priority']]\n    return result.sort_values('ticket_id').reset_index(drop=True)",
    "explanation": "`str.contains(pattern, case=False)` does a case-insensitive substring search, returning a boolean Series. `na=False` ensures NaN values map to False instead of NaN (which would fail boolean indexing). This is the pandas equivalent of SQL `WHERE LOWER(issue_type) LIKE '%billing%'` and is vectorized — no loops needed.",
    "hints": [
      "Apply the same text parsing or matching rule to the whole column instead of looping row by row.",
      "Set `na=False` so that any NaN in issue_type is treated as non-matching rather than raising an error"
    ],
    "concepts": [
      "BOOLEAN INDEXING & FILTERING",
      "COLUMN SELECTION & PROJECTION"
    ]
  },
  {
    "id": 31022,
    "order": 22,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Revenue by year",
    "description": "You have a DataFrame `df_orders`.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `discount_amount`, `net_amount`, `payment_status`\n\n1. Convert `order_date` to datetime and extract the year into a column `year`.\n2. Group by `year` and compute:\n   - `total_revenue`: sum of `net_amount`, rounded to 2 decimal places\n   - `order_count`: number of orders\n3. Return `year`, `total_revenue`, `order_count`, sorted by `year` ascending.",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Extract year from order_date, aggregate per year\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['order_date'] = pd.to_datetime(df['order_date'])\n    df['year'] = df['order_date'].dt.year\n    result = df.groupby('year', as_index=False).agg(\n        total_revenue=('net_amount', 'sum'),\n        order_count=('order_id', 'count')\n    )\n    result['total_revenue'] = result['total_revenue'].round(2)\n    return result.sort_values('year').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['order_date'] = pd.to_datetime(df['order_date'])\n    df['year'] = df['order_date'].dt.year\n    result = df.groupby('year', as_index=False).agg(\n        total_revenue=('net_amount', 'sum'),\n        order_count=('order_id', 'count')\n    )\n    result['total_revenue'] = result['total_revenue'].round(2)\n    return result.sort_values('year').reset_index(drop=True)",
    "explanation": "`pd.to_datetime()` converts the string column to datetime. `.dt.year` extracts the integer year. The named aggregation syntax `agg(col_name=('source_col', 'func'))` produces cleanly named output columns in a single call — cleaner than chaining `.rename()` after plain `.agg()`. This is the preferred modern pattern since pandas 0.25.",
    "hints": [
      "Normalize the date values into a true time representation before extracting calendar pieces or measuring intervals.",
      "Named aggregation in `.agg()` lets you define both the aggregation function and the output column name in a single call, without a separate `.rename()` step."
    ],
    "concepts": [
      "DATETIME OPERATIONS",
      "GROUPED AGGREGATION"
    ]
  },
  {
    "id": 31023,
    "order": 23,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Flag Failed Payments with .loc",
    "description": "You have a DataFrame `df_orders` loaded from the orders dataset.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `discount_amount`, `net_amount`, `payment_status`\n\nAdd a boolean column `needs_review` that is `True` for rows where `payment_status == 'failed'`, and `False` otherwise. Use `.loc` for the assignment to avoid `SettingWithCopyWarning`. Return all columns sorted by `order_id`.",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Add needs_review column using .loc — True where payment_status == 'failed'\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['needs_review'] = False\n    df.loc[df['payment_status'] == 'failed', 'needs_review'] = True\n    return df.sort_values('order_id').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    df = df_orders.copy()\n    df['needs_review'] = False\n    df.loc[df['payment_status'] == 'failed', 'needs_review'] = True\n    return df.sort_values('order_id').reset_index(drop=True)",
    "explanation": "Always call `.copy()` before mutating a slice to avoid `SettingWithCopyWarning`. Initialise `needs_review` to `False` for all rows, then use `.loc[mask, col] = value` to flip it to `True` for matching rows. Chained indexing like `df[mask]['col'] = val` silently modifies a copy — `.loc` is the correct, safe way to set values on a filtered subset.",
    "hints": [
      "Conditional assignment means: work on a frame you own, then write the new value only into the rows that match your condition.",
      "`.loc[boolean_mask, 'column_name'] = value` is the safe pattern for conditional assignment."
    ],
    "concepts": [
      "DEBUG PANDAS",
      "BOOLEAN INDEXING & FILTERING"
    ]
  },
  {
    "id": 31024,
    "order": 24,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Drop Events Without a Product",
    "description": "You have a DataFrame `df_events` loaded from the events dataset.\n\n**Columns:** `event_id`, `session_id`, `user_id`, `event_time`, `event_name`, `product_id`\n\nThe `product_id` column has some null values (events not linked to a product). Remove only rows where `product_id` is null by passing `subset=['product_id']` to `dropna`. Return columns `event_id`, `user_id`, `event_name`, `product_id`, sorted by `event_id`.",
    "dataframes": {
      "df_events": "events.csv"
    },
    "schema": {
      "df_events": [
        "event_id",
        "session_id",
        "user_id",
        "event_time",
        "event_name",
        "product_id"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_events):\n    # Drop rows where product_id is null — use subset to avoid dropping rows with other nulls\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_events):\n    result = df_events.dropna(subset=['product_id'])\n    return result[['event_id', 'user_id', 'event_name', 'product_id']].sort_values('event_id').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_events):\n    result = df_events.dropna(subset=['product_id'])\n    return result[['event_id', 'user_id', 'event_name', 'product_id']].sort_values('event_id').reset_index(drop=True)",
    "explanation": "`.dropna()` with no arguments removes any row with at least one null across all columns — too broad. `.dropna(subset=['product_id'])` removes only rows where `product_id` is null, leaving all other null values in place. This is the precise tool for column-specific null filtering.",
    "hints": [
      "Which rows have a null value in a *specific* column? Think about how you'd target a single column instead of dropping any row with any null.",
      "After filtering, select only the required columns and sort by `event_id`."
    ],
    "concepts": [
      "BOOLEAN INDEXING & FILTERING",
      "MISSING VALUE STRATEGY",
      "COLUMN SELECTION & PROJECTION"
    ]
  },
  {
    "id": 31025,
    "order": 25,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Forward-Fill Missing Resolution Hours",
    "description": "You have a DataFrame `df_support_tickets` loaded from the support_tickets dataset.\n\n**Columns:** `ticket_id`, `user_id`, `created_at`, `issue_type`, `priority`, `status`, `resolution_hours`\n\nOpen or pending tickets have a null `resolution_hours`. Sort the DataFrame by `created_at`, then forward-fill `resolution_hours` so each null is replaced by the most recently seen non-null value. Return columns `ticket_id`, `user_id`, `created_at`, `resolution_hours`, sorted by `created_at`.",
    "dataframes": {
      "df_support_tickets": "support_tickets.csv"
    },
    "schema": {
      "df_support_tickets": [
        "ticket_id",
        "user_id",
        "created_at",
        "issue_type",
        "priority",
        "status",
        "resolution_hours"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_support_tickets):\n    # Sort by created_at, forward-fill resolution_hours\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_support_tickets):\n    df = df_support_tickets.copy()\n    df['created_at'] = pd.to_datetime(df['created_at'])\n    df = df.sort_values('created_at')\n    df['resolution_hours'] = df['resolution_hours'].ffill()\n    return df[['ticket_id', 'user_id', 'created_at', 'resolution_hours']].reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_support_tickets):\n    df = df_support_tickets.copy()\n    df['created_at'] = pd.to_datetime(df['created_at'])\n    df = df.sort_values('created_at')\n    df['resolution_hours'] = df['resolution_hours'].ffill()\n    return df[['ticket_id', 'user_id', 'created_at', 'resolution_hours']].reset_index(drop=True)",
    "explanation": "`.ffill()` (forward fill) replaces each null with the last observed non-null value in the column, scanning downward in row order. Sorting by `created_at` first ensures the fill travels in chronological order — each null gets the most recently resolved ticket's hours as its estimate. `.bfill()` does the reverse: fills from below.",
    "hints": [
      "Sort before filling so the propagation travels in the direction the data is ordered — here, chronological.",
      "`.ffill()` replaces each null with the last non-null value seen above it in the column."
    ],
    "concepts": [
      "MISSING VALUE STRATEGY",
      "DATETIME OPERATIONS"
    ]
  },
  {
    "id": 31026,
    "order": 26,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Event Count per Product Including Null Group",
    "description": "You have a DataFrame `df_events` loaded from the events dataset.\n\n**Columns:** `event_id`, `session_id`, `user_id`, `event_time`, `event_name`, `product_id`\n\nCount the number of events per `product_id`. Include events where `product_id` is null as their own group (a `NaN` row in the output). Return columns `product_id` and `event_count`, sorted by `event_count` descending.",
    "dataframes": {
      "df_events": "events.csv"
    },
    "schema": {
      "df_events": [
        "event_id",
        "session_id",
        "user_id",
        "event_time",
        "event_name",
        "product_id"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_events):\n    # Count events per product_id — include the null group\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_events):\n    result = (\n        df_events\n        .groupby('product_id', dropna=False, as_index=False)\n        .size()\n        .rename(columns={'size': 'event_count'})\n    )\n    return result.sort_values('event_count', ascending=False).reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_events):\n    result = (\n        df_events\n        .groupby('product_id', dropna=False, as_index=False)\n        .size()\n        .rename(columns={'size': 'event_count'})\n    )\n    return result.sort_values('event_count', ascending=False).reset_index(drop=True)",
    "explanation": "By default `groupby` silently drops NaN keys. Passing `dropna=False` keeps null keys as a separate group — the same behaviour as SQL `GROUP BY` which includes NULL rows. `as_index=False` returns `product_id` as a regular column instead of the index, keeping the output as a flat DataFrame.",
    "hints": [
      "What happens to rows where the *grouping key* itself is null — are they silently dropped or kept? Check whether your aggregation includes or excludes null keys by default.",
      "`as_index=False` keeps the group key as a regular column rather than promoting it to the index."
    ],
    "concepts": [
      "MISSING VALUE STRATEGY",
      "GROUPED AGGREGATION"
    ]
  },
  {
    "id": 31027,
    "order": 27,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Average Order Value by Status",
    "description": "You have a DataFrame `df_orders` loaded from the orders dataset.\n\n**Columns:** `order_id`, `user_id`, `order_date`, `status`, `gross_amount`, `discount_amount`, `net_amount`, `payment_status`\n\nCompute the average `net_amount` per `status` using named aggregation and `as_index=False`. Round the result to 2 decimal places. Return columns `status` and `avg_net_amount`, sorted by `status` alphabetically.",
    "dataframes": {
      "df_orders": "orders.csv"
    },
    "schema": {
      "df_orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "gross_amount",
        "discount_amount",
        "net_amount",
        "payment_status"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_orders):\n    # Average net_amount per status — use as_index=False and named aggregation\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_orders):\n    result = df_orders.groupby('status', as_index=False).agg(\n        avg_net_amount=('net_amount', 'mean')\n    )\n    result['avg_net_amount'] = result['avg_net_amount'].round(2)\n    return result.sort_values('status').reset_index(drop=True)",
    "solution_code": "import pandas as pd\n\ndef solve(df_orders):\n    result = df_orders.groupby('status', as_index=False).agg(\n        avg_net_amount=('net_amount', 'mean')\n    )\n    result['avg_net_amount'] = result['avg_net_amount'].round(2)\n    return result.sort_values('status').reset_index(drop=True)",
    "explanation": "`groupby('status', as_index=False)` keeps `status` as a regular column in the output. Named aggregation syntax `agg(output_col=('source_col', 'func'))` names the result column directly, avoiding a separate `.rename()` call. This is the modern pandas pattern for clean, readable groupby aggregations since pandas 0.25.",
    "hints": [
      "You want one average per status — a clean two-column result keyed by status.",
      "Named aggregation `agg(output_col=('source_col', 'func'))` lets you name the output column in the same call; `as_index=False` keeps the group key as a column."
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "COLUMN SELECTION & PROJECTION"
    ]
  },
  {
    "id": 31028,
    "order": 28,
    "topic": "python_data",
    "difficulty": "easy",
    "title": "Sessions by channel group",
    "description": "You have a DataFrame `df_sessions` loaded from the sessions dataset.\n\n**Columns:** `session_id`, `user_id`, `session_start`, `device_type`, `traffic_source`, `country`\n\nThe `traffic_source` column contains values like `paid_search`, `paid_social`, `organic`, `direct`, `referral`, `email`. Some sources are compound (separated by `_`); others are single words.\n\nCreate a `channel_group` column that takes only the **prefix before the first underscore** (or the whole value if there is no underscore). Then count the number of sessions per `channel_group`. Return a DataFrame with columns `channel_group` and `session_count`, sorted by `session_count` descending.",
    "dataframes": {
      "df_sessions": "sessions.csv"
    },
    "schema": {
      "df_sessions": [
        "session_id",
        "user_id",
        "session_start",
        "device_type",
        "traffic_source",
        "country"
      ]
    },
    "starter_code": "import pandas as pd\n\ndef solve(df_sessions):\n    # Your code here\n    pass",
    "expected_code": "import pandas as pd\n\ndef solve(df_sessions):\n    df_sessions = df_sessions.copy()\n    df_sessions['channel_group'] = df_sessions['traffic_source'].str.split('_').str[0]\n    result = (\n        df_sessions.groupby('channel_group', as_index=False)\n        .agg(session_count=('session_id', 'count'))\n        .sort_values('session_count', ascending=False)\n        .reset_index(drop=True)\n    )\n    return result",
    "solution_code": "import pandas as pd\n\ndef solve(df_sessions):\n    df_sessions = df_sessions.copy()\n    df_sessions['channel_group'] = df_sessions['traffic_source'].str.split('_').str[0]\n    result = (\n        df_sessions.groupby('channel_group', as_index=False)\n        .agg(session_count=('session_id', 'count'))\n        .sort_values('session_count', ascending=False)\n        .reset_index(drop=True)\n    )\n    return result",
    "explanation": "`.str.split('_')` splits each value into a list on underscores; `.str[0]` takes the first element. This normalises `paid_search` and `paid_social` into the same `paid` group in one vectorized call. The groupby + named aggregation counts sessions per group; descending sort surfaces the largest channel first.",
    "hints": [
      "The traffic_source values follow a pattern: a prefix word, then a separator, then optional detail. Which string accessor lets you split on a separator and pick the first part?",
      "A named aggregation — `agg(session_count=('session_id', 'count'))` — gives the output column its final name in one step."
    ],
    "concepts": [
      "COLUMN SELECTION & PROJECTION",
      "GROUPED AGGREGATION"
    ]
  }
]