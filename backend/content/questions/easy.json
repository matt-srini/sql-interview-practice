[
  {
    "id": 11001,
    "order": 1,
    "title": "Completed orders",
    "description": "Return order_id, user_id, order_date, and net_amount for all orders where status is 'completed'. Order by order_id ascending.",
    "difficulty": "easy",
    "schema": {
      "orders": [
        "order_id",
        "user_id",
        "order_date",
        "status",
        "net_amount"
      ]
    },
    "dataset_files": [
      "orders.csv"
    ],
    "expected_query": "SELECT order_id, user_id, order_date, net_amount\nFROM orders\nWHERE status = 'completed'\nORDER BY order_id",
    "solution_query": "SELECT order_id, user_id, order_date, net_amount\nFROM orders\nWHERE status = 'completed'\nORDER BY order_id;",
    "explanation": "The WHERE clause filters rows before they are returned. Only orders where the status column equals the string 'completed' pass the filter. String comparisons are case-sensitive in most databases, so the value must match exactly. The status column is included in the schema because it is used in the WHERE clause even though it does not appear in the SELECT list. ORDER BY order_id ASC produces a deterministic result.",
    "hints": [
      "Decide which rows qualify before you project columns or worry about the final ordering.",
      "String comparisons are case-sensitive — match the exact value in the data"
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "DETERMINISTIC RESULT ORDERING"
    ],
    "companies": [
      "Amazon",
      "Shopify"
    ]
  },
  {
    "id": 11002,
    "order": 2,
    "title": "US users",
    "description": "Return user_id, name, and email for all users from the United States (country = 'US'). Order by user_id ascending.",
    "difficulty": "easy",
    "schema": {
      "users": [
        "user_id",
        "name",
        "email",
        "country"
      ]
    },
    "dataset_files": [
      "users.csv"
    ],
    "expected_query": "SELECT user_id, name, email\nFROM users\nWHERE country = 'US'\nORDER BY user_id",
    "solution_query": "SELECT user_id, name, email\nFROM users\nWHERE country = 'US'\nORDER BY user_id;",
    "explanation": "WHERE country = 'US' applies a string equality filter that keeps only rows whose country column exactly matches 'US'. The country column is included in the schema because it is the filter column, even though it does not appear in the SELECT list. ORDER BY user_id ensures a deterministic output.",
    "hints": [
      "Decide which rows qualify before you project columns or worry about the final ordering.",
      "String values in WHERE clauses must be wrapped in single quotes"
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "DETERMINISTIC RESULT ORDERING"
    ],
    "companies": [
      "Meta",
      "Google"
    ]
  },
  {
    "id": 11003,
    "order": 3,
    "title": "High-salary employees",
    "description": "Return employee_id, employee_name, and salary for all employees with a salary greater than 80000. Order by salary descending.",
    "difficulty": "easy",
    "schema": {
      "employees": [
        "employee_id",
        "employee_name",
        "salary"
      ]
    },
    "dataset_files": [
      "employees.csv"
    ],
    "expected_query": "SELECT employee_id, employee_name, salary\nFROM employees\nWHERE salary > 80000\nORDER BY salary DESC",
    "solution_query": "SELECT employee_id, employee_name, salary\nFROM employees\nWHERE salary > 80000\nORDER BY salary DESC;",
    "explanation": "WHERE salary > 80000 is a numeric comparison filter. Only employees whose salary value is strictly greater than 80000 are included. Numeric comparisons do not require quotes around the value. ORDER BY salary DESC places the highest earners at the top of the result.",
    "hints": [
      "Identify the salary threshold in the prompt and filter rows to those strictly above it.",
      "Descending order on a numeric column surfaces the largest values first — here the direction follows naturally from 'high salary'."
    ],
    "concepts": [
      "NUMERIC COMPARISON FILTER",
      "DESCENDING RESULT ORDERING"
    ],
    "companies": [
      "Google",
      "Microsoft"
    ]
  },
  {
    "id": 11004,
    "order": 4,
    "title": "Employees by hire date",
    "description": "Return employee_id, employee_name, and hire_date for employees with a known hire_date. Order by hire_date ascending to show the longest-tenured employees first.",
    "difficulty": "easy",
    "schema": {
      "employees": [
        "employee_id",
        "employee_name",
        "hire_date"
      ]
    },
    "dataset_files": [
      "employees.csv"
    ],
    "expected_query": "SELECT employee_id, employee_name, hire_date\nFROM employees\nWHERE hire_date IS NOT NULL\nORDER BY hire_date ASC",
    "solution_query": "SELECT employee_id, employee_name, hire_date\nFROM employees\nWHERE hire_date IS NOT NULL\nORDER BY hire_date ASC;",
    "explanation": "WHERE hire_date IS NOT NULL filters out rows that do not have a known hire date before sorting begins. After that filter, ORDER BY hire_date ASC sorts the remaining employees from the earliest hire date to the most recent. This keeps the result focused on employees whose tenure can be evaluated and makes the final row order deterministic.",
    "hints": [
      "Decide what to do with rows whose hire_date is unknown before you pick a sort order — undated rows can't anchor a chronological result.",
      "Date columns sort chronologically — ASC means earliest first"
    ],
    "concepts": [
      "NULL AWARENESS",
      "ASCENDING RESULT ORDERING"
    ],
    "companies": [
      "Google",
      "LinkedIn"
    ]
  },
  {
    "id": 11005,
    "order": 5,
    "title": "Total order count",
    "description": "Return the total number of orders in the orders table as a single value aliased as total_orders.",
    "difficulty": "easy",
    "schema": {
      "orders": [
        "order_id"
      ]
    },
    "dataset_files": [
      "orders.csv"
    ],
    "expected_query": "SELECT COUNT(order_id) AS total_orders\nFROM orders",
    "solution_query": "SELECT COUNT(order_id) AS total_orders\nFROM orders;",
    "explanation": "COUNT(order_id) counts all non-NULL values of order_id. Because order_id is the primary key and is never NULL, this equals the total number of rows in the table. No GROUP BY is needed since we want a single aggregate across the entire table. The AS alias gives the output column a meaningful name.",
    "hints": [
      "Collapse the qualifying rows into a single summary value rather than returning row-level detail.",
      "No GROUP BY is needed when aggregating the entire table into a single result"
    ],
    "concepts": [
      "COLUMN PROJECTION",
      "ROW COUNT AGGREGATION"
    ],
    "companies": [
      "Amazon",
      "Shopify"
    ]
  },
  {
    "id": 11006,
    "order": 6,
    "title": "Most expensive product",
    "description": "Return the highest price among all products as a single value aliased as max_price.",
    "difficulty": "easy",
    "schema": {
      "products": [
        "price"
      ]
    },
    "dataset_files": [
      "products.csv"
    ],
    "expected_query": "SELECT MAX(price) AS max_price\nFROM products",
    "solution_query": "SELECT MAX(price) AS max_price\nFROM products;",
    "explanation": "MAX(price) scans all rows in the products table and returns the single highest value in the price column. NULL values are automatically ignored by MAX. No GROUP BY is needed since we want a single aggregate across the entire table. The result is one row with one column.",
    "hints": [
      "Reduce the entire table to a single row by finding the extreme value of the price column.",
      "`MAX()` returns the largest value in a column."
    ],
    "concepts": [
      "COLUMN PROJECTION",
      "EXTREMUM IDENTIFICATION"
    ],
    "companies": [
      "Amazon",
      "eBay"
    ]
  },
  {
    "id": 11007,
    "order": 7,
    "title": "Fastest ticket resolution",
    "description": "Return the minimum resolution_hours from all 'resolved' support tickets as a single value aliased as min_resolution_hours. Only include tickets where status = 'resolved'.",
    "difficulty": "easy",
    "schema": {
      "support_tickets": [
        "status",
        "resolution_hours"
      ]
    },
    "dataset_files": [
      "support_tickets.csv"
    ],
    "expected_query": "SELECT MIN(resolution_hours) AS min_resolution_hours\nFROM support_tickets\nWHERE status = 'resolved'",
    "solution_query": "SELECT MIN(resolution_hours) AS min_resolution_hours\nFROM support_tickets\nWHERE status = 'resolved';",
    "explanation": "WHERE status = 'resolved' filters the table to only closed tickets before MIN runs. MIN(resolution_hours) returns the smallest resolution time among those rows — the ticket that was resolved fastest. Filtering first ensures we do not include NULL or irrelevant resolution_hours from tickets that are not yet resolved.",
    "hints": [
      "Not every ticket qualifies — narrow the table to the right subset first, then take the minimum across just those rows.",
      "MIN() returns the smallest value in a column"
    ],
    "concepts": [
      "LOWEST-VALUE IDENTIFICATION",
      "CONDITIONAL ROW FILTERING"
    ],
    "companies": [
      "Zendesk",
      "Salesforce"
    ]
  },
  {
    "id": 11008,
    "order": 8,
    "title": "Orders by status",
    "description": "Return each distinct order status and the count of orders with that status as order_count. Order by order_count descending.",
    "difficulty": "easy",
    "schema": {
      "orders": [
        "order_id",
        "status"
      ]
    },
    "dataset_files": [
      "orders.csv"
    ],
    "expected_query": "SELECT status, COUNT(order_id) AS order_count\nFROM orders\nGROUP BY status\nORDER BY order_count DESC",
    "solution_query": "SELECT status, COUNT(order_id) AS order_count\nFROM orders\nGROUP BY status\nORDER BY order_count DESC;",
    "explanation": "GROUP BY status groups all order rows that share the same status value into a single group. COUNT(order_id) counts the rows in each group. Every non-aggregated column in the SELECT list must appear in the GROUP BY clause — here that is status. ORDER BY order_count DESC lists the most common statuses first.",
    "hints": [
      "Collapse the data to the business grain named in the result before you calculate the summary values.",
      "Once you have a count per group, the sort order should put the most frequent category at the top — think about which direction achieves that."
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "ROW COUNT AGGREGATION"
    ],
    "companies": [
      "Amazon",
      "Shopify"
    ]
  },
  {
    "id": 11009,
    "order": 9,
    "title": "Total spend per user",
    "description": "Return each user_id and the total net_amount they have spent across all their orders as total_spent. Order by total_spent descending.",
    "difficulty": "easy",
    "schema": {
      "orders": [
        "user_id",
        "net_amount"
      ]
    },
    "dataset_files": [
      "orders.csv"
    ],
    "expected_query": "SELECT user_id, SUM(net_amount) AS total_spent\nFROM orders\nGROUP BY user_id\nORDER BY total_spent DESC",
    "solution_query": "SELECT user_id, SUM(net_amount) AS total_spent\nFROM orders\nGROUP BY user_id\nORDER BY total_spent DESC;",
    "explanation": "GROUP BY user_id groups all orders belonging to the same user. SUM(net_amount) sums the net_amount for each group, giving the total spend per user. Users with multiple orders have their amounts summed together. ORDER BY total_spent DESC puts the highest spenders at the top.",
    "hints": [
      "Group rows by the user entity, then sum the spend column within each group.",
      "SUM(net_amount) totals spending per group"
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "VALUE AGGREGATION"
    ],
    "companies": [
      "Amazon",
      "Stripe"
    ]
  },
  {
    "id": 11010,
    "order": 10,
    "title": "Support tickets by issue type",
    "description": "Return each issue_type and the count of support tickets with that type as ticket_count. Order by ticket_count descending.",
    "difficulty": "easy",
    "schema": {
      "support_tickets": [
        "ticket_id",
        "issue_type"
      ]
    },
    "dataset_files": [
      "support_tickets.csv"
    ],
    "expected_query": "SELECT issue_type, COUNT(ticket_id) AS ticket_count\nFROM support_tickets\nGROUP BY issue_type\nORDER BY ticket_count DESC",
    "solution_query": "SELECT issue_type, COUNT(ticket_id) AS ticket_count\nFROM support_tickets\nGROUP BY issue_type\nORDER BY ticket_count DESC;",
    "explanation": "GROUP BY issue_type creates one aggregate group per issue type, and COUNT(ticket_id) measures how many tickets fall into each group. ORDER BY ticket_count DESC shows the most common issue categories first. This is the standard distribution query for understanding the breakdown of a categorical support column.",
    "hints": [
      "Collapse the data to the business grain named in the result before you calculate the summary values.",
      "Once you have a count per group, the sort order should put the most frequent category at the top — think about which direction achieves that."
    ],
    "concepts": [
      "GROUPED AGGREGATION",
      "ROW COUNT AGGREGATION"
    ],
    "companies": [
      "Zendesk",
      "Salesforce"
    ]
  },
  {
    "id": 11011,
    "order": 11,
    "title": "Order count per user",
    "description": "Return user_id, name, and the total number of orders each user has placed as order_count. Include users who have placed zero orders. Order by user_id ascending.",
    "difficulty": "easy",
    "schema": {
      "users": [
        "user_id",
        "name"
      ],
      "orders": [
        "order_id",
        "user_id"
      ]
    },
    "dataset_files": [
      "users.csv",
      "orders.csv"
    ],
    "expected_query": "SELECT u.user_id, u.name, COUNT(o.order_id) AS order_count\nFROM users u\nLEFT JOIN orders o ON u.user_id = o.user_id\nGROUP BY u.user_id, u.name\nORDER BY u.user_id",
    "solution_query": "SELECT u.user_id, u.name, COUNT(o.order_id) AS order_count\nFROM users u\nLEFT JOIN orders o ON u.user_id = o.user_id\nGROUP BY u.user_id, u.name\nORDER BY u.user_id;",
    "explanation": "A LEFT JOIN keeps every row from the users table (the left side) even if there is no matching row in orders. COUNT(o.order_id) counts non-NULL order_id values per group — users with no orders will have NULL for o.order_id, so COUNT returns 0 for them, not NULL. GROUP BY u.user_id, u.name is required because u.name is a non-aggregated column in the SELECT list. ORDER BY u.user_id gives a deterministic result.",
    "hints": [
      "Bring together the sources that hold the required attributes, then summarize at the business grain named in the prompt.",
      "COUNT(o.order_id) returns 0 for users with no orders because NULL values are not counted by COUNT"
    ],
    "concepts": [
      "OPTIONAL ENTITY PRESERVATION",
      "ROW COUNT AGGREGATION",
      "GROUPED AGGREGATION"
    ],
    "companies": [
      "Amazon",
      "Shopify"
    ]
  },
  {
    "id": 11012,
    "order": 12,
    "title": "Employees with department names",
    "description": "Return employee_id, employee_name, salary, and department_name for every employee and their department. Order by employee_id ascending.",
    "difficulty": "easy",
    "schema": {
      "employees": [
        "employee_id",
        "employee_name",
        "salary",
        "department_id"
      ],
      "departments": [
        "department_id",
        "department_name"
      ]
    },
    "dataset_files": [
      "employees.csv",
      "departments.csv"
    ],
    "expected_query": "SELECT e.employee_id, e.employee_name, e.salary, d.department_name\nFROM employees e\nJOIN departments d ON e.department_id = d.department_id\nORDER BY e.employee_id",
    "solution_query": "SELECT e.employee_id, e.employee_name, e.salary, d.department_name\nFROM employees e\nJOIN departments d ON e.department_id = d.department_id\nORDER BY e.employee_id;",
    "explanation": "An INNER JOIN (written as JOIN) links each employee row to its department using the shared foreign key department_id. Only employees that have a matching department_id in the departments table are returned. Table aliases (e, d) keep column references short and unambiguous when querying multiple tables. ORDER BY e.employee_id produces a deterministic result.",
    "hints": [
      "Combine the sources that hold the needed attributes before you filter or shape the final result.",
      "When pulling columns from two tables, the query needs a way to distinguish which table each column comes from — short labels for each table prevent ambiguity."
    ],
    "concepts": [
      "REQUIRED ENTITY MATCHING",
      "DETERMINISTIC RESULT ORDERING"
    ],
    "companies": [
      "Google",
      "Microsoft"
    ]
  },
  {
    "id": 11013,
    "order": 13,
    "title": "Products in price range",
    "description": "Return product_id, product_name, and price for all products with a price between 100 and 500 inclusive. Order by price ascending.",
    "difficulty": "easy",
    "schema": {
      "products": [
        "product_id",
        "product_name",
        "price"
      ]
    },
    "dataset_files": [
      "products.csv"
    ],
    "expected_query": "SELECT product_id, product_name, price\nFROM products\nWHERE price BETWEEN 100 AND 500\nORDER BY price",
    "solution_query": "SELECT product_id, product_name, price\nFROM products\nWHERE price BETWEEN 100 AND 500\nORDER BY price;",
    "explanation": "BETWEEN 100 AND 500 is a range filter that is inclusive on both ends — it is equivalent to WHERE price >= 100 AND price <= 500. This syntax is more readable than writing two separate conditions. ORDER BY price ASC arranges results from cheapest to most expensive within the range.",
    "hints": [
      "Narrow the rows to those that fall within the price bounds stated in the prompt.",
      "BETWEEN is equivalent to >= lower AND <= upper"
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "RANGE FILTERING"
    ],
    "companies": [
      "Amazon",
      "eBay"
    ]
  },
  {
    "id": 11014,
    "order": 14,
    "title": "Users who signed up in 2024",
    "description": "Return user_id, name, and signup_date for all users who signed up during the year 2024. Order by signup_date ascending.",
    "difficulty": "easy",
    "schema": {
      "users": [
        "user_id",
        "name",
        "signup_date"
      ]
    },
    "dataset_files": [
      "users.csv"
    ],
    "expected_query": "SELECT user_id, name, signup_date\nFROM users\nWHERE signup_date >= '2024-01-01'\n  AND signup_date < '2025-01-01'\nORDER BY signup_date",
    "solution_query": "SELECT user_id, name, signup_date\nFROM users\nWHERE signup_date >= '2024-01-01'\n  AND signup_date < '2025-01-01'\nORDER BY signup_date;",
    "explanation": "A half-open date range (>= '2024-01-01' AND < '2025-01-01') captures every date within the year 2024 without depending on time components. This approach is more portable than using YEAR() or EXTRACT() functions, which have slightly different syntax across PostgreSQL, MySQL, and SQL Server. ORDER BY signup_date ASC lists users in chronological order.",
    "hints": [
      "Restrict rows to the signup year named in the prompt before selecting the output columns.",
      "Compare signup_date to date string literals in 'YYYY-MM-DD' format"
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "DATE-BASED FILTERING"
    ],
    "companies": [
      "Meta",
      "Airbnb"
    ]
  },
  {
    "id": 11015,
    "order": 15,
    "title": "Users in selected countries",
    "description": "Return user_id, name, and country for users whose country is 'UK', 'IN', or 'DE'.",
    "difficulty": "easy",
    "schema": {
      "users": [
        "user_id",
        "name",
        "country"
      ]
    },
    "dataset_files": [
      "users.csv"
    ],
    "expected_query": "SELECT user_id, name, country\nFROM users\nWHERE country IN ('UK', 'IN', 'DE')",
    "solution_query": "SELECT user_id, name, country\nFROM users\nWHERE country IN ('UK', 'IN', 'DE');",
    "explanation": "The IN operator checks whether a column matches any value in a short list of exact options. Here it keeps only users whose country is UK, IN, or DE without repeating the country column in several OR conditions. This is easier to read and maintain than writing multiple equality checks. The query returns only the requested identifying columns, which keeps the output focused on the business question.",
    "hints": [
      "Use the country list in the prompt to match only qualifying rows.",
      "Put the allowed country codes inside parentheses after IN"
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "SET MEMBERSHIP FILTERING"
    ],
    "tags": [
      "easy",
      "users",
      "filtering",
      "in"
    ],
    "companies": [
      "Meta",
      "Airbnb"
    ]
  },
  {
    "id": 11016,
    "order": 16,
    "title": "Products in selected categories",
    "description": "Return product_id, product_name, and category_id for products whose category_id is 3, 8, or 14. Order by category_id ascending.",
    "difficulty": "easy",
    "schema": {
      "products": [
        "product_id",
        "product_name",
        "category_id"
      ]
    },
    "dataset_files": [
      "products.csv"
    ],
    "expected_query": "SELECT product_id, product_name, category_id\nFROM products\nWHERE category_id IN (3, 8, 14)\nORDER BY category_id ASC",
    "solution_query": "SELECT product_id, product_name, category_id\nFROM products\nWHERE category_id IN (3, 8, 14)\nORDER BY category_id ASC;",
    "explanation": "This query filters products to a small set of category IDs using IN. IN is the right choice when the same column needs to match one of several exact values, because it is more compact and clearer than chaining multiple OR comparisons. ORDER BY category_id ASC then groups the matching categories into a predictable sequence for review. Only the requested product columns are selected, so the result stays aligned with the prompt.",
    "hints": [
      "Match rows against the exact set of category values listed in the prompt.",
      "After identifying the qualifying rows, look at what ordering the problem specifies for the final output."
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "SET MEMBERSHIP FILTERING"
    ],
    "tags": [
      "easy",
      "products",
      "filtering",
      "in"
    ],
    "companies": [
      "Amazon"
    ]
  },
  {
    "id": 11017,
    "order": 17,
    "title": "Checkout-related events",
    "description": "Return event_id, user_id, and event_name for events whose event_name contains the word 'checkout'.",
    "difficulty": "easy",
    "schema": {
      "events": [
        "event_id",
        "user_id",
        "event_name"
      ]
    },
    "dataset_files": [
      "events.csv"
    ],
    "expected_query": "SELECT event_id, user_id, event_name\nFROM events\nWHERE event_name LIKE '%checkout%'",
    "solution_query": "SELECT event_id, user_id, event_name\nFROM events\nWHERE event_name LIKE '%checkout%';",
    "explanation": "The LIKE pattern '%checkout%' finds rows where the text 'checkout' appears anywhere inside event_name. The percent sign before and after the word means characters are allowed on both sides of the match, which makes this a contains search rather than a prefix or suffix search. This is useful when business event names follow a naming convention and you want to capture a family of related actions. The query then returns the key identifying fields for each matching event row.",
    "hints": [
      "Use a pattern match to isolate events whose names share the qualifying substring.",
      "Filter event_name with LIKE before selecting the event columns"
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "PATTERN-BASED FILTERING"
    ],
    "tags": [
      "easy",
      "events",
      "pattern-matching",
      "like"
    ],
    "companies": [
      "Meta",
      "Amazon"
    ]
  },
  {
    "id": 11018,
    "order": 18,
    "title": "Orders that need review",
    "description": "Return order_id, status, payment_status, and net_amount for orders where the order status is 'cancelled' or the payment_status is 'failed'.",
    "difficulty": "easy",
    "schema": {
      "orders": [
        "order_id",
        "status",
        "payment_status",
        "net_amount"
      ]
    },
    "dataset_files": [
      "orders.csv"
    ],
    "expected_query": "SELECT order_id, status, payment_status, net_amount\nFROM orders\nWHERE status = 'cancelled' OR payment_status = 'failed'",
    "solution_query": "SELECT order_id, status, payment_status, net_amount\nFROM orders\nWHERE status = 'cancelled' OR payment_status = 'failed';",
    "explanation": "OR makes the filter broader by keeping rows that satisfy either condition. In this case, an order appears in the result if it was cancelled, if its payment failed, or if both things are true for the same row. This is different from AND, which would require both conditions to be true at once. Returning both status columns helps explain why each order qualifies for the review list.",
    "hints": [
      "Two columns each carry a reason a row might need review — the requirement is to keep rows satisfying at least one reason, not only rows satisfying both.",
      "Both status columns belong in the result because both are part of the business rule"
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "PRE-AGGREGATION FILTERING"
    ],
    "tags": [
      "easy",
      "orders",
      "logical-operators",
      "or"
    ],
    "companies": [
      "Amazon",
      "Stripe"
    ]
  },
  {
    "id": 11019,
    "order": 19,
    "title": "US paid users on higher tiers",
    "description": "Return user_id, name, country, and plan_tier for users who are in 'US' and whose plan_tier is either 'pro' or 'enterprise'.",
    "difficulty": "easy",
    "schema": {
      "users": [
        "user_id",
        "name",
        "country",
        "plan_tier"
      ]
    },
    "dataset_files": [
      "users.csv"
    ],
    "expected_query": "SELECT user_id, name, country, plan_tier\nFROM users\nWHERE country = 'US'\n  AND (plan_tier = 'pro' OR plan_tier = 'enterprise')",
    "solution_query": "SELECT user_id, name, country, plan_tier\nFROM users\nWHERE country = 'US'\n  AND (plan_tier = 'pro' OR plan_tier = 'enterprise');",
    "explanation": "This filter combines AND and OR to express a more precise business rule. The user must first be in the US, and then their plan_tier must be either pro or enterprise. Parentheses matter because they keep the two plan_tier checks together as one grouped condition. Without the parentheses, SQL operator precedence could change the result and allow rows that do not satisfy the full rule.",
    "hints": [
      "The qualifying rows must satisfy a geography condition and one of two tier conditions at the same time.",
      "Wrap the OR conditions in parentheses so they stay grouped together"
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "MULTI-CONDITION FILTERING",
      "PRE-AGGREGATION FILTERING"
    ],
    "tags": [
      "easy",
      "users",
      "logical-operators",
      "and-or"
    ],
    "companies": [
      "Meta",
      "Airbnb"
    ]
  },
  {
    "id": 11020,
    "order": 20,
    "title": "Users missing an email address",
    "description": "Return user_id, name, and country for users whose email is missing.",
    "difficulty": "easy",
    "schema": {
      "users": [
        "user_id",
        "name",
        "country",
        "email"
      ]
    },
    "dataset_files": [
      "users.csv"
    ],
    "expected_query": "SELECT user_id, name, country\nFROM users\nWHERE email IS NULL",
    "solution_query": "SELECT user_id, name, country\nFROM users\nWHERE email IS NULL;",
    "explanation": "NULL represents missing or unknown data, so it cannot be tested with = NULL. The correct SQL syntax is IS NULL, which matches rows where the column truly has no value. Here email is used only in the filter while the output focuses on user identifiers and country. This is a common data-quality query for finding incomplete customer records.",
    "hints": [
      "Filter to rows in which a specific column carries no value, rather than testing for a string match.",
      "Missing values can't be matched with `=` — use the dedicated NULL-comparison operator."
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "NULL STATE FILTERING"
    ],
    "tags": [
      "easy",
      "users",
      "null-handling",
      "is-null"
    ],
    "companies": [
      "Meta"
    ]
  },
  {
    "id": 11021,
    "order": 21,
    "title": "Events linked to a product",
    "description": "Return event_id, user_id, event_name, and product_id for events where product_id is present.",
    "difficulty": "easy",
    "schema": {
      "events": [
        "event_id",
        "user_id",
        "event_name",
        "product_id"
      ]
    },
    "dataset_files": [
      "events.csv"
    ],
    "expected_query": "SELECT event_id, user_id, event_name, product_id\nFROM events\nWHERE product_id IS NOT NULL",
    "solution_query": "SELECT event_id, user_id, event_name, product_id\nFROM events\nWHERE product_id IS NOT NULL;",
    "explanation": "IS NOT NULL keeps only rows where the column contains a real value. In the events table, that means the event is associated with a specific product instead of leaving product_id blank. This is the correct SQL form for checking populated fields, because comparisons like <> NULL do not work the way beginners often expect. The query returns the event fields needed to inspect those product-linked actions.",
    "hints": [
      "Select rows that have a populated value in the linking column rather than those with no association.",
      "To exclude rows where a column has no value, use the dedicated NOT-NULL test rather than `<> NULL`."
    ],
    "concepts": [
      "CONDITIONAL ROW FILTERING",
      "KNOWN-VALUE FILTERING"
    ],
    "tags": [
      "easy",
      "events",
      "null-handling",
      "is-not-null"
    ],
    "companies": [
      "Meta",
      "Amazon"
    ]
  },
  {
    "id": 11022,
    "order": 22,
    "title": "Total payment amount for failed or refunded payments",
    "description": "Return status and the total payment amount for that status as total_amount, but only for payments where status is 'failed' or 'refunded'. Order by total_amount descending, then status ascending.",
    "difficulty": "easy",
    "schema": {
      "payments": [
        "status",
        "amount"
      ]
    },
    "dataset_files": [
      "payments.csv"
    ],
    "expected_query": "SELECT status, SUM(amount) AS total_amount\nFROM payments\nWHERE status = 'failed' OR status = 'refunded'\nGROUP BY status\nORDER BY total_amount DESC, status",
    "solution_query": "SELECT status, SUM(amount) AS total_amount\nFROM payments\nWHERE status = 'failed' OR status = 'refunded'\nGROUP BY status\nORDER BY total_amount DESC, status;",
    "explanation": "The WHERE clause uses OR to keep only the two requested payment statuses. After that filter is applied, GROUP BY status creates one group for each remaining status value and SUM(amount) adds the payment amounts within each group. This is a good beginner example of combining a multi-value filter with a simple aggregation. ORDER BY total_amount DESC makes the grouped output deterministic and surfaces the larger total first.",
    "hints": [
      "Narrow to the two qualifying statuses first, then aggregate separately for each one.",
      "After filtering, group by status and sum the amount column"
    ],
    "concepts": [
      "PRE-AGGREGATION FILTERING",
      "GROUPED AGGREGATION"
    ],
    "tags": [
      "easy",
      "payments",
      "aggregation",
      "or"
    ],
    "companies": [
      "Stripe",
      "PayPal"
    ]
  },
  {
    "id": 11023,
    "order": 23,
    "title": "Active users by country",
    "description": "Count active users (`is_active = true`) per country. Return `country` and `user_count`. Order by `user_count` descending, then `country` ascending.",
    "difficulty": "easy",
    "schema": {
      "users": [
        "user_id",
        "country",
        "is_active"
      ]
    },
    "dataset_files": [
      "users.csv"
    ],
    "expected_query": "SELECT country, COUNT(*) AS user_count\nFROM users\nWHERE is_active = true\nGROUP BY country\nORDER BY user_count DESC, country ASC",
    "solution_query": "SELECT country, COUNT(*) AS user_count\nFROM users\nWHERE is_active = true\nGROUP BY country\nORDER BY user_count DESC, country ASC;",
    "required_concepts": [
      "group_by",
      "where",
      "order_by"
    ],
    "enforce_concepts": true,
    "explanation": "Filtering with WHERE before GROUP BY is the correct pattern for pre-aggregation filtering. `COUNT(*)` counts all rows that pass the filter per group. The dual ORDER BY (count DESC, country ASC) produces a deterministic result when two countries share the same user count.",
    "hints": [
      "Filter the data to the relevant subset first, then collapse it to the business grain and calculate the summary values.",
      "When multiple groups have the same count, a secondary order column makes the output deterministic — pick a column that uniquely distinguishes the remaining groups."
    ],
    "concepts": [
      "PRE-AGGREGATION FILTERING",
      "MULTI-COLUMN ORDERING",
      "GROUP SIZE COMPARISON"
    ],
    "companies": [
      "Meta",
      "LinkedIn",
      "Salesforce"
    ]
  },
  {
    "id": 11024,
    "order": 24,
    "title": "Order amount with discount fallback",
    "description": "Return `order_id`, `user_id`, `gross_amount`, `discount_amount`, and a computed column `final_amount` = `gross_amount - COALESCE(discount_amount, 0)`. Order by `order_id` ascending.",
    "difficulty": "easy",
    "schema": {
      "orders": [
        "order_id",
        "user_id",
        "gross_amount",
        "discount_amount"
      ]
    },
    "dataset_files": [
      "orders.csv"
    ],
    "expected_query": "SELECT order_id, user_id, gross_amount, discount_amount,\n       gross_amount - COALESCE(discount_amount, 0) AS final_amount\nFROM orders\nORDER BY order_id",
    "solution_query": "SELECT order_id, user_id, gross_amount, discount_amount,\n       gross_amount - COALESCE(discount_amount, 0) AS final_amount\nFROM orders\nORDER BY order_id;",
    "required_concepts": [
      "coalesce"
    ],
    "enforce_concepts": true,
    "explanation": "COALESCE(discount_amount, 0) replaces NULL values with 0 so the subtraction never produces NULL. Without COALESCE, any row where discount_amount IS NULL would yield NULL for final_amount — a silent bug that propagates through downstream aggregations. This pattern (null-safe arithmetic) is ubiquitous in financial reporting queries.",
    "hints": [
      "Two of the columns you need feed an arithmetic expression — what happens to that expression if one of them has no value?",
      "COALESCE returns its first non-NULL argument, so COALESCE(discount_amount, 0) returns 0 when discount is missing"
    ],
    "concepts": [
      "NULL-SAFE ARITHMETIC",
      "COALESCE DEFAULT SUBSTITUTION",
      "DERIVED FIELD CREATION"
    ],
    "companies": [
      "Stripe",
      "Amazon",
      "PayPal"
    ]
  },
  {
    "id": 11025,
    "order": 25,
    "title": "Orders per calendar month",
    "description": "Count orders grouped by calendar month. Return `order_month` (formatted as `'YYYY-MM'` using `STRFTIME('%Y-%m', order_date)`) and `order_count`. Order by `order_month` ascending.",
    "difficulty": "easy",
    "schema": {
      "orders": [
        "order_id",
        "order_date"
      ]
    },
    "dataset_files": [
      "orders.csv"
    ],
    "expected_query": "SELECT STRFTIME('%Y-%m', order_date) AS order_month, COUNT(*) AS order_count\nFROM orders\nGROUP BY order_month\nORDER BY order_month",
    "solution_query": "SELECT STRFTIME('%Y-%m', order_date) AS order_month,\n       COUNT(*) AS order_count\nFROM orders\nGROUP BY order_month\nORDER BY order_month;",
    "required_concepts": [
      "group_by",
      "date_trunc",
      "order_by"
    ],
    "enforce_concepts": true,
    "explanation": "STRFTIME('%Y-%m', order_date) truncates each date to its year-month string. Grouping on this string produces one row per calendar month. Sorting the 'YYYY-MM' string lexicographically gives correct chronological order because ISO format is zero-padded. This is the foundational pattern for time-series aggregations in analytics.",
    "hints": [
      "Group orders by the calendar month they were placed, then count rows within each group.",
      "A date truncated to year-month format works as both a grouping key and a sort key — string comparison order aligns with chronological order for this format."
    ],
    "concepts": [
      "DATE BUCKET GROUPING",
      "TIME-SERIES AGGREGATION",
      "STRFTIME FORMATTING"
    ],
    "companies": [
      "Meta",
      "Stripe",
      "Airbnb",
      "LinkedIn"
    ]
  },
  {
    "id": 11026,
    "order": 26,
    "title": "Products launched in 2024",
    "description": "Return `product_id`, `product_name`, `brand`, and `launch_date` for all products launched in the calendar year 2024. Order by `launch_date` ascending.",
    "difficulty": "easy",
    "schema": {
      "products": [
        "product_id",
        "product_name",
        "brand",
        "launch_date"
      ]
    },
    "dataset_files": [
      "products.csv"
    ],
    "expected_query": "SELECT product_id, product_name, brand, launch_date\nFROM products\nWHERE launch_date >= '2024-01-01' AND launch_date < '2025-01-01'\nORDER BY launch_date",
    "solution_query": "SELECT product_id, product_name, brand, launch_date\nFROM products\nWHERE launch_date >= '2024-01-01' AND launch_date < '2025-01-01'\nORDER BY launch_date;",
    "required_concepts": [
      "where",
      "order_by"
    ],
    "enforce_concepts": true,
    "explanation": "Using `>= '2024-01-01' AND < '2025-01-01'` is the portable, precise way to filter a full calendar year. It works correctly regardless of whether dates include time components, and avoids functions like YEAR() that can prevent index use in production databases. Prefer this half-open interval pattern over BETWEEN or YEAR() extraction.",
    "hints": [
      "Filter to the qualifying rows before you project columns or worry about the final ordering.",
      "Bounding a date column to a range can be done with `BETWEEN` or a pair of half-open comparisons; pick whichever makes the inclusivity explicit."
    ],
    "concepts": [
      "HALF-OPEN DATE INTERVAL",
      "RANGE FILTERING",
      "DATE COMPARISON"
    ],
    "companies": [
      "Amazon",
      "Shopify",
      "eBay"
    ]
  },
  {
    "id": 11027,
    "order": 27,
    "title": "Employees in US or UK",
    "description": "Return `employee_id`, `employee_name`, `country`, and `salary` for all employees whose `country` is either `'US'` or `'UK'`. Order by `salary` descending.",
    "difficulty": "easy",
    "schema": {
      "employees": [
        "employee_id",
        "employee_name",
        "country",
        "salary"
      ]
    },
    "dataset_files": [
      "employees.csv"
    ],
    "expected_query": "SELECT employee_id, employee_name, country, salary\nFROM employees\nWHERE country IN ('US', 'UK')\nORDER BY salary DESC",
    "solution_query": "SELECT employee_id, employee_name, country, salary\nFROM employees\nWHERE country IN ('US', 'UK')\nORDER BY salary DESC;",
    "required_concepts": [
      "where",
      "order_by"
    ],
    "enforce_concepts": true,
    "explanation": "The IN operator is a cleaner alternative to multiple OR conditions when filtering against a known set of values. `country IN ('US', 'UK')` is equivalent to `country = 'US' OR country = 'UK'` but is more readable and easier to extend. Ordering by salary DESC surfaces the highest-paid employees first.",
    "hints": [
      "Filter to the qualifying rows before you project columns or worry about the final ordering.",
      "IN is equivalent to country = 'US' OR country = 'UK' but more readable; ORDER BY makes results deterministic"
    ],
    "concepts": [
      "IN CLAUSE FILTERING",
      "SET MEMBERSHIP TEST"
    ],
    "companies": [
      "Google",
      "Microsoft",
      "LinkedIn"
    ]
  },
  {
    "id": 11028,
    "order": 28,
    "title": "Highest and lowest order value",
    "description": "Return a single row with two columns: `max_order` (the highest `net_amount` across all orders) and `min_order` (the lowest `net_amount`).",
    "difficulty": "easy",
    "schema": {
      "orders": [
        "order_id",
        "net_amount"
      ]
    },
    "dataset_files": [
      "orders.csv"
    ],
    "expected_query": "SELECT MAX(net_amount) AS max_order, MIN(net_amount) AS min_order\nFROM orders",
    "solution_query": "SELECT MAX(net_amount) AS max_order,\n       MIN(net_amount) AS min_order\nFROM orders;",
    "required_concepts": [],
    "enforce_concepts": false,
    "explanation": "Multiple aggregate functions can be computed in a single SELECT without a GROUP BY — the entire table acts as one group. MAX and MIN scan all rows and return the extreme values. Naming output columns with AS makes results self-documenting. This pattern is foundational for summary statistics queries.",
    "hints": [
      "Reduce the entire table to a single row that captures both extremes of the order-value column simultaneously.",
      "No GROUP BY is needed when aggregating over the entire table"
    ],
    "concepts": [
      "MULTI-METRIC SUMMARIZATION",
      "TABLE-WIDE SUMMARY",
      "SCALAR AGGREGATION"
    ],
    "companies": [
      "Amazon",
      "eBay",
      "Shopify"
    ]
  },
  {
    "id": 11029,
    "order": 29,
    "title": "Users by plan tier",
    "description": "Count users grouped by `plan_tier`. Return `plan_tier` and `user_count`. Order by `user_count` descending.",
    "difficulty": "easy",
    "schema": {
      "users": [
        "user_id",
        "plan_tier"
      ]
    },
    "dataset_files": [
      "users.csv"
    ],
    "expected_query": "SELECT plan_tier, COUNT(*) AS user_count\nFROM users\nGROUP BY plan_tier\nORDER BY user_count DESC",
    "solution_query": "SELECT plan_tier, COUNT(*) AS user_count\nFROM users\nGROUP BY plan_tier\nORDER BY user_count DESC;",
    "required_concepts": [
      "group_by",
      "order_by"
    ],
    "enforce_concepts": true,
    "explanation": "Grouping by a categorical column and counting is the canonical segmentation query. The result shows the distribution of users across plan tiers, which directly maps to subscription revenue breakdowns. Ordering by count descending surfaces the dominant tier first.",
    "hints": [
      "Group users by their subscription tier and count how many belong to each one.",
      "Sorting by the aggregate count descending puts the most populous group at the top, which is the natural reading order when ranking groups by size."
    ],
    "concepts": [
      "CATEGORICAL SEGMENTATION",
      "GROUP SIZE COMPARISON",
      "DISTRIBUTION ANALYSIS"
    ],
    "companies": [
      "Stripe",
      "Shopify",
      "Salesforce"
    ]
  },
  {
    "id": 11030,
    "order": 30,
    "title": "Orders with a non-zero discount",
    "description": "Return `order_id`, `gross_amount`, `discount_amount`, and `net_amount` for orders where `discount_amount` IS NOT NULL and `discount_amount > 0`. Order by `discount_amount` descending.",
    "difficulty": "easy",
    "schema": {
      "orders": [
        "order_id",
        "gross_amount",
        "discount_amount",
        "net_amount"
      ]
    },
    "dataset_files": [
      "orders.csv"
    ],
    "expected_query": "SELECT order_id, gross_amount, discount_amount, net_amount\nFROM orders\nWHERE discount_amount IS NOT NULL AND discount_amount > 0\nORDER BY discount_amount DESC",
    "solution_query": "SELECT order_id, gross_amount, discount_amount, net_amount\nFROM orders\nWHERE discount_amount IS NOT NULL AND discount_amount > 0\nORDER BY discount_amount DESC;",
    "required_concepts": [
      "where",
      "null_check",
      "order_by"
    ],
    "enforce_concepts": true,
    "explanation": "Combining IS NOT NULL with a comparison operator (`> 0`) requires both conditions with AND. The IS NOT NULL check is needed because `NULL > 0` evaluates to NULL (not true), which would silently exclude those rows anyway — but being explicit is a best practice that makes intent clear. This combined pattern is common in any query that filters on an optional numeric field.",
    "hints": [
      "Filter to the qualifying rows before you project columns or worry about the final ordering.",
      "When a column can be NULL, a numeric comparison silently fails for NULL rows — think about what separate condition handles NULLs explicitly."
    ],
    "concepts": [
      "NULL CHECK COMBINED WITH COMPARISON",
      "COMBINED AND CONDITIONS",
      "OPTIONAL FIELD FILTERING"
    ],
    "companies": [
      "Amazon",
      "Shopify",
      "eBay"
    ]
  },
  {
    "id": 11031,
    "order": 31,
    "title": "Monthly user signups with a CTE",
    "description": "Using a CTE named `monthly_signups`, compute the number of users who signed up each month (group by `STRFTIME('%Y-%m', signup_date)`). Return `signup_month` and `user_count` from the CTE, ordered by `signup_month` ascending.\n\nThis is an introduction to CTEs (`WITH` clauses), which create named temporary result sets that can be referenced like a table.",
    "difficulty": "easy",
    "schema": {
      "users": [
        "user_id",
        "signup_date"
      ]
    },
    "dataset_files": [
      "users.csv"
    ],
    "expected_query": "WITH monthly_signups AS (\n    SELECT STRFTIME('%Y-%m', signup_date) AS signup_month,\n           COUNT(*) AS user_count\n    FROM users\n    GROUP BY signup_month\n)\nSELECT signup_month, user_count\nFROM monthly_signups\nORDER BY signup_month",
    "solution_query": "WITH monthly_signups AS (\n    SELECT STRFTIME('%Y-%m', signup_date) AS signup_month,\n           COUNT(*) AS user_count\n    FROM users\n    GROUP BY signup_month\n)\nSELECT signup_month, user_count\nFROM monthly_signups\nORDER BY signup_month;",
    "required_concepts": [
      "cte",
      "group_by",
      "order_by"
    ],
    "enforce_concepts": true,
    "explanation": "A CTE (Common Table Expression) defined with `WITH name AS (...)` creates a named temporary result set that lives only for the duration of the query. The CTE is defined first, then referenced in the main SELECT just like a table. CTEs make complex queries readable by naming intermediate steps — they are fundamental to writing maintainable SQL at any level. Here the CTE computes monthly counts, and the outer query retrieves and sorts them.",
    "hints": [
      "A CTE creates a named intermediate result set that acts like a temporary table — define the aggregation logic once inside the CTE, then select and sort from it in the outer query without repeating the expression.",
      "Reference the CTE name in the outer SELECT like a table; grouping by the extracted date part and ordering by it ensures chronological output."
    ],
    "concepts": [
      "STAGED QUERY DECOMPOSITION",
      "TIME-BUCKETED SIGNUP TREND",
      "MONTHLY ENTITY COUNTING"
    ],
    "companies": [
      "Meta",
      "LinkedIn",
      "Google",
      "Stripe"
    ]
  },
  {
    "id": 11032,
    "order": 32,
    "title": "Tickets by priority and status",
    "description": "Count support tickets grouped by both `priority` and `status`. Return `priority`, `status`, and `ticket_count`. Order by `priority` ascending, then `status` ascending.",
    "difficulty": "easy",
    "schema": {
      "support_tickets": [
        "ticket_id",
        "priority",
        "status"
      ]
    },
    "dataset_files": [
      "support_tickets.csv"
    ],
    "expected_query": "SELECT priority, status, COUNT(*) AS ticket_count\nFROM support_tickets\nGROUP BY priority, status\nORDER BY priority, status",
    "solution_query": "SELECT priority, status, COUNT(*) AS ticket_count\nFROM support_tickets\nGROUP BY priority, status\nORDER BY priority, status;",
    "required_concepts": [
      "group_by",
      "order_by"
    ],
    "enforce_concepts": true,
    "explanation": "Multi-column GROUP BY produces one row per unique combination of the grouped columns — here, one row per (priority, status) pair. This is the standard approach for cross-tabulating two categorical dimensions before pivoting. The result is effectively a raw pivot table in long format, which is the input for most BI tools and dashboards.",
    "hints": [
      "Group the ticket rows by both columns named in the result set before counting.",
      "When grouping by two columns, each unique combination produces one output row — ordering by both keeps the most important dimension as the primary sort axis."
    ],
    "concepts": [
      "MULTI-COLUMN GROUPING",
      "CROSS-DIMENSIONAL AGGREGATION",
      "LONG-FORMAT CROSS-TAB"
    ],
    "companies": [
      "Zendesk",
      "Salesforce",
      "Meta"
    ]
  },
  {
    "id": 11033,
    "order": 33,
    "title": "Normalize acquisition channel names",
    "difficulty": "easy",
    "description": "If the column had inconsistent casing or surrounding whitespace, the result should still group correctly — write a query that handles that case defensively. Return `channel` and `user_count`, ordered by `user_count` descending.",
    "dataset_files": [
      "users.csv"
    ],
    "schema": {
      "users": [
        "acquisition_channel"
      ]
    },
    "companies": [
      "Airbnb",
      "LinkedIn",
      "Shopify"
    ],
    "concepts": [
      "STRING TRIMMING",
      "CASE-INSENSITIVE NORMALIZATION",
      "CHANNEL STANDARDIZATION"
    ],
    "required_concepts": [
      "string_functions",
      "group_by",
      "order_by"
    ],
    "enforce_concepts": true,
    "expected_query": "SELECT UPPER(TRIM(acquisition_channel)) AS channel, COUNT(*) AS user_count\nFROM users\nGROUP BY UPPER(TRIM(acquisition_channel))\nORDER BY user_count DESC",
    "solution_query": "SELECT UPPER(TRIM(acquisition_channel)) AS channel,\n       COUNT(*) AS user_count\nFROM users\nGROUP BY UPPER(TRIM(acquisition_channel))\nORDER BY user_count DESC;",
    "hints": [
      "Two functions applied in sequence can collapse variant spellings of the same label into one group.",
      "UPPER() and TRIM() must appear consistently in both SELECT and GROUP BY — or alias the expression in a CTE and group by the alias."
    ],
    "explanation": "TRIM() removes leading and trailing whitespace; UPPER() folds case. Together they normalize free-text categorical values before grouping. The pair must appear in both the SELECT and GROUP BY because SQL evaluates GROUP BY before SELECT aliases are visible. This is the standard ETL pattern for deduplicate-before-aggregate when source data comes from user input or multiple systems with inconsistent formatting."
  },
  {
    "id": 11034,
    "order": 34,
    "title": "Extract email domains",
    "difficulty": "easy",
    "description": "Write a query that returns `user_id`, `email`, and `domain` — the portion of each email address after the `@` character. Order by `user_id` ascending.",
    "dataset_files": [
      "users.csv"
    ],
    "schema": {
      "users": [
        "user_id",
        "email"
      ]
    },
    "companies": [
      "Google",
      "Stripe",
      "LinkedIn"
    ],
    "concepts": [
      "SUBSTRING EXTRACTION",
      "SPLIT_PART",
      "EMAIL PARSING"
    ],
    "required_concepts": [
      "string_functions",
      "order_by"
    ],
    "enforce_concepts": true,
    "expected_query": "SELECT user_id, email, SPLIT_PART(email, '@', 2) AS domain\nFROM users\nORDER BY user_id",
    "solution_query": "SELECT user_id, email,\n       SPLIT_PART(email, '@', 2) AS domain\nFROM users\nORDER BY user_id;",
    "hints": [
      "A function that splits a string on a delimiter and returns a numbered part can isolate one side of the '@' in a single call.",
      "The domain is the second segment when you split on '@'."
    ],
    "explanation": "SPLIT_PART(string, delimiter, n) splits a string on the given delimiter and returns the nth part (1-indexed). For emails, '@' separates the local part (1) from the domain (2). This is cleaner than SUBSTRING + STRPOS when the result is always at a fixed position relative to a known delimiter. SPLIT_PART is available in PostgreSQL, DuckDB, Redshift, and Snowflake; use SUBSTR + INSTR in MySQL."
  },
  {
    "id": 11035,
    "order": 35,
    "title": "Build employee display labels",
    "difficulty": "easy",
    "description": "Write a query that returns each employee's `employee_id`, `employee_name`, and a `label` formatted as `'Name (Department)'`. If an employee has no matching department, substitute `'Unknown'` for the department name. Order by `employee_id` ascending.",
    "dataset_files": [
      "employees.csv",
      "departments.csv"
    ],
    "schema": {
      "employees": [
        "employee_id",
        "employee_name",
        "department_id"
      ],
      "departments": [
        "department_id",
        "department_name"
      ]
    },
    "companies": [
      "Meta",
      "Google",
      "Salesforce"
    ],
    "concepts": [
      "STRING CONCATENATION",
      "NULL HANDLING IN CONCAT",
      "EMPLOYEE LABELING"
    ],
    "required_concepts": [
      "string_functions",
      "join",
      "order_by"
    ],
    "enforce_concepts": true,
    "expected_query": "SELECT e.employee_id, e.employee_name,\n       e.employee_name || ' (' || COALESCE(d.department_name, 'Unknown') || ')' AS label\nFROM employees e\nLEFT JOIN departments d ON e.department_id = d.department_id\nORDER BY e.employee_id",
    "solution_query": "SELECT e.employee_id, e.employee_name,\n       e.employee_name || ' (' || COALESCE(d.department_name, 'Unknown') || ')' AS label\nFROM employees e\nLEFT JOIN departments d ON e.department_id = d.department_id\nORDER BY e.employee_id;",
    "hints": [
      "When your query must include every row from the primary table — even when no corresponding row exists in the other table — what SQL operation achieves this, and what value fills the columns that have no match?",
      "COALESCE returns the first non-NULL argument — use it to substitute a fallback string for the nullable column before it reaches the `||` concatenation."
    ],
    "explanation": "The `||` operator is DuckDB's idiomatic string concatenation — it chains operands left to right and propagates NULL if any operand is NULL. That NULL-propagation is exactly why COALESCE(d.department_name, 'Unknown') is essential: the LEFT JOIN produces NULL for department_name when no department row matches, and wrapping it in COALESCE converts that NULL to 'Unknown' before the concatenation runs. This pattern — LEFT JOIN + COALESCE in a string expression — is common in display-layer queries and reporting pipelines."
  },
  {
    "id": 11036,
    "order": 36,
    "title": "Days since user signup",
    "difficulty": "easy",
    "description": "Write a query that returns `user_id`, `name`, `signup_date`, and `days_since_signup` — the number of full days between each user's `signup_date` and today. Only include active users (`is_active = true`). Order by `days_since_signup` descending.",
    "dataset_files": [
      "users.csv"
    ],
    "schema": {
      "users": [
        "user_id",
        "name",
        "signup_date",
        "is_active"
      ]
    },
    "companies": [
      "Stripe",
      "Airbnb",
      "LinkedIn"
    ],
    "concepts": [
      "CURRENT DATE",
      "DATE ARITHMETIC",
      "TENURE CALCULATION"
    ],
    "required_concepts": [
      "date_functions",
      "order_by"
    ],
    "enforce_concepts": true,
    "expected_query": "SELECT user_id, name, signup_date,\n       (CURRENT_DATE - signup_date)::INT AS days_since_signup\nFROM users\nWHERE is_active = true\nORDER BY days_since_signup DESC",
    "solution_query": "SELECT user_id, name, signup_date,\n       (CURRENT_DATE - signup_date)::INT AS days_since_signup\nFROM users\nWHERE is_active = true\nORDER BY days_since_signup DESC;",
    "hints": [
      "Think about what SQL reference point represents 'today', and what arithmetic operation captures the gap between that point and a historical date column.",
      "Filter to active users before computing the metric."
    ],
    "explanation": "CURRENT_DATE returns today's date as a DATE value. Subtracting two DATE values in DuckDB yields an interval; casting with ::INT gives a plain day count. This pattern — age = CURRENT_DATE − date_column — appears everywhere: churn models, SLA monitors, cohort analyses, and subscription expiry checks. Results will differ each day you run the query, which is expected behavior for a live tenure column."
  },
  {
    "id": 11037,
    "order": 37,
    "title": "Monthly signup trend",
    "difficulty": "easy",
    "description": "Write a query that counts how many users signed up in each calendar month. Return `signup_month` (the `signup_date` truncated to the first day of the month using `DATE_TRUNC`) and `signup_count`. Order by `signup_month` ascending.",
    "dataset_files": [
      "users.csv"
    ],
    "schema": {
      "users": [
        "signup_date"
      ]
    },
    "companies": [
      "Stripe",
      "Airbnb",
      "Meta"
    ],
    "concepts": [
      "DATE TRUNCATION",
      "MONTHLY TREND",
      "GROUPED DATE AGGREGATION"
    ],
    "required_concepts": [
      "date_functions",
      "group_by",
      "order_by"
    ],
    "enforce_concepts": true,
    "expected_query": "SELECT DATE_TRUNC('month', signup_date) AS signup_month,\n       COUNT(*) AS signup_count\nFROM users\nGROUP BY DATE_TRUNC('month', signup_date)\nORDER BY signup_month",
    "solution_query": "SELECT DATE_TRUNC('month', signup_date) AS signup_month,\n       COUNT(*) AS signup_count\nFROM users\nGROUP BY DATE_TRUNC('month', signup_date)\nORDER BY signup_month;",
    "hints": [
      "How would you assign every date within the same calendar month to a single bucket so that multiple rows in the same month can be collapsed into one aggregate row?",
      "Group by the same truncated expression used in SELECT."
    ],
    "explanation": "DATE_TRUNC('month', date) truncates to the first of the month (e.g., 2023-03-15 → 2023-03-01). Unlike STRFTIME('%Y-%m', date), DATE_TRUNC preserves a proper DATE type that sorts correctly and can be fed into downstream date arithmetic. This is the canonical pattern for time-series bucketing in analytics SQL — use DATE_TRUNC when you need a proper date; use STRFTIME when you need a formatted string for display."
  }
]
