# Future Enhancements (TODO)

- [ ] Add real user accounts (optional) and/or a “reset progress” UI for the current session.
- [ ] Move question seeds to JSON/YAML and add a small validation CLI (schema validation + uniqueness checks).
- [ ] Add richer feedback on incorrect answers (e.g., highlight mismatched columns/rows) without weakening SQL guardrails.
- [ ] Add search within the sidebar (kept out of scope for the MVP extension).
