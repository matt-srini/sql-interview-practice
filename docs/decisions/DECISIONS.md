# Decision Log

The **why** layer for this platform. The `docs/` files record *what is true now* (the "is"); this file records *why it's true and what we rejected* (the reasoning). They are complementary — never duplicate a doc's content here; link to it instead.

This log exists to kill three recurring failures: decisions evaporating when a session closes, nuances never reaching the docs, and **A→B→A oscillation** — re-deciding something weeks later because the original rationale (and the alternatives already rejected) was never written down.

---

## How to use this file

**Read it** — before *reversing or re-deciding* anything in a load-bearing area: architecture, content gating / unlock rules, the mock contract, pricing, the concept taxonomy, or any per-track curriculum framing. Grep by `**Area:**` tag or keyword first. If a prior entry already settled the question, honor it or supersede it deliberately — don't silently re-litigate.

**Write it** — after any *meaningful or direction-changing* decision, append an entry. The entry rides in the **same commit** as the change it describes (tie it to the existing "commit after meaningful changes" habit). Not every commit needs one — only those that carry a real decision, a rejected alternative, or a reversal.

**Never edit a past entry.** Append-only. To change a decision, add a *new* entry at the top whose `Status:` is `accepted` and that names the entry it **Supersedes**; flip the old entry's `Status:` to `superseded` (the one allowed edit — a single status word) and nothing else. The full chain stays visible: that's what prevents oscillation.

**Never expire.** Old decisions are the dangerous ones — they cause the most re-litigation. We archive, we do not delete. When this file passes ~1500 lines, move the oldest entries to `docs/decisions/archive-<year>.md` (same pattern as `docs/archive/`).

**Index, don't expand.** This is the index of *why*. The durable *rule* still lives in its source-of-truth doc (`docs/...`, the authoring agent, a track doc). Every entry links to where the rule landed via `**Affects:**`.

### Entry template

```markdown
## YYYY-MM-DD — <short imperative title>
**Area:** <architecture|content|gating|mock|pricing|taxonomy|frontend|ops|process|...> · **Status:** accepted
**Decision:** <the call, in one or two sentences>
**Rejected:** <the alternative(s) considered and why they lost — the most important line>
**Affects:** <docs/path.md, file, or "none — reasoning only">
**Supersedes:** <YYYY-MM-DD entry title, only if this reverses a prior decision>
```

Keep entries to 4–6 lines. Friction kills logs; if it's longer than the change deserves, it won't get written. Newest entry on top.

---

## Entries

## 2026-06-25 — Add per-session loop_escalated storage (past-session history accuracy)
**Area:** mock · backend · schema · **Status:** accepted (complements the 2026-06-22 SQL-medium escalation)
**Decision:** Added `mock_sessions.loop_escalated` (BOOLEAN, nullable), set at interview_loop session creation from the drawn chain's actual difficulties; history + active-session badges now read it (cell-flag fallback if NULL), while the pre-start badge stays cell-level. Backfilled every existing loop session from its stored `mock_session_questions` (accurate — the stored ids are the actual served questions). This closes the residual edge the 2026-06-22 escalate-the-3 decision left: a PAST SQL-medium session that drew 12077/12079/12093 (flat at the time) was shown "medium → hard" by the recomputed-now-uniform cell flag; it now correctly shows "medium". Migration `20260624_000001` applied to prod + `_SCHEMA_SQL` synced.
**Note:** This is the per-session `loop_escalated` column the 2026-06-22 entry listed as "rejected" — adopted here ONLY for historical accuracy, NOT the rejected 3-state "may escalate" pre-start UI (still unnecessary: escalation is uniform going forward, so the existing two-state cell badge is honest).
**Affects:** backend/db.py (`_SCHEMA_SQL` + create/read), backend/routers/mock.py, backend/alembic/versions/20260624_000001_*, backend/scripts/backfill_loop_escalated.py, backend/tests/test_11_mock.py, docs/features/mock.md, docs/deployment.md

## 2026-06-22 — SQL-medium Interview Loop: escalate the 3 flat chains (not message around them) + uniformity validator
**Area:** content · mock · validation · **Status:** accepted
**Decision:** SQL-medium had MIXED escalation — only 5/8 chains escalated medium→hard (the 5 newest); 3 older chains (12077/12079/12093) stayed all-medium — yet the cell-level `escalates` flag (ANY-of-cell) rendered "medium → hard" on all three surfaces (pre-start, active, history), over-promising for the 3 flat chains. We ESCALATED the 3 flat chains: appended one genuine HARD follow-up to each (13158 cross-session attribution mismatch / 13159 per-tier peak-drawdown / 13160 dominant-tier-per-country), making SQL-medium uniformly 8/8 escalating like Python-medium (18/18) and Data-Modeling-medium (4/4). The existing simple flag is now honest by construction. Added `_validate_loop_escalation_uniformity` (validate_content.py, ERROR-level, + a pytest guard): a (track, difficulty) Loop cell must be all-escalating or none.
**Rejected:** (a) per-session `loop_escalated` column + a 3-state "may escalate" UI (rejected — a prod schema migration + backfill + multi-surface UI to faithfully describe a state that was never an intended feature); (b) rewriting the 3 medium pivots into hard ones (rejected — destroys 3 good medium follow-ups; appended instead → length-3 chains at 45 min via the unchanged 15×length rule, NO loop-time retiming, to preserve the existing time reasoning). New follow-ups dedup-verified against the dense 193-question SQL mock pool and 3-model blind-solved (Opus+Sonnet+GPT-5.5); 13158 iterated once (a date-granularity stem ambiguity the blind-solve caught).
**Revisit:** relax the uniformity validator deliberately if a track ever genuinely needs mixed escalation (then build the per-session / "may escalate" path).
**Affects:** backend/content/questions/{hard,medium}.json, backend/scripts/validate_content.py, backend/tests/test_11_mock.py, backend/routers/mock.py, CLAUDE.md, docs/features/mock.md, docs/content-authoring.md

## 2026-06-24 — Editing an executable key requires syncing its displayed solution (CI guard)
**Area:** content · process · **Status:** accepted
**Decision:** Every executable question has two solution fields — the grader reference (`expected_query`/`expected_code`) and the user-facing displayed solution (`solution_query`/`solution_code`) — and they must stay in lockstep. The 2026-06-23 pandas key fixes (32099/33026/33029/33030) updated `expected_code` but not `solution_code`, so `tests/test_code_references.py::test_pandas_reference_executes_and_solution_agrees` went red on `main` (`validate_content.py` passed — it doesn't check this agreement). Fixed by syncing `solution_code := expected_code` for the four; documented the rule in `docs/content-authoring.md` § Output contract item 10 + § Verification, the authoring-agent verification block, and the CLAUDE.md post-fix checklist (run `test_code_references.py` after any key edit).
**Rejected:** leaving the displayed solution divergent (it is shown to users after solving — a stale solution is a content bug, not just a test failure); relying on `validate_content.py` alone for key edits (it validates structure/balance/reverse-preview but not reference⇄solution agreement — that is exactly what `test_code_references.py` exists for).
**Affects:** backend/content/pandas_questions/{medium,hard}.json, docs/content-authoring.md, .github/agents/question-authoring.agent.md, CLAUDE.md

## 2026-06-23 — Pandas mock blind-QA: 16 content fixes (4 wrong keys, 12 stem/method)
**Area:** content · **Status:** accepted
**Decision:** Two-lens QA (blind-solve 122/132 + adversarial stem↔key audit + empirical verification) on the 132 mock-only pandas questions landed 16 fixes. **4 key/logic corrections** (the serious ones): 33026 & 33030 conversion window `days_to_order <= 30` → `.between(0, 30)` (it counted pre-signup orders); 32099 inner-merge → left-merge (dropped zero-revenue active products the parent 32098's cohort includes); 33029 headcount `count(salary)` → `count(employee_id)` (excluded null-salary employees). **12 stem clarifications** pinning precision (32055, 32072, 33049), metric definition (32061, 32034, 33051, 33074, 33079, 33082), and method (33040, 33043, 33045). Each fix verify-before-write blind-solved against the final key; validate_content clean.
**Rejected:** "fix the stem to match the key" for 33026/33030/32099/33029 — the keys were genuinely wrong vs the stem's intent, so the keys were corrected (per the "fix whichever is wrong" call). Also rejected 2 audit false-positives after empirical check (33059 rounding didn't change the row set; 32082 had no non-numeric columns) and dropped row/column-order findings entirely (the pandas grader sorts both).
**Affects:** backend/content/pandas_questions/{medium,hard}.json, docs/content-authoring.md (§ Output contract — pandas clauses 6–9 + delivery contract), docs/tracks/pandas.md

## 2026-06-23 — Pandas column names must ship on every surface (fix mock + practice column blindness)
**Area:** frontend · content · **Status:** accepted
**Decision:** The pandas DataFrame **column list is load-bearing** and the prop/key is `schema` everywhere. Fixed three surfaces that were hiding it: mock omitted `schema` from `_public_question_payload` *and* `MockSession` passed `schema={{}}` (132 mock questions); practice `QuestionPage` read a phantom `question.dataframe_schema` the backend never sends (it sends `schema`) so columns were blank too (93 practice questions). Sample was already correct. All three now flow `question.schema` → `VariablesPanel`.
**Why:** Pandas has no SchemaViewer (`showSchema` is SQL-only) — VariablesPanel's column list is the *only* place a candidate sees available columns, and only 6/132 mock stems list every column in prose. Without it the candidate can't reliably write `df['<col>']`; the questions are effectively unsolvable. Surfaced by the mock blind-QA effort (same delivery-bug family as the earlier `def solve()`/dataframes-missing mock pandas bug). Caught at payload-inspection *before* blind-solving — had the solvers run on the column-less payload, their failures would have been mis-read as content defects.
**Rejected:** keep columns hidden as "added difficulty" (would require listing every column in 123 stems — high cost, and inconsistent with practice/sample); inventing a separate `dataframe_schema` key (a second name for the same fact — the exact drift the single-SoT rule forbids).
**Affects:** backend/routers/mock.py, frontend/src/pages/MockSession.js, frontend/src/pages/QuestionPage.js, docs/frontend.md

## 2026-06-22 — Interview Loop exhausted-cell: redirect-first, replay demoted (resolves P2)
**Area:** mock · UX · **Status:** accepted (refines the 2026-06-19 replay end-state)
**Decision:** When a user finishes every chain in a (track, difficulty) Loop cell, the exhausted state now LEADS with a "Fresh chains to try" rail of the user's still-unexhausted cells (`/api/mock/access` → top-level `fresh_loop_cells`, requested track first, capped 4) and DEMOTES replay to a quiet "Or replay these chains →" link. Replay is kept (honest, no dead-end) but no longer headlines; if the user has exhausted *every* cell, `fresh_loop_cells` is empty and replay becomes the only CTA (the sole remaining non-dead-end).
**Why:** The Loop's value is the *unscripted* interviewer pivot — replaying a chain whose pivots you already know is low value, so it shouldn't headline. With 127 chains across the catalog, a user who finishes one cell has fresh reasoning waiting elsewhere; steering there reinforces the deep-catalog + capstone positioning. Resolves TODO P2 ("keep vs scope-down replay") now that all thin cells are expanded (Stats-hard, SQL, Pandas-medium 2→8).
**Rejected:** keep replay as the headline (signals thin content / low learning value); N-gate or cooldown on replay (manufactured friction — the retention anti-pattern); hard dead-end (punitive on a paid feature). 
**Affects:** backend/routers/mock.py (`_fresh_loop_cells`, `/access`, `_loop_exhausted_copy`), frontend MockHub.js + App.css, docs/features/mock.md, TODO.md.

## 2026-06-22 — +6 pandas-medium Interview Loop chains (2→8), triple blind-solve gate
**Area:** content · mock · process · **Status:** accepted
**Decision:** Authored 6 new mock-only pandas-medium chains (ids 32092–32109; 6 parents + 12 follow-ups), taking pandas-medium Interview Loop chains 2→8 — the thinnest cell, the one deferred in the P2 replay note. Verified with a 3-model blind-solve (Opus + Sonnet + GPT-5.5 each writing the solution from the candidate view only, graded by the real pandas evaluator): all 18 pass Opus+Sonnet; GPT-5.5 passes 17/18 with 32099's reference confirmed by an identical-output GPT generation (the lone miss was GPT non-determinism, not an ambiguity).
**Rejected / caught before landing:** Two authoring passes were corrected — (1) a parent duplicating existing 32034 (payment failure-rate-by-method) plus a near-clone traffic-source funnel pair; (2) five output-population/format ambiguities the blind-solve surfaced — never-sold inclusion, all-launched-vs-sold cohort, conversion-rate %-vs-proportion, NULL-launch-date handling, and Period-vs-string launch_quarter (the same class as the SQL month-format trap). All fixed by stating the included population + output format explicitly in each stem. No near-clone padding; every parent verified distinct from the existing pandas pool.
**Affects:** backend/content/pandas_questions/medium.json, CLAUDE.md, docs/features/mock.md, docs/content-authoring.md, docs/tracks/pandas.md, TODO.md (P2)

## 2026-06-21 — Brand mark → inline SVG component (kill the recurring "logo missing" prod-blocker)
**Area:** frontend · ops · **Status:** accepted
**Decision:** Render the datathink monogram as **inline SVG** (`frontend/src/components/BrandMark.js`, used in Topbar + both sample topbars) instead of `<img src="/branding/mark-no-bg.svg">`. The two-rect mark is tiny; inlining makes the logo **zero-network**, so it cannot fail to load on any server blip / cold start / moved file / SPA-fallback mis-serve. Light fills are baked in as `fill` attributes (renders even if CSS fails); the dark variant is the dormant `[data-theme="dark"] .brand-mark-primary/secondary` CSS, so the two-file + `isDark`-JS-branch is gone. Regression guard: `BrandMark.test.js` (asserts inline-svg, not `<img>`).
**Rejected:** (1) **Keep `<img>` + harden serving / add tests** — leaves the logo a runtime fetch, i.e. one transient away from breaking again; doesn't address the *class* of bug. (2) **Bundled `import mark from '...svg'`** (Vite inlines <4 KB as a data-URI) — bulletproofs prod but the *dev* server still serves it as a URL, so dev blips still break it; inline JSX is immune in both. (3) **`?raw` + `dangerouslySetInnerHTML`** — zero-network too but less idiomatic + a11y-fiddly. Diagnosis note: the missing-file and broken-prod-serving theories were **checked and ruled out** — the file serves 200, the build emits `dist/branding/…`, and the SPA serves real dist files; the screenshot's `net::ERR_CO` was a transient dev-server connection loss (the `client:736` HMR-reconnect storm), but the *structural* fragility (fetch-based, duplicated 3×, not build-validated — see the repeated logo fixes in git history) is the real recurrence driver, which this removes.
**Affects:** `frontend/src/components/BrandMark.js` (+ `.test.js`), `Topbar.js`, `pages/SampleQuestionPage.js`, `App.css` (dormant dark fills), `CLAUDE.md` § Design system, `docs/design/color-palette.md` § Logo

## 2026-06-21 — Interview Loop positioned as the earned capstone (framing, never a gate)
**Area:** mock · frontend · **Status:** accepted
**Decision:** Closed TODO P3. Frame the Interview Loop as the **Benchmark → Drill → Loop** capstone across five surfaces (MockHub hero + how-it-works, Loop mode card / setup copy, a `Suggested first` soft-nudge in the Loop setup rail, a dashboard readiness-chip **"Ready for a Loop →"** CTA, and a mock post-mortem **"Try an Interview Loop →"** primary CTA). Backed by a new Elite-only `readiness_scores[track].loop_ready` flag (`score ≥ 65` + ≥1 *direct* benchmark on the track; mixed benchmarks excluded). The dashboard uses the holistic `loop_ready`; the post-mortem uses *this session's strong benchmark (≥60%) + a ≥65 score floor* — two honest, Elite-scoped triggers at two moments.
**Rejected:** (1) **Hard-gating the Loop** behind a completed benchmark — artificial friction on a paid Elite feature (serving-a-metric-not-the-user); every nudge stays suggestive and never disables Start. (2) **A single shared trigger** for both surfaces — the post-mortem reading `loop_ready` directly would let the benchmark-count cache lag *hide* the capstone at the exact moment a practice-ready user aces their first benchmark; reading the `score` (the ≥1-benchmark half satisfied by the current session) fixes it. (3) **Putting the dashboard CTA in `study_plan`** — the readiness chip is where the readiness signal lives, so the CTA is honest-by-construction there, not mixed into the gap-focused action list. (4) **Manufactured scarcity** (cooldowns / daily caps) — rejected; the finite chain pools are honest scarcity already.
**Affects:** `backend/routers/insights.py` (`loop_ready`), `frontend/src/pages/{ProgressDashboard,MockHub,MockSession}.js`, `frontend/src/mockModeConfig.js`, `docs/features/mock.md` § Interview Loop positioning, `docs/features/dashboard.md`, `docs/frontend.md`, `TODO.md` (P3 closed)

## 2026-06-21 — Launch-readiness P1 batch: prod config guard, payment alerting, rollback runbook, cookie consent
**Area:** ops · frontend · process · **Status:** accepted
**Decision:** Closed four launch-readiness P1s. (1) **Startup config guard** — `validate_production_config()` now hard-requires `RAZORPAY_PLAN_PRO/ELITE`, `RESEND_API_KEY`, and `ADMIN_SECRET` (≥32 chars) in production, so a misconfigured deploy fails at boot rather than silently breaking subscriptions/email at the first paying customer. (2) **Payment alerting** — `capture_payment_failure()` (Sentry, no-op without DSN) at every revenue-critical failure point in razorpay/paddle/account, all under one `alert=payment_failure` tag (logs only non-sensitive context — never signatures/secrets); the `plan_persist` wraps re-raise so the provider retries. (3) **Rollback runbook** in `docs/deployment.md` (Railway code rollback + additive-vs-destructive migration decision tree + smoke checklist). (4) **Cookie consent** — a global, **opt-in** banner; PostHog does not initialize until the visitor clicks Accept; choice persisted in localStorage.
**Rejected:** Cookie consent: (a) **opt-out** (track-until-rejected) — weaker GDPR posture; opt-in is the defensible default for an EU-infra analytics stack. (b) **EU-only geo-gated banner** — extra complexity (geo lookup) for a global-audience launch; one global banner is simpler and safe. (c) granular per-category toggles — unnecessary, PostHog is the only non-essential tracker so a single Accept/Reject suffices. Alerting: capturing-only (no rule) was rejected — capture ≠ alerting; but the Sentry *rules* themselves are dashboard config, left to the operator (documented).
**Affects:** backend/config.py + tests, backend/sentry_utils.py, backend/routers/{razorpay,paddle,account}.py, docs/runbooks/alerting.md (new), docs/deployment.md (rollback + prod-validation env list), frontend/src/{consent.js,components/CookieConsent.js,index.js,App.js,App.css,pages/PrivacyPolicyPage.js}, CLAUDE.md (ADMIN_SECRET boot-required), TODO.md. **Operator's remaining step:** create the Sentry alert rules + uptime monitor per `docs/runbooks/alerting.md`.

## 2026-06-21 — Footer legal links: all open as portaled modals; close stays put
**Area:** frontend · **Status:** accepted
**Decision:** All five footer legal links (FAQ, Privacy, Terms, Refund, Contact) open as **modal overlays** over the landing; every close path (footer "Close", top-right ✕, backdrop click) returns the user to the exact footer scroll position via `state.preserveScroll`, never jumping to the top or `/`. `PolicyModal` is **portaled to `document.body`** so the overlay escapes `.route-transition`, whose enter-animation `transform` otherwise becomes the containing block for the `position:fixed` overlay and pins it to the document (off-screen when opened while scrolled down). Direct `/faq` / `/privacy` / … URLs still render standalone (minimal topbar, full `<Helmet>`/JSON-LD) via each page's `isModal` prop.
**Rejected:** (a) **Dedicated standalone pages** (tried first, same day, then reverted) — a full-page nav unmounts the landing, so "Back to home" had to remount and smooth-scroll to a `#footer` anchor, which visibly flashed top-then-scrolled-down; there is no way to "stay put" without keeping the landing mounted underneath, i.e. without a modal. (b) Leaving the overlay inline in the route tree — fragile: any current/future ancestor transform (here, the route-fade-in) re-breaks fixed positioning; portaling is the durable, standard abstraction (Radix/Headless UI do the same). (c) Scroll-locking the background while a modal is open — unnecessary; the footer is *meant* to stay where it was behind the dimmed overlay.
**Affects:** frontend/src/App.js (`createPortal` PolicyModal; RouteTransition skip on `backgroundLocation`/`preserveScroll`), frontend/src/pages/{LandingPage,PrivacyPolicyPage,TermsPage,RefundPolicyPage,FAQPage,ContactPage}.js, docs/frontend.md § Policy pages.

## 2026-06-20 — SQL Interview Loop: 10 new chains (5 escalating-medium + 5 hard); chain-only band exception
**Area:** content · mock · curriculum · **Status:** accepted
**Decision:** Added 10 new SQL Interview Loop chains (28 mock-only questions: medium parents 12124–12128 → hard follow-ups; hard chains 13143–13157) — SQL was the only track thin at the track level (6 chains, lowest of any track). The 5 medium chains escalate medium→hard (the Python/DM pattern); the 5 hard chains are pure-hard. SQL mock-only 165→193, total ratio 1.40×→1.64× — above the 1.0–1.5× band, a deliberate **chain-only exception** (chains appear only in Elite Interview Loop, never benchmarks/drills; standalone budget unchanged at ~1.30× / 153 standalone). Dimensions deliberately exercise the previously-starved performance/stakeholder/abstraction pivots.
**Rejected:** (a) holding SQL at 6 chains — leaves Interview Loop thinnest on the flagship/highest-traffic track. (b) flat (non-escalating) medium chains — escalating medium→hard yields more reasoning per chain. (c) the Pareto framing for the running-total chain — dup-risk vs 3 existing questions; swapped to per-method threshold-crossing (13149).
**Affects:** backend/content/questions/{medium,hard}.json (28 new); CLAUDE.md + docs/features/mock.md + docs/content-authoring.md (counts → SQL 193 / grand total 1,148). Post-authoring audit (GPT-5.5 + Claude blind solve + detail review) added YYYY-MM format specs to 9 stems, fixed 3 hint leaks (13151/13145/12128), and retagged the 13152–13154 self-comparison chain.

## 2026-06-19 — Statistics hard Interview Loop: 6 new chains; ratio band exception for chain-only additions
**Area:** content · mock · curriculum · **Status:** accepted
**Decision:** Added 6 new hard Interview Loop chains (IDs 73077–73094, 18 questions) to Statistics, bringing the total to 7 hard chains. The 18 additions push the Statistics mock-only ratio to 1.34× — above the previously locked 1.15–1.25× band (see `docs/tracks/statistics.md` § Coverage & sizing targets). This is an intentional band exception: the 1.25× ceiling was designed to bound standalone mock questions competing on the anti-clone surface; chains appear *only* in Elite Interview Loop sessions (never in benchmarks, drills, or practice), so they do not compete with the surface the ceiling was protecting. The ratio band stands for standalone additions; chain-only additions are exempt from it because their consumption path is entirely separate. Chains exercise 6 follow-up dimensions with strong coverage of two previously-starved dimensions: `stakeholder_pivot` (3 uses across new chains, was 0) and `edge_case_pivot` (3 uses, was 0). All 6 concept families covered: bayesian inference, errors & power, experimental design, multiple testing & correction, variance decomposition & ANOVA, survival analysis & time-to-event, confidence intervals & estimation.
**Rejected:** (a) capping at 3 chains to stay under 1.25× — the ratio band exception argument is sound; artificial cap would leave Interview Loop thin at hard. (b) adding balanced medium chains instead — hard chains are the primary Interview Loop consumption path for expert users; medium chains already have 11. (c) authoring numerical follow-ups for every chain — not all scenarios support deterministic computation; 3 numerical parents (non-inferiority, O'Brien-Fleming, partial η²) was the correct boundary.
**Affects:** backend/content/statistics_questions/hard.json (18 new questions), docs/tracks/statistics.md (counts + ratio note), docs/features/mock.md (question bank status), CLAUDE.md (content footprint).

## 2026-06-19 — Interview Loop: consent-gated replay replaces the pool-exhaustion dead-end
**Area:** mock · **Status:** accepted
**Decision:** When an Elite user has completed every chain at a (track, difficulty), Interview Loop no longer hard dead-ends. The exhausted state is framed as a *completion* (positive message + the in-panel track/difficulty/mode selectors as next steps) and offers a **Replay** button that re-issues `POST /start` with `replay: true`; `_select_chain` then re-draws a completed chain (fresh-first) and flags `is_replay`. Default is unchanged — 409 `pool_exhausted` + new `replayable: true` — and replay is never silent. Relaxes the mock.md atomicity "a user sees the chain exactly once, ever" lockout: atomicity (whole-chain travel) stays; *uniqueness* becomes the default, not an absolute.
**Rejected:** (a) the prior permanent lockout ("re-showing dilutes the readiness signal") — inconsistent: the readiness *score* is a separate aggregate Interview Loop doesn't feed, and the 30-pt mock booster it *does* feed already counts recycled benchmark/custom repeats, so the lockout guarded a door left wide open next door. (b) silent auto-recycle like benchmark/custom — loses the explicit consent that makes a loop replay *more* honest than the silent recycling elsewhere. (c) a remaining-chain "N left" counter — manufactures scarcity on deep pools (8+ chains/track) and advertises the thin cells; replaced by the completion frame. (d) a destructive consumption reset — replay is a non-mutating `/start` flag, so consumption history stays intact.
**Affects:** docs/features/mock.md (§ Degradation contracts, § Follow-up Chain Atomicity rules 2 & 6, § Pre-flight access, § Backend Endpoints), docs/specs/mock-benchmark-spec.md, backend/routers/mock.py, frontend/src/pages/{MockHub,MockSession}.js + mockModeConfig.js. **Note:** the SQL/Stats/Pandas chain-pool sizing (authoring) is tracked separately — replay reduces its urgency but doesn't replace it.

## 2026-06-18 — Incident (RESOLVED): prod Postgres credential was once committed to git history; key since rotated
**Area:** ops · security · process · **Status:** accepted
**Decision:** Recorded so a future history grep doesn't re-trigger alarm. The production Postgres `DATABASE_URL` (with password) was once hardcoded in `docs/deployment.md` + `.claude/settings.local.json` (commits `33c0339`, `292a49e`) and **remains recoverable from git history** — the current tree was scrubbed by `5b88501`, which also added the standing "read `DATABASE_URL` from `backend/.env`, never hardcode it" rule. **Resolution: the prod DB credential was rotated after the event, so the value preserved in history is dead — no live exposure.** `backend/.env` is gitignored (never committed); the only connection strings in tracked files are localhost/test throwaways (CI, docker-compose, dev seed scripts). No further action required.
**Rejected:** Rewriting git history (`git filter-repo` / BFG + force-push) to purge the dead credential — not pursued: rotation already neutralizes it, and a history rewrite is invasive (rewrites every later commit hash, breaks clones/forks/worktrees) and especially risky given the concurrent worktree sessions on this repo. Rotation is the authoritative remediation for a committed secret; history-scrub is optional cosmetic cleanup at best.
**Affects:** none — incident record only. The going-forward rule lives in CLAUDE.md § "Every new Alembic migration MUST be applied to production immediately" ("read it from there — never hardcode") + the 2026-06-18 code↔prod schema-sync invariant.

## 2026-06-18 — Paddle billing-parity (rail-aware account panel), prod startup guard, Paddle legal disclosure
**Area:** pricing · ops · frontend · process · **Status:** accepted
**Decision:** Now that Paddle is live, closed three launch gaps in one batch. (1) **Billing-parity:** the Razorpay-backed `/account` panel is rail-aware — `GET /api/account/billing` detects a Paddle subscriber (paid plan, no `razorpay_subscription_id`, has `payment_events` with `provider='paddle'`) and returns `provider:'paddle'` + `managed_externally:true` + a history reconstructed from `payment_events` (the Paddle webhook now records `amount`+`currency`); `cancel`/`switch`/`update-payment` return an honest **409** ("manage via Paddle") instead of the misleading "no subscription on record" 400; the frontend hides the Razorpay-only buttons + the "Razorpay will email you" copy and formats invoice amounts by each invoice's currency. The Paddle webhook also now resolves the plan from the subscription's `price_id` (fixing the `subscription.updated` stale-`custom_data` bug — the Paddle twin of the 2026-06-18 Razorpay switch P0). (2) **Prod startup guard** (`config.validate_production_config()`): fails loud if Paddle is half-configured in prod (gated on `PADDLE_CLIENT_TOKEN`); webhook secrets/tokens/price IDs are whitespace-stripped at read time. (3) **Legal:** Privacy/Terms/Refund disclose Paddle as Merchant of Record; fixed the Privacy-policy SameSite claim (session cookie is `Lax`, the CSRF cookie is `Strict`).
**Rejected:** (a) **Adding a `paddle_subscription_id` column now** (a prod migration) — deferred: rail-detection from `payment_events` fully covers the visible parity without a mid-launch schema change; the column lands with (b). (b) **Full in-app Paddle cancel/switch via the Paddle API** — needs `PADDLE_API_KEY` + a server-side Paddle integration; deferred as a scoped follow-up. Until then Paddle subscribers manage via Paddle's emailed portal link, and the 409 says so. (c) Leaving the 400 "no subscription on record" — actively misleading for a paying customer.
**Affects:** backend/routers/paddle.py (`_plan_tier_for_price_id` + amount capture), backend/db.py (`get_paddle_payment_events`), backend/routers/account.py (rail-aware `/billing` + 409 guards), backend/config.py (`validate_production_config` + `_stripped`), frontend/src/pages/AccountPage.js + {Privacy,Terms,Refund}Page.js, backend/tests/{test_15,test_17,test_18,test_startup_config}.py, frontend/src/pages/AccountPage.test.js, docs/features/pricing.md, docs/backend.md, docs/deployment.md.

## 2026-06-18 — Drop the per-track "solve time" metric; show the mock-attempt count on every track that has one
**Area:** frontend · **Status:** accepted
**Decision:** Removed the per-track median solve-time display (the "N min" line) **and** the cross-track "you solve X slower than Y" coaching insight that ran on the same number. The metric was the median wall-clock gap between a question's *first attempt* and *first correct* submission — a calendar gap (days, if you return later), not effort; it ignored the recorded `duration_ms`, was unlabeled, and only appeared on tracks whose median gap was non-zero, so it looked arbitrary. Separately, broadened the `N in mock` accuracy qualifier to show on **any track with `mock_attempts > 0`**, not only purely-mock tracks, so the practice+mock blend is always disclosed.
**Rejected:** Labeling/keeping the time metric — it measures the wrong thing, so a label would only dignify a wrong number; a correct "solve pace" signal (median of recorded `duration_ms`, labeled, shown for every solved track) is a separate future feature, not a patch. Keeping the mock tag gated to purely-mock — left mixed tracks (e.g. SQL = practice + some mock) silently blending mock into accuracy with no hint.
**Affects:** backend/routers/insights.py (removed `_to_median_solve_seconds`, `_build_cross_track_insight`, `median_solve_seconds`, `cross_track_insight`), frontend/src/pages/ProgressDashboard.js, frontend/src/App.css, docs/features/dashboard.md §Sections.
**Note:** refines the gate in the entry below (purely-mock → any mock); that entry's other rules (accuracy = practice + mock, `—` for zero attempts) still stand.

## 2026-06-18 — Dashboard per-track accuracy: disclose mock basis, don't exclude it; null (not 0%) for zero attempts
**Area:** frontend · **Status:** accepted
**Decision:** Per-track accuracy on the Track Overview deliberately spans **practice + mock** submissions (mock answers are recorded to `submissions`; mock-under-pressure is real signal, matching weak-concepts + readiness). To stop a `0 practice solved · 100% acc` row reading as a bug, we **disclose** the basis: `accuracy_pct` is `null` → `—` when a track has zero attempts, and the row shows a muted `N in mock` qualifier when attempts are **purely mock** (`practice_attempts == 0`). Backend now also returns `attempts`/`practice_attempts`/`mock_attempts` per track (mock-only IDs resolved via `_MOCK_ONLY_IDS`, mirroring `_build_concepts_lookup`).
**Rejected:** (a) Make accuracy **practice-only** — throws away deliberate mock signal and would blank a track answered only in mocks. (b) **Suppress** low-attempt accuracy below a threshold — DM had 7 attempts (above any sane floor), so it wouldn't fix the actual confusion, which is *scope mismatch*, not sample size. (c) Leave `0.0`/`0%` for zero attempts — reads as "failed everything" and silently dragged the hero **Avg. accuracy** down (the null fix also corrects that, since it filters non-numbers).
**Affects:** backend/routers/insights.py, frontend/src/pages/ProgressDashboard.js, frontend/src/App.css (`.db-track-basis`), docs/features/dashboard.md §Sections.

## 2026-06-18 — Session debrief: family-aware known-weakness, evidence-weighted NEXT STEP, dead-output cleanup (F1–F7 audit)
**Area:** mock · frontend · **Status:** accepted
**Decision:** An audit of `build_session_debrief` (the Elite mock post-mortem) fixed seven issues. **(F1, functional)** the cross-session "known weakness" lookup keyed history by **uppercased** tags but probed with **raw** session tags, so it silently never fired for **Statistics** (100% lowercase tags) — both sides now resolve to canonical **family** (`resolve_to_family`), matching the dashboard + drill; the frontend "known weakness" badge compare is likewise made case-insensitive. **(F2/F4)** the NEXT-STEP weak-concept sort now ranks by **session misses (attempts−correct) → accuracy → known-weak → concept name**, so a better-evidenced gap (1/5) outranks a lone careless 0/1 and full ties resolve deterministically (was an implicit, mis-documented dict-insertion order). **(F5)** the time-sink ratio's denominator is the sum of per-question active times (was session wall-clock — mixed two time bases). **(F6)** removed dead `priority_question_ids` (never read by the frontend). **(F7)** pluralized follow-up / single-attempt copy and made the executable-vs-reasoning copy follow the **weak concept's own track** (fixes Mixed sessions). The two distinct weakness *definitions* are deliberately UNCHANGED — the debrief's absolute cross-session threshold (≥3 attempts & <60%) and the dashboard's relative bottom-3; only concept *matching* was unified, not the criteria.
**Rejected:** Fixing F1 by uppercasing the session tag instead of `resolve_to_family` — works only because the bank is currently variant-free (each family has exactly one tag, verified), leaves the debrief's concept identity inconsistent with the dashboard/drill, and re-breaks the moment a tag variant is authored; `resolve_to_family` already contains the uppercase as its no-match fallback, so it is a strict superset at identical cost. Also rejected normalizing the Statistics tags to uppercase in JSON (content churn; the resolver is the proper home for normalization).
**Affects:** backend/routers/insights.py, backend/tests/test_11_mock.py, frontend/src/pages/MockSession.js, docs/features/mock.md.

## 2026-06-18 — Concept-drill hub redirect uses the backend drill result, not a frontend catalog re-match
**Area:** frontend · **Status:** accepted
**Decision:** A drill link landing on the hub (`/practice/{track}?drill={concept}`) is redirected to the first drill question by navigating to the **backend** drill result's `questions[0]` (`GET /api/practice/drill` — family-aware membership + unsolved-first order), the single source of truth. The prior `AppShell` hub effect re-derived the entry question with a frontend substring match against the catalog that normalized hyphens on the *requested* concept but **not** on the question concepts — so any hyphenated family name (e.g. "MULTI-TABLE ENTITY LINKING") matched nothing and stranded the user on the TrackHub page, even though the `DrillSidebar` still populated (it already used the backend). The bug hit **both Pro and Elite**; it was concept-shaped (hyphen), not plan-shaped — the original report mis-attributed it to plan because its two test cases varied both plan *and* concept. One fix covers every drill-link source (benchmark / custom / Interview Loop post-mortems, dashboard, landing) since all share this redirect.
**Rejected:** De-hyphenating both sides of the frontend substring match — still diverges from the backend family resolver (a family name need not be a substring of any member tag), so it would re-leak the same bug class for non-substring families; reusing the already-fetched `drillData` deletes the second matcher entirely (single SoT).
**Affects:** frontend/src/components/AppShell.js, frontend/src/components/AppShell.test.js, docs/frontend.md §Concept drill.

## 2026-06-18 — Fix P0: Pro→Elite plan switch was charged but never applied (webhook resolves plan from plan_id, not stale notes)
**Area:** pricing · ops · **Status:** accepted
**Decision:** `switch-plan` updated the Razorpay subscription's `plan_id` (so the customer *was* charged the new tier) but deferred the DB change to the webhook, which read `notes.target_plan` — frozen at subscription creation and never updated by a switch — so `_apply_plan_change` saw `old==new` and no-op'd. Net: a Pro→Elite upgrade billed Elite and left the user on Pro forever, silently. Two-part fix: (1) the webhook now resolves the plan for `subscription.activated`/`.charged` from the subscription's **authoritative `plan_id`** (`_plan_tier_for_plan_id`, the reverse of `_plan_ids`/`_plan_ids_usd`), falling back to `notes.target_plan` only for orders/lifetime (`payment.captured`, which carry no `plan_id`); (2) `switch-plan` with `timing='now'` (upgrades) now applies the new plan to the DB **immediately** (instant entitlement) instead of waiting for the webhook. `cycle_end` switches stay deferred to the now-correct webhook. Guards: `test_tc223b` (charged event with the new `plan_id` + stale notes → user upgraded) + DB-state assertions added to TC-239/TC-240.
**Rejected:** (a) **Refreshing `notes` in `subscription.update`** (the obvious patch) — depends on Razorpay propagating mutated notes onto child `subscription.charged` events (unverified) and risks an unsupported `notes` field on the update API; `plan_id` is already authoritative and current, so resolving from it is robust and assumption-free. (b) **Applying the DB change immediately for ALL timings** — wrong for `cycle_end` (a downgrade must keep the higher tier until the cycle ends; a scheduled upgrade must not grant early); only `timing='now'` may apply immediately.
**Affects:** backend/routers/razorpay.py (`_plan_tier_for_plan_id` + webhook plan resolution), backend/routers/account.py (`switch_plan` immediate-apply for `now`), backend/tests/test_14_payments.py (`test_tc223b`), backend/tests/test_17_account.py (TC-239/TC-240 DB assertions), docs/features/pricing.md.

## 2026-06-17 — Ship light-only at launch; dark mode deferred to a future version (dormant, not deleted)
**Area:** frontend · design · process · **Status:** accepted
**Decision:** For launch we ship **light mode only**. The dark-mode differentiation pass (role-differentiation, tracked under `dark_accent_differentiation`) is unfinished, so rather than expose a half-done dark theme we lock the app to light: `ThemeProvider` (`App.js`) and the `index.html` pre-paint bootstrap both force `theme='light'` / `data-theme="light"`, ignore `localStorage` + OS `prefers-color-scheme`, and flush any stored `theme`; the toggle is removed from `Topbar` (desktop + mobile). Everything dark stays **dormant** — the `[data-theme="dark"]` CSS, the CodeEditor forest/charcoal switch, the `isDark` logo logic, and the landed `#2FBE6B` hue work all remain; `isDark` is just permanently false, so re-enabling dark later is a near-one-line flip at those two lock points.
**Rejected:** (a) Exposing the half-finished dark theme via the existing toggle — ships visibly unfinished work and undercuts the polished light surface that is the brand's dominant face. (b) Ripping out the `[data-theme="dark"]` CSS + `isDark` plumbing — discards weeks of landed dark work (charcoal pivot, accent-hue tuning, code-surface theming) we intend to finish and re-ship; deletion would make re-enabling a rebuild, not a flag flip.
**Affects:** frontend/src/App.js, frontend/src/components/Topbar.js, frontend/index.html, docs/design/color-palette.md, docs/frontend.md, CLAUDE.md § Design system.

## 2026-06-17 — Sentry replay masking, frontend Sentry.setUser, sourcemap cleanup
**Area:** frontend · ops · privacy · **Status:** accepted
**Decision:** Three coordinated Sentry hardening changes before launch: (1) `maskAllText:true` on Session Replay so visible PII (email in topbar, question content) never reaches Sentry's servers; (2) `Sentry.setUser` added in `AuthContext.js` alongside `identifyUser` calls so frontend errors carry user/plan attribution independent of backend tagging; (3) `filesToDeleteAfterUpload: ['./dist/**/*.map']` in the Vite plugin so `.map` files are uploaded to Sentry then stripped from the served bundle (`hidden` only omits the `sourceMappingURL` comment — files were still publicly accessible at `/assets/*.js.map`). Privacy policy updated to accurately describe these behaviors.
**Rejected:** Keeping `maskAllText:false` for richer replay signal — the mismatch with the policy's "anonymised" wording and the real PII leak (email rendered in topbar captured verbatim in replays) outweighed the triage convenience. User attribution is retained via explicit `setUser(id, email)`; the only debugging loss is unmasked ambient page text.
**Affects:** `frontend/src/index.js`, `frontend/src/contexts/AuthContext.js`, `frontend/vite.config.js`, `frontend/src/pages/PrivacyPolicyPage.js`, `docs/frontend.md`

## 2026-06-17 — Dark accent re-sharpened #5FB98C → #2FBE6B (the mute went too pale/minty)
**Area:** frontend · design · **Status:** accepted
**Decision:** Lived-in, the muted `#5FB98C` (HSL 150/39/55) read **pale/washed-out and minty** — at 39% saturation the crisp track colors always drowned it, and hue 150 leans teal rather than green. Re-sharpened the dark `--accent` to **`#2FBE6B`** (HSL 145/60/47): hue pulled off the mint edge toward emerald (150→145), saturation back to ~60%, held slightly darker (L 47%) so it's vivid and legible (~7.2:1 on cards) without the `#43D27C` glow. The one token carries ~263 uses; the 5 hardcoded mock-hub `rgba(95,185,140)` literals, the dark brand mark (`mark-reverse-no-bg.svg` + re-rendered favicon/icon/OG PNGs), and `--brand-accent` all follow. **Hue-only — frequency/role usage unchanged.** Light mode untouched.
**Rejected:** (a) Nudging saturation up while keeping hue 150 (`#5BC18E`-ish) — still minty; the *hue* was the problem, not just chroma. (b) Returning to the bright `#43D27C` — that's the glow the mute removed. (c) Moving the brand accent off green to a non-green hue (the textbook brand≠success separation) — a partial rebrand of the green datathink identity; named honestly, deferred, not taken.
**Affects:** frontend/src/App.css (dark `--accent`/`-strong`/`-soft`/`-soft-strong` + `--brand-accent` + 5 rgba literals + 2 comments), frontend/public/{favicon,icon-maskable,og-image,branding/mark-reverse-no-bg}.svg + 7 re-rendered dark PNGs, docs/design/color-palette.md, docs/frontend.md, docs/design/dark-mode-comparison.html.
**Refines:** 2026-06-17 `dark_accent_mute` (hue value only — its "mute the accent globally, don't make green rare" rule is untouched here; a separate, deliberate role-differentiation pass will revisit that).

## 2026-06-17 — Mock NEXT STEP breaks weak-concept ties toward a known cross-session weakness
**Area:** mock · frontend · **Status:** accepted
**Decision:** When session concepts tie on accuracy + attempts (e.g. several at `0/1`), the debrief's NEXT STEP concept (`weak[0]`) now breaks the tie toward a **known cross-session weakness** (≥3 past attempts, <60% historical accuracy) before falling back to question order. The "known weak" test — already used for the pattern wording — is extracted to one helper `_is_known_weak` and reused in the `weak` sort (single definition, no duplication). The frontend mirrors this by **floating the backend's `priority_concept` to the top row** of the concept breakdown rather than re-deriving the tiebreak in JS, so the table's first row always equals NEXT STEP.
**Rejected:** (B) Replicating the known-weak tiebreak in the frontend `conceptRows` sort via `insights.weakest_concepts` — two definitions of "known weak" (backend `hist_concept` vs frontend `weakest_concepts`) that can drift on a tie; rendering the backend's single pick can't drift. Context: before this, the alignment users noticed (NEXT STEP landing on the badged concept) was *coincidental* — the Interview-Loop parent question simply listed that concept first; reversing question order flipped the pick (verified empirically).
**Affects:** backend/routers/insights.py (`_is_known_weak` helper + `weak` sort tiebreak), frontend/src/pages/MockSession.js (`priorityKey` float in `conceptRows`), backend/tests/test_11_mock.py (`test_debrief_tiebreak_prefers_known_weakness`), frontend/src/pages/MockSession.test.js (fixes a test left red by the concept-only commit + adds the float test), docs/features/mock.md.

## 2026-06-17 — Mock post-mortem is concept-drill-only; the dashboard is the sole concept+path surface
**Area:** mock · frontend · **Status:** accepted
**Decision:** The entire mock post-mortem now links to **concept drills only — no learning paths.** (1) The debrief "Next step" drops the path secondary it briefly had — the concept drill is the only link. (2) The concept breakdown makes **every weak concept independently drillable** — each weak row carries its own `Drill →` (`/practice/{row.track}?drill={row.concept}`) so the user *chooses* which to attack, replacing the single aggregate "Drill weak concepts →" that deep-linked one arbitrarily-chosen concept (and disagreed with the debrief's pick on ties). (3) The custom/Loop footer's concept-drill primary is removed → footer is `Share` + `Back to drill lobby`. (4) The benchmark `Study {path} →` aggregate is dropped. `build_session_debrief` no longer returns `priority_path_slug`/`priority_path_title`. The dashboard alone keeps concept-primary / path-secondary.
**Rejected:** Keeping the path as an honest secondary in the debrief (the prior call) — the user wants mock kept strictly concept-oriented and the path affordance consolidated on the dashboard, so there is exactly one place paths are recommended. Also rejected: a relabeled "Drill weakest concept →" footer button — redundant once every weak row is drillable.
**Affects:** backend/routers/insights.py (`build_session_debrief` — path fields removed), frontend/src/pages/MockSession.js (per-row drill links; aggregate + footer drill buttons + dead `topDrillConcept` removed), frontend/src/App.css (`.mock-concept-drill-link`; removed `.mock-debrief-path-link-secondary`), backend/tests/test_11_mock.py, docs/features/mock.md, CLAUDE.md.
**Supersedes:** 2026-06-17 — Mock debrief "Next step" is the concept drill, not a learning path

## 2026-06-17 — Preview/milestone upgrade nudges: green wash in dark (drop the clashing indigo)
**Area:** frontend · design · **Status:** accepted
**Decision:** Removed the one-off indigo dark overrides (`rgba(91,106,240,0.12)`) on `.preview-locked-callout` (the "PREVIEW MODE" block on locked questions) and `.track-hub-milestone-upgrade`, so both inherit their green `--accent-soft` base in dark — matching their own green left-border + "Unlock with Pro" CTA and the other (already-green) upgrade nudges. The indigo clashed with those green accents and read as a jarring blue block on the charcoal UI. Light is unchanged (it was always green via the base; only the dark overrides were removed).
**Supersedes:** the "`preview-locked-callout` / `track-hub-milestone-upgrade` keep their pre-existing blue/info wash" carve-out from the dark_accent_mute entry below — that deferral is resolved; all upgrade nudges now read green in both themes.
**Affects:** frontend/src/App.css (the two dark overrides removed; base green `--accent-soft` rules unchanged).

## 2026-06-17 — Free users on a drill link: redirect to the clean URL + a dismissible Pro upsell banner
**Area:** frontend · gating · product · **Status:** accepted
**Decision:** Refines the "silent fallback" from the drill-gating entry below. When a non-Pro user follows a concept-drill link, `AppShell` now (1) **redirects to strip the inert `?drill=`** — `navigate(cleanPath, { replace: true, state: { drillUpsell } })` — for both shapes (`/practice/:t/questions/:id?drill=` → the question; `/practice/:t?drill=` → the hub), so the URL is clean and shareable; and (2) shows a **dismissible Pro upsell banner** below the topbar ("Concept drills are a Pro feature" + `UpgradeButton`), driven by the router-state flag and re-armed per navigation. Pro/Elite are untouched — `?drill=` preserved, drill renders, no banner. Verified all four paths in-browser (free question / free hub / dismiss / Pro); 193 frontend tests green.
**Rejected:** A toast (the earlier idea) — wrong affordance for a feature the user *actively clicked* (transient, easy to miss) and it never fired reliably from `AppShell`; a persistent dismissible banner is the premium gated-feature pattern and is deterministic. This supersedes the "dropped the upsell for a clean silent fallback" note in the drill-gating entry below.
**Affects:** frontend/src/components/AppShell.js (redirect effect + `DrillUpsellBanner`), frontend/src/App.css (`.app-banner-upsell`), docs/frontend.md.

## 2026-06-17 — Mock debrief "Next step" is the concept drill, not a learning path
**Area:** mock · frontend · **Status:** superseded
**Decision:** The Elite session debrief's priority action now leads with the **concept drill** (`/practice/{track}?drill={concept}`) and demotes the matching curated path to an honest secondary — matching the dashboard weak-areas panel and the rest of the mock post-mortem. `build_session_debrief` always emits `priority_concept`/`priority_track` + a drill-oriented `priority_action` for a weak concept; the path lookup only populates the secondary `priority_path_slug`/`priority_path_title`. The Interview-Loop summary (e.g. `/mock/:id`) previously deep-linked `/learn/...` as the primary "Next step", contradicting the documented contract. Also dropped the frontend regex that munged the path sentence, and the duplicate local `_TRACK_DISPLAY` (now the module-level `_TRACK_LABELS`).
**Rejected:** Removing the path from the debrief entirely — the dashboard keeps it as an honest secondary ("or take the … path"), so the fix is *priority*, not removal.
**Affects:** backend/routers/insights.py (`build_session_debrief`), frontend/src/pages/MockSession.js, frontend/src/App.css, backend/tests/test_11_mock.py (`test_debrief_weak_concept_recommends_drill_not_path`), docs/features/mock.md. Completes the intent of the 2026-06-13 concept-drill rollout ("insights.py … debrief → `?drill=`").

## 2026-06-17 — Concept drills are UI-gated to Pro+ (fixes a 403 that shimmered the sidebar forever)
**Area:** frontend · gating · **Status:** accepted
**Decision:** The concept-drill UI now enforces the Pro+ gate the backend already had. `GET /api/practice/drill` returns 403 for Free/anonymous (tested: `test_tc_d01_free_user_403`), but `AppShell` entered drill mode purely from the `?drill=` URL param — rendering the "Drilling:" pill and a `DrillSidebar` whose `!drillData` branch **shimmered forever** once the 403 was caught (drillData stayed null). Fix: AppShell derives `canDrill` from the plan (fail-closed while auth loads) and treats `?drill=` as **inert** for non-Pro — they get the normal practice question + catalog sidebar, no drill chrome, no Pro-only fetch. `DrillSidebar` now distinguishes loading from loaded-empty/failed (finite "Drill unavailable" fallback, never an infinite shimmer). `QuestionPage`'s own drill fetch is gated the same way. So the backend 403 becomes defense-in-depth, not the only gate; `frontend.md`'s "Free users never reach a live drill" is now actually enforced client-side.
**Rejected:** (a) An upsell toast for Free users hitting a drill link — built it, but `notify` didn't fire reliably from `AppShell` and it's a non-essential nicety; dropped for a clean silent fallback (the question still loads normally). Possible follow-up. (b) Backend-only fix — the 403 was already correct and tested; the defect was the UI ignoring it, so the fix belongs client-side.
**Affects:** frontend/src/components/AppShell.js (`canDrill` gate + DrillSidebar loading/error states), frontend/src/pages/QuestionPage.js (drill fetch plan-gate), frontend/src/components/DrillSidebar.test.js (new), docs/frontend.md.

## 2026-06-17 — Code/sandbox surfaces go charcoal in DARK mode (editor + code outputs + MCQ evidence)
**Area:** frontend · design · **Status:** accepted
**Decision:** The always-dark code surfaces are now **theme-aware**: forest-green under LIGHT pages (unchanged), neutral **charcoal `#16181C`** under DARK pages. (1) The Monaco editor gains a second theme `charcoal-dark` alongside `forest-dark`; `CodeEditor` reads `useTheme().isDark` and switches the `theme` prop (reactive to the toggle, no reload). (2) The whole code-surface family follows via dark-scoped App.css overrides: `.editor-wrapper`, `.question-code-snippet`, `.question-evidence-card`, `.scenario-context-pre`, `.solution-card pre`, `.quality-alt-code`, `.test-case-error-content`, `.print-output-content` (forest bases `#0F2218`/`#0F1C13`/`#0B1710` kept for light). (3) **Stale old-green cleanup** — `rgba(67,210,124,…)` (the old `#43D27C`) that escaped the Move-1 token mute because it was hardcoded: the MCQ selected-option wash → `var(--accent-soft)`, plus 5 mock-hub literals (blueprint gradient, selected mode-card border, Pro badge, benchmark empty-card) → muted. So in dark, code reads as part of the calm charcoal UI, not green islands; light keeps the praised forest two-tone.
**Rejected:** (a) Charcoal editor in **both** themes — would change the praised light-mode editor; dark-only preserves the brand two-tone where it lives. (b) Charcoal **only** the editor, leaving code outputs/evidence forest — half-done: a charcoal editor above forest-green output/evidence panels is the inconsistency this set out to remove. (c) Re-tinting the landing HeroIDE (`.lp-ide*`, still forest + old-green sweep) — it's a marketing surface, out of scope (the `.lp-ide` old-green literals are deliberately left).
**Supersedes:** the **editor sub-decision** of the 2026-06-12 charcoal entry below — point (3) "the always-dark editor stays forest-green in both themes … we explicitly did *not* adopt a neutral-charcoal editor" and its rejected-(c). That call predated muting the accent; once the whole dark UI went calm charcoal with a muted `#5FB98C`, the forest editor became the lone green *environment* left — the very thing the charcoal pivot removes. The rest of the 2026-06-12 entry (charcoal surfaces, two-tier green, deep CTA) still stands.
**Affects:** frontend/src/components/CodeEditor.js (`charcoal-dark` theme + `useTheme` switch), frontend/src/App.css (dark code-surface overrides + stale-green mutes), docs/design/color-palette.md, docs/frontend.md, docs/design/dark-mode-comparison.html, CLAUDE.md.

## 2026-06-17 — Logo mark + brand assets re-matched to #5FB98C (completes the deferred follow-up)
**Area:** frontend · design · **Status:** accepted
**Decision:** Re-colored the dark brand mark `#43D27C` → the muted UI accent `#5FB98C` across the live brand sources (`favicon.svg`, `icon-maskable.svg`, `og-image.svg`, `branding/mark-reverse-no-bg.svg`) and regenerated the 7 dark favicon/app-icon/OG PNGs via `frontend/scripts/render-brand-assets.mjs` (light variants re-rendered byte-identical; the secondary `#87B09A` block unchanged). Completes the "Deferred" item in the dark_accent_mute entry below — the mark tracks the UI accent again, per the 2026-06-13 precedent.
**Rejected:** Re-coloring the unused `branding/lockup-bar-reverse-no-bg.{svg,png}` lockup — it isn't referenced in-app (Topbar/Sample use `mark-*.svg`) and its *transparent* PNG isn't covered by the opaque-background render script, so editing only the SVG would leave an SVG/PNG mismatch on a dead asset. Left matched-but-untouched; re-export manually if it ever goes into use.
**Affects:** frontend/public/{favicon.svg, icon-maskable.svg, og-image.svg, branding/mark-reverse-no-bg.svg} + 7 regenerated dark PNGs; docs/design/color-palette.md; docs/frontend.md.

## 2026-06-17 — Dark-mode accent muted to #5FB98C; broad "de-overload" trialed and reverted
**Area:** frontend · design · **Status:** accepted
**Decision:** Dark `--accent` dropped from the glowing `#43D27C` (HSL 144/61/54) to a muted sage-emerald **`#5FB98C`** (150/39/55) — chroma 61%→39% with lightness held (~7.2:1 on cards), chroma-matched to `--success` (the calm green nobody flagged). One token recolors ~263 uses, so this *is* the fix. Two component tweaks rode along: the **SESSION DEBRIEF** results panel goes neutral chrome in dark (a green wash there competed with the page CTA — bullets/link/✓ stay green), and **`.tier-banner-paid`** (Pro/Elite) gets the same green wash as `.tier-banner-free` in *both* themes (the one deliberate light-mode touch). The deep CTA `#1C8A4F` is unchanged; light mode otherwise untouched.
**Rejected:** (a) The "blind" full inversion of the light tokens — too green for an educational tool (the original reason for the charcoal pivot). (b) Bumping back up to `#5BC18E` (43% sat) — imperceptible next to `#5FB98C` and works against the calm goal. (c) Darkening the CTA (`#258A53`, `#1A6B40`) to harmonize — it recedes into the charcoal and lets the light active-pills outshine the primary action; CTA stays the deep `#1C8A4F`. (d) **The broad de-overload** — demoting link-arrows / kickers / numerals / upgrade-washes to neutral — trialed across every surface and **reverted**: on a dark ground *color is the click/affordance signal* (neutral links read as dead) and muted kickers/numerals look flat. So the durable rule is "**mute the accent globally + neutralize one results panel**," not "make green rare." (The pre-existing `.mock-hub-page` kicker neutralization is left as-is so the dense hub doesn't over-green.)
**Deferred:** the logo-mark SVG + rendered favicons/OG card are still `#43D27C` (a static brand-asset pipeline) — re-rendering to `#5FB98C` to re-match is a non-blocking follow-up.
**Affects:** frontend/src/App.css (dark `--accent*` tokens + the dark-mode-accent-calm block + `.tier-banner-paid`), docs/design/color-palette.md, docs/frontend.md, docs/design/dark-mode-comparison.html.

## 2026-06-16 — Interview Loop difficulty RESTORED as a track-aware selector (reverses 2026-06-15)
**Area:** mock · gating · frontend · product · **Status:** accepted
**Decision:** Re-added a difficulty selector to Interview Loop — the user picks **medium or hard**, track-scoped: Python → medium only (escalates to hard); ML-Fundamentals/Experimentation → hard only; the rest → medium + hard (Data-Modeling medium also escalates). `/access` returns per-difficulty `{can_start, block_reason, escalates}`; `/start` requires the difficulty and **stores it** (no longer NULL); `_select_chain` draws from the chosen difficulty. Escalating loops (Python-medium, DM-medium — verified 18/18 and 4/4 of their medium chains go medium→hard) display as **"medium → hard"** everywhere (selector hint, brief, session view, MockHub + dashboard history) via `formatLoopDifficulty()`. The debrief now detects loops by **mode**, not null difficulty.
**Why this reverses 2026-06-15:** that decision dropped the selector for three reasons, each now answered — (1) *"the selector was the root of every dead-end"* → the **grey + clickable + message** gating convention standardized 2026-06-16 handles unavailable levels cleanly (no bespoke caption machinery); (2) *"don't author hard Python chains"* → respected: Python stays **medium-only** with the escalates-to-hard note, we author nothing; (3) *"no analytics group by difficulty, so NULL loses nothing"* → NULL was the literal root of the P3 "try a hard session" debrief bug, and the value here is **user control** (warm-up vs stress-test) + a clean P3 fix, not analytics. New data was decisive: only **2 of 9 tracks escalate** — the original was over-generalized from Python.
**Rejected:** (a) keep it emergent + display-only (no choice) — loses the user-control benefit the reversal is *for*. (b) store the difficulty as "medium→hard" — breaks the difficulty enum/filtering; store the entry level and *display* the arrow instead.
**Supersedes:** 2026-06-15 "Interview Loop drops the difficulty selector entirely."
**Affects:** backend/routers/mock.py (`_loop_difficulty_escalates`, start, access, session/history `escalates`), insights.py (debrief mode-detect), tests/test_11_mock.py; frontend/src/mockModeConfig.js (`formatLoopDifficulty`), pages/MockHub.js, MockSession.js, ProgressDashboard.js (+ tests); docs/features/mock.md.

## 2026-06-16 — Weak-concept detection counts mock attempts; the drill it links to stays practice-only
**Area:** mock · dashboard · frontend · **Status:** accepted
**Decision:** The dashboard "weakest concepts" panel and the mock post-mortem resolve each submission's `question_id`→concepts via `_CONCEPTS_LOOKUP` (`routers/insights.py`), which was built **practice-only**. So every mock-only question resolved to an empty concept list and a mock miss counted toward *no* concept — silently discarding the strongest weakness signal there is (solving under interview pressure). `_build_concepts_lookup` now spans practice **+ mock-only** questions, so mock failures feed weak-concept on both surfaces (one lookup, both consumers — `mock.py` imports it). The **drill** the weak concept links to is unchanged and stays **practice-only** (`/api/practice/drill` reads the practice catalog): mock-only questions are *detected-from* but never *served back* in a drill. Investigation also confirmed the practice solve-count (`/api/dashboard` ∩ catalog), readiness coverage (∩ catalog by difficulty), and the drill were already mock-free; accuracy + readiness already blend mock by design — so weak-concept was the sole gap.
**Rejected:** (a) exclude mock from weak-concept (my first read of "don't pollute the dashboard") — operator corrected: a mock miss is the *biggest* indicator of a weak concept, not noise. (b) also serve mock questions in the drill — the drill is a practice remediation walk; mock-only content must never surface there. Detection (includes mock) and remediation (practice-only) are deliberately separate.
**Affects:** backend/routers/insights.py (`_build_concepts_lookup`), backend/tests/test_12_dashboard.py (TC-182b), CLAUDE.md, docs/features/dashboard.md, docs/features/mock.md.

## 2026-06-16 — One gating convention on the mock hub: grey + clickable + message (never hard-disable)
**Area:** mock · gating · frontend · **Status:** accepted
**Decision:** Every gate on MockHub now follows one pattern — the control is visually greyed, stays **clickable** (never `disabled`/hidden), and surfaces a clear message on interaction. Sonnet established this on the **Mixed track pill** (greyed + clickable → "Interview Loop runs on one track — can't choose Mixed." / the gold "needs a role" hint), replacing the earlier auto-switch. This pass extended it to the two **Focus-mode gates** that still broke it: the Elite lock was a dead `disabled` checkbox → now a clickable card that reveals "Focus mode is an Elite feature." + an Unlock-with-Elite CTA; the 1–3 concept cap hard-`disabled` the extra pills → they stay clickable and a 4th click shows "You can focus on up to 3 concepts at a time." Mode cards (Pro/Elite locks) and difficulty pills (plan locks) already conformed.
**Rejected:** (a) hard-`disabled` controls — greys the thing but gives the user no reason and nothing to click (the dead-end this convention exists to kill). (b) hiding a gated option — removes the signal entirely; the user can't learn what they'd unlock. (c) leaving the focus gates "because they sort of communicate" — partial conformance is how drift starts.
**Supersedes:** 2026-06-15 "Interview Loop on Mixed auto-switches the track" — the Mixed+Loop conflict is now the greyed-clickable Mixed pill (Sonnet 3f1e4e6→99b65f8), not a mode-driven auto-switch.
**Affects:** frontend/src/pages/MockHub.js (focusLockNote/focusLimitNote + clickable focus gates), frontend/src/App.css (`.mock-focus-locked` button, `.mock-focus-lock-note`, `.mock-focus-limit-note`, `.mock-focus-concept-pill--blocked`), frontend/src/pages/MockHub.test.js, docs/features/mock.md (§Surface in the UI)

## 2026-06-15 — Interview Loop on Mixed auto-switches the track (not a silent disabled card)
**Area:** mock · frontend · **Status:** superseded
**Decision:** Interview Loop is single-track, so it's incompatible with the Mixed track. Previously the Interview Loop mode card just went `disabled` (not-allowed cursor) with **no explanation** — users didn't know why they couldn't pick it. Now the card stays enabled; selecting it while Mixed is active **auto-switches** the track to a single track (the selected role's first, else SQL) and shows an indigo note ("Interview Loop runs on one track — switched to SQL."). Latest explicit click wins, with a visible reason — no dead-end.
**Rejected:** (a) Keep it disabled but add a "single-track only" tooltip/sub-line — transparent, but still a manual two-step dead-end (user picked by the product owner over this). (b) Gate the "Mixed" track pill when Loop is the mode — a bigger mode-first interaction change. Note the reverse path (clicking the Mixed pill *while on* Loop) still flips mode→Custom via the existing effect, so the conflict never persists either way.
**Affects:** frontend/src/mockModeConfig.js (loop card `disabled: false`), frontend/src/pages/MockHub.js (`handleSelectMode` + note), frontend/src/App.css (`.mock-loop-switch-note`), docs/features/mock.md

## 2026-06-15 — Mock "Run Code" gets its own session-scoped endpoint (not the practice run path)
**Area:** mock · backend · frontend · **Status:** accepted
**Decision:** Added `POST /api/mock/:id/run` and pointed MockSession's "Run Code" at it. The mock run gate is **session membership** (the question must be in the user's active session), not the practice unlock state. Fixes a bug where running code in a mock session 403'd *"Question is locked for your current plan or progress"* — because mock reused the practice `/{track}/run-code` endpoint, whose unlock check treats mock-only questions (every Interview Loop chain) as locked. The new endpoint reuses the same run functions (`run_query` / `run_python_code` / `run_pandas_code_checked`), so output is identical (public test cases + stdout, no solution, no progress); it mirrors the existing `/{session_id}/submit` structure. Run is callable repeatedly; Submit stays one-shot (documented contract, unchanged).
**Rejected:** (a) Skipping the lock for `mock_only` questions in the four practice run endpoints — smaller, but couples "runnable" to a content flag and lets any caller run a mock-only qid; session membership is the correct gate. (b) Removing the unlock check from practice run entirely — changes practice behavior, out of scope.
**Affects:** backend/routers/mock.py (`run_mock_code`, `_run_submission`), frontend/src/pages/MockSession.js, docs/features/mock.md (§ Active Session + Backend Endpoints), backend/tests/test_11_mock.py

## 2026-06-15 — Interview Loop drops the difficulty selector entirely (difficulty is emergent)
**Area:** mock · gating · frontend · product · **Status:** superseded
**Decision:** Removed difficulty from Interview Loop end-to-end: no difficulty selector in MockHub, no `difficulty` in the start payload, the chain is drawn from the track's **full** pool (`_chain_parents_for(track, "mixed")`), and the session stores `mock_sessions.difficulty = NULL`. Gating is **track-level** via `access.mixed` (Start disabled + "you've completed every chain for this track" when the pool is exhausted). History/dashboard show no difficulty badge for loop rows; per-question difficulty stays visible in-session. Rationale: Interview Loop *is* the escalating-follow-up chain — difficulty is intrinsic to the chain, not a user knob; the selector was the root of every dead-end (easy-everywhere, Python-no-hard, ML/Exp-no-medium) and the grey/caption machinery only treated the symptom.
**Rejected:** (a) Keeping difficulty as an optional advanced filter — re-imports the gating complexity we deleted. (b) Authoring hard Python chain parents to fill the "no hard" slot — contradicts the documented Python curriculum (`docs/tracks/python.md`: chains are medium-parent → hard-follow-up *by design*); the hard reasoning already lives in the follow-up. (c) Storing a single session difficulty (parent's, or "mixed") — misleading for a chain that spans medium→hard, and **no analytics groups by mock difficulty** (verified: `_compute_mock_analytics` keys on mode/concept/dimension; dashboard difficulty is practice-only), so `NULL` loses nothing.
**Affects:** backend/routers/mock.py, frontend/src/pages/MockHub.js (+ `.test.js`), frontend/src/App.css, docs/features/mock.md, backend/tests/test_11_mock.py; also fixed a stale `/api/mock/analytics` docstring that claimed "track/difficulty splits"
**Supersedes:** 2026-06-14 "Interview Loop difficulty: grey + red caption…" and 2026-06-14 "Interview Loop difficulty is gated to chain availability…" (both now moot — there is no difficulty to gate)

## 2026-06-14 — Interview Loop difficulty: grey + red caption on the selected blocked pill, no auto-jump
**Area:** mock · frontend · **Status:** superseded
**Decision:** Replaced the auto-select-first-available behavior with a static treatment: blocked difficulty pills are greyed but stay selectable, and a **red caption** under the selector explains the *selected* blocked difficulty only ("Easy has no Interview Loop chains. Chains are available at Medium and Hard."). The session-brief rail no longer shows the difficulty block message for Interview Loop (no duplication). Also gave `.mock-drill-plan-mode` a dark-theme override (it was near-invisible `#3d4ed7` on the dark surface) to match `.mock-benchmark-blueprint-time`.
**Rejected:** Auto-jumping selection off the blocked difficulty (the prior approach) — it flashed the rail's red block message for a split second before correcting, which read as a pointless error blip; a stable greyed pill + on-selection caption is calmer and clearer.
**Affects:** frontend/src/pages/MockHub.js, frontend/src/App.css, docs/features/mock.md
**Supersedes:** the auto-jump portion of "2026-06-14 — Interview Loop difficulty is gated to chain availability" (the access-gating itself stands)

## 2026-06-14 — Interview Loop difficulty is gated to chain availability, not offered blindly
**Area:** mock · gating · frontend · **Status:** superseded
**Decision:** Interview Loop draws only from mock-only chains, which exist at medium/hard and not for every track (no track has easy chains; Python has no hard, ML/Experimentation no medium). The access endpoint now reports per-difficulty chain availability for `interview_loop` (consumption-aware: `no_chains` vs `pool_exhausted`), the MockHub selector disables blocked difficulties and auto-selects the first available, and `/mock/start` returns a clear **403** for a chain-less difficulty (the **409** `pool_exhausted` is reserved for the dynamic "user consumed them all" case). Fixes a dead-end where Elite + Easy + Interview Loop showed a generic "Failed to start session" — the 409's dict-detail `block_copy` was spread to the top level of the error body and the frontend only read `data.error` / `data.detail`, so the helpful copy was dropped.
**Rejected:** (a) Authoring easy chains — a chain is deep follow-up reasoning, which is not "easy"; bending the curriculum to satisfy a UI control. (b) Hardcoding which difficulties have chains in the frontend — violates single-SoT and drifts as content changes; availability is derived from the question files instead. (c) Making `compute_mock_access` chain-aware — keeps the pure plan/caps function clean (existing `test_tc134f` plan-level truth preserved); chain availability is a content concern layered in the router.
**Affects:** docs/features/mock.md (§ Pre-flight access check, § Interview Loop Eligibility + Chain selection algorithm), backend/routers/mock.py, frontend/src/pages/MockHub.js

## 2026-06-13 — Public /pricing page: spell out every tier feature (transparency as the selling point), from one data source
**Area:** pricing · frontend · product · **Status:** accepted
**Decision:** Added a dedicated public `/pricing` page (`PricingPage.js`) laying out the full Free/Pro/Elite comparison in plain language + the Free-tier difficulty-unlock ladders, reached from a "Compare every feature in detail →" link below the landing pricing cards. Rationale: a reasoning-premium product should be transparent about exactly what each tier offers — helping a user honestly self-select (even to "Free/Pro is enough") builds trust and yields better-quality conversions. The three landing cards stay **brief** (brevity converts); the full honesty lives one click away (**progressive disclosure**). Copy is concrete — no vague adjectives.
**Rejected:** (a) inline-expanding the matrix under the cards / a modal — chose a real **shareable page** (linkable from emails, topbar, upgrade CTAs; room to breathe). (b) reusing the internal `docs/tier-wise-features.html` directly — that's a file-path-referencing internal doc, not a themed customer surface. (c) hardcoding the matrix in the page — instead **one source `frontend/src/data/tierFeatures.js`** feeds it, to fight the drift of entitlements living in pricing.md + mock.md + unlock.py + the landing cards + the docs HTML.
**Affects:** `frontend/src/data/tierFeatures.js` (new, single source) · `frontend/src/pages/PricingPage.js` (+ `.test.js`) · `frontend/src/App.js` (route) · `frontend/src/App.css` · `frontend/src/pages/LandingPage.js` (compare link) · `docs/frontend.md` · `docs/features/pricing.md`. Keep `tierFeatures.js` synced with the entitlement SoT.

## 2026-06-13 — Weak-areas coaching panel ungated to Pro+ (was Pro=1 / Elite=full); Elite differentiates on the prescription
**Area:** dashboard · pricing · product · **Status:** accepted
**Decision:** The dashboard "Where to focus" panel gated Pro to the **single top** weak concept and Elite-locked the rest ("Elite members see all gaps"), while the logged-in landing already showed Pro **all** weak concepts — an inconsistency that also undercut the Elite upsell (Pro saw on the landing exactly what the dashboard charged Elite for). Reconciled by **ungating the dashboard to match the landing**: any paying user (Pro+) sees the full gap list; Free still sees the upgrade teaser. Removed the Elite-lock overlay + its dead `.db-weak-locked*` CSS.
**Rejected:** the reverse — **gating the landing down to Pro=1** to preserve the Elite lock. Paywalling a user's *own* weak-spot diagnosis between paid tiers ("pay Elite to see your other 2 weaknesses") is the "serving a metric, not the user" anti-pattern, and Elite keeps a strong, honest differentiator without it: the **prescription** (personalised study plan + per-track readiness scores), **Interview Loop**, focus mode, coaching debrief, deep trend/dimension analytics, and unlimited mock volume. The split is now **Pro = diagnosis (your weak spots, drill them); Elite = prescription + deep simulation.**
**Affects:** `frontend/src/pages/ProgressDashboard.js` (Where-to-focus gating), `frontend/src/App.css` (removed `.db-weak-locked*`), `docs/features/pricing.md`, `docs/features/dashboard.md`, `docs/tier-wise-features.html` (new at-a-glance tier + unlock reference).

## 2026-06-13 — Pricing: silent currency (detect-only, no toggle) + USD anchor raised to $15/$25/$149/$249; India held
**Area:** pricing · frontend · **Status:** accepted
**Decision:** (1) Removed the user-facing currency `CurrencyToggle` and its localStorage layer — currency is now **detected silently** from the browser timezone (`detectCurrency()`: India → INR, else → USD), with no selector and no indicator. (2) Raised the **USD** standing anchor: Pro $12→$15/mo, Elite $22→$25/mo, Lifetime Pro $129→$149, Lifetime Elite $229→$249. (3) **India held** unchanged (₹999/₹1,999/₹11,999/₹19,999), deliberately, on home-market price-sensitivity.
**Rejected:** (a) **Visible currency selector** (the 2026-06-13 dual-rail entry's "explicit + persisted selector") — surfacing INR signals the India/Razorpay origin and becomes an arbitrage vector once INR < USD, which this bump creates (US Pro $15 vs India ~$12); silent detection removes both. (b) **Bumping India to USD parity** — would dent conversion in the most price-sensitive market. (c) **Keeping the low $12/$22 list price** — under-signals a reasoning-premium product; the anchor is set correctly now (pre-launch, no one to grandfather), with early adoption driven by a separate time-boxed founder's promo, not a permanently low price.
**Refines:** the currency-selector aspect of the 2026-06-13 "Global payments dual-rail" entry (selector → silent detection); the dual-rail Razorpay+Paddle core stands.
**Affects:** frontend `utils/currency.js`, `components/UpgradeButton.js`, `pages/LandingPage.js`, `App.css`, `pages/LandingPageTiers.test.js`; backend `config.py` (USD lifetime amounts); docs `features/pricing.md`, `frontend.md`, `deployment.md`. External (not code): Razorpay dashboard monthly plans + Paddle catalog prices + prod env USD lifetime amounts.

## 2026-06-13 — Dark accent toned one notch off the neon (`#4ADE80` → `#43D27C`); keep the two-tier
**Area:** frontend · brand · **Status:** accepted
**Decision:** The bright dark accent `#4ADE80` read as overpowering — fine on a Mac, too hot on mobile/OLED — and made the deep `#1C8A4F` action button look dull beside it. Toned the dark `--accent` to a **bright emerald `#43D27C`** (one notch off the neon; `--accent-strong #5BDD90`, soft tints + hardcoded IDE greens swept to `rgba(67,210,124,…)`). Buttons are **unchanged** — deep `#1C8A4F` + white. Net: vibrant-but-calmer accent that sits closer to the button, so the button no longer looks dull. Light mode untouched.
**Rejected:** **collapsing dark to a single green** (the "light mode is one gorgeous green, why can't dark be" goal). Prototyped `--accent #2EAE66` used for *both* accent and the CTA fill (dark-ink text). Killed it: (a) the muted green read **too dull**, and (b) on a dark background no single green can serve both roles — white text needs a *deep* green (which is dull as text/icons, ~3.9:1), bright green needs *ink* text. White-text buttons therefore **require** a separate deep button → the two-tier is the physically correct dark structure, not a flaw. Light mode gets one green for free only because its near-white surface lets the deep green do both jobs.
**Affects:** `frontend/src/App.css` (dark `--accent` family + hardcoded IDE greens); the brand SVG sources (`favicon.svg`, `og-image.svg`, `icon-maskable.svg`, `branding/mark-reverse-no-bg.svg`, `branding/lockup-bar-reverse-no-bg.svg`) + their rendered PNG favicons / app icons / OG (via `frontend/scripts/render-brand-assets.mjs`); `docs/design/color-palette.md`; `docs/frontend.md`. **Logo/brand alignment:** done in the same effort — the dark mark, adaptive favicon, OG card, and app icons were re-colored `#4ADE80` → `#43D27C` so the mark matches the toned UI accent (no longer a pending iteration).

## 2026-06-13 — Global payments: dual-rail Razorpay (India) + Paddle (Merchant of Record, rest of world)
**Area:** pricing · ops · **Status:** accepted
**Decision:** Serve each market on the rail built for it. INR → Razorpay (India: UPI/cards, clean per-transaction eFIRC). Every other currency → **Paddle as Merchant of Record**: Paddle is the legal seller and collects + remits global VAT/GST/sales tax, so we never operate worldwide tax compliance. A persisted frontend currency selector drives the rail; `UpgradeButton` branches on `railForCurrency`. Both rails feed one `payment_events` log — Paddle events stored `paddle:`-prefixed with a new `provider` column so ids never collide. Upgrade-path matrix extracted to shared `plan_policy.py`. No PPP at launch.
**Rejected:** (a) **Extend Razorpay internationally only** — its monthly-USD path already 503s, intl cards settle INR with FX, and it does nothing for EU/UK/US consumption-tax liability (the unmanageable part for a small team). (b) **Stripe + Stripe Tax** — cheaper headline fee but Stripe is a PSP, not MoR: *we'd* register + remit tax in every jurisdiction; the ~5%+ MoR fee buys exactly that away. (c) **PPP/location pricing at launch** — adds arbitrage + complexity; revisit as a growth lever. Residual cost (per-payout-not-per-sale FIRC on the Paddle leg + ~7–8% all-in) is contained to the global slice by design.
**Affects:** backend `routers/paddle.py` (new), `plan_policy.py` (new), `config.py`, `models.py`, `db.py` (+`provider`), `main.py`, `alembic/versions/20260612_000001_*`, `tests/test_15_paddle.py`; frontend `components/UpgradeButton.js`, `utils/currency.js`, `pages/LandingPage.js`, `App.css`; docs `features/pricing.md`, `backend.md`, `frontend.md`, `deployment.md`, `CLAUDE.md`

## 2026-06-12 — Dark mode → charcoal: bright brand green accent + deep-green action button (replaces the mistaken neutral-charcoal commits)
**Area:** frontend · brand · design · **Status:** accepted
**Decision:** Dark mode reads "too green/neon" because the brand hue was in the *environment* (green-tinted surfaces + text + accent all at once). Fix: near-neutral **charcoal** surfaces (`--bg-page #121315`, cards `#1A1B1E`, cool-gray text) so the brand green reads as the one **accent** that pops, not the whole UI. (1) **Accent stays the bright brand green `#4ADE80`** (hover `#6EF09A`) — a brief de-neon experiment to a muted emerald `#42BE87` read *too muted* on charcoal. (2) **Primary button = deep "action green" `#1C8A4F` + white** (white-on-`#4ADE80` fails contrast ~1.7:1) across `.btn-primary` + the sibling CTAs (auth submit / account save / path-next / landing paths) + `UpgradeButton` tiers; Elite keeps the green→teal gradient, Pro is solid deep green. Net is a **two-tier green system**: bright `#4ADE80` accents, deep `#1C8A4F` action button. (3) **The always-dark code editor stays forest-green** in both themes — a deliberate brand note in an otherwise monochrome question page (we explicitly did *not* adopt a neutral-charcoal editor). Light mode is untouched (the brand's dominant face). Verified charcoal + popping green on landing/dashboard/practice/auth/mock; light unchanged.
**Rejected:** (a) **green-tinted "Calm" surfaces** (keep surfaces gently green, de-neon the accent) — reads as a bold green *environment*, fighting the calm a 30–90 min study session needs. (b) **de-neoned muted-emerald accent `#42BE87`** — tried, too muted on charcoal; bright green pops hardest on a neutral field. (c) **neutral-charcoal `#1A1A1A` Monaco editor** (harmonise the editor with the UI) — removes the one piece of brand colour from the question page; the forest editor's two-tone split is a feature, not noise. (d) **white-on-`#4ADE80` primary button** — fails contrast; the deep action green is the readable, calmer alternative.
**Affects:** `frontend/src/App.css` (root `[data-theme="dark"]` tokens + `.btn-primary`/sibling dark rules + mock-only accents); `docs/design/color-palette.md` (Dark-mode table + rationale); `docs/frontend.md` (§Design system token table + primary-button note)
**Note:** Replaces two dark-mode commits (`28f1145` neutral-charcoal, `fc25123` landing/mock polish) that were committed to `main` by mistake and removed via history rewrite; this is the agreed final design.

## 2026-06-11 — Readiness mock component: confidence-weighted accuracy over engaged sessions; fixed recency bug; re-keyed mock_limited on volume
**Area:** Dashboard / Elite readiness — mock component · **Status:** accepted
**Decision:** Mock performance is now confidence-weighted accuracy over *engaged* sessions (`attempted_count ≥ 1`): per-session score = `solved_count / attempted_count`; most-recent 5 engaged sessions averaged; result × 30 × `min(n_engaged / 4, 1)`. Abandoned/timed-out/seed sessions (0 attempts) excluded. `mock_limited` re-keyed on engaged-mock volume: `(coverage + quality) ≥ 50 and n_engaged < 4 and total < 80` (practice-strong but not enough timed reps to trust the mock signal). Also fixed a recency bug (was averaging the 5 oldest of the most-recent-20 window instead of the 5 most recent). `db.get_mock_history` now returns `attempted_count` per session.
**Why:** Flat solve-rate let one abandoned `0/n` session tank the average and one lucky session inflate it. Engaged-only + accuracy-over-attempted measures real correctness; the confidence factor ensures mock is only decisive when there's a genuine track record, preventing noise-driven top-band scores. `mock_limited` previously flagged `mock < 10` (score-based), which mislabelled frequent low-scorers as "just needing more reps" — re-keying on `n_engaged < 4` correctly identifies the volume-constrained case.
**Rejected:** (a) raising the flat mock weight above 30 — over-weights a small-sample signal; confidence-gating is the principled lever. (b) changing the analytics `avg_score_pct` to accuracy-over-attempted for parity — deferred; the analytics headline legitimately measures session outcome (solved/total including gaps), and changing a user-facing historical metric is out of scope here.
**Affects:** `backend/routers/insights.py` (`_compute_readiness_scores`, `mock_limited`); `backend/db.py` (`get_mock_history` — adds `attempted_count`); `docs/features/dashboard.md` (§ Interview readiness score — Mock performance row + `mock_limited` paragraph + new note on metric distinction)
**Supersedes:** the mock-pts calculation in the 2026-06-11 "Replace Elite readiness formula" entry (that entry established Coverage/Quality/Mock; this one tightens the Mock component specifically)

## 2026-06-11 — Replace Elite readiness formula: Coverage 45 + Solve quality 25 + Mock performance 30 (monotonic, practice cap 70)
**Area:** dashboard · product · **Status:** accepted
**Decision:** Replaced the three-component readiness formula (Practice coverage 40 / Mock accuracy 35 / Concept strength 25) with a new monotonic model: Coverage 45 (difficulty-weighted distinct solves) + Solve quality 25 (first-time-correct rate) + Mock performance 30 (avg solve-rate last 5 sessions). Practice-only ceiling is 70 ("Getting there"); reaching "Interview ready" (80+) or "Strong" (90+) requires mock evidence. Solve time excluded. Labels unchanged. Each track object now includes `mock_limited` (bool) to flag practice-strong/mock-poor tracks for the UI. Docs-follow-product change — the backend formula was recalibrated first; this entry records the rationale.
**Rejected:** (a) **scoring solve time** — contradicts reasoning-over-speed positioning; time pressure is mock's job, not a factor in a readiness index; also noisy across question difficulty. (b) **keep concept strength but remove the 1.5× clamp** — a first-time-correct quality signal is a cleaner mastery proxy than a ratio of tagged-concept accuracy buckets; concepts remain the diagnostic layer in "Where to focus". (c) **make mock fully optional (practice alone reaches "Strong")** — dishonest readiness signal: a user who has only drilled practice questions has not demonstrated timed performance, which is what interviews test.
**Why the old model failed:** The `max(0, strong − 1.5×weak)/8` concept-strength term returned 0 for any user below ~70% accuracy on 8+ concepts and *decreased* when they engaged new (weak) material — so the score could drop as users learned. Mock (35%) was effectively a hard gate for the top bands, but practice was also hard-capped at 40. Net: 60 of 100 points were structurally unreachable for a normally-progressing user; a user who had solved 66% of a full track catalog (including hard questions) still showed "Early stage".
**Affects:** `backend/routers/insights.py` (`_compute_readiness_scores`); `docs/features/dashboard.md` (§ Interview readiness score — component table + explanatory text rewritten)

## 2026-06-10 — Warn (don't finalize) before leaving an in-progress mock; beforeunload + popstate, not useBlocker
**Area:** mock · frontend · **Status:** accepted
**Decision:** Pressing the browser Back button during an active mock left silently (and Forward resumed, since sessions are server-persisted) — no heads-up. Added a leave-guard in `MockSession`, active only while `status==='active'`: `beforeunload` (native dialog for refresh / tab-close / external nav) + a `popstate` "sentinel" trap (push a duplicate history entry; on Back, `window.confirm` "Leave your active mock session? Your timer keeps running — you can resume"). Confirming navigates to `/mock` **without** finalizing (the session stays active/resumable); cancelling re-arms the trap. Verified both branches + the beforeunload prevent.
**Rejected:** (a) **React Router `useBlocker`** — requires a data router (`createBrowserRouter`); the app uses `BrowserRouter`, and migrating the whole router just for this is disproportionate and risky (esp. with a parallel session editing the app). (b) **reuse the existing exit-confirm modal** — that modal's `handleExitConfirm` *finalizes/scores* the session; an accidental Back must not end a timed run, so the guard is a non-destructive warning instead. (c) **native `confirm` vs a styled modal** — chose native confirm: robust, zero new components, and standard for leave-guards (beforeunload's dialog is unstyleable anyway); a styled modal is a possible polish upgrade later. **Affects:** frontend/src/pages/MockSession.js
**Also in this commit (bug fix, no separate entry):** the `.shortcut-kbd` "⌘⇧↵" chip on the green primary button was a white box (rgba(255,255,255,…)) — harsh on the dark-mode green; scoped `.btn-primary .shortcut-kbd` to match the button text (white in light, forest-ink in dark). And the Sample code Run/Submit buttons were missing the chip entirely — added `⌘↵`/`⌘⇧↵` to match Practice/Mock. (frontend/src/App.css, SampleQuestionPage.js)

## 2026-06-10 — Light-mode brand assets: adaptive favicon (switches), light OG as a standalone alternate (can't)
**Area:** brand · frontend · **Status:** accepted
**Decision:** Added light-mode variants only where a browser can actually *deliver* one by viewer theme. `favicon.svg` is now **self-adaptive** — one file with an internal `@media (prefers-color-scheme: light)` that flips the chip to white + hairline + deep-green mark (`#166534`/`#4B6858`); chip geometry stays full-bleed so the dark raster is byte-identical. Added light PNG fallbacks (`favicon-light-16/32/48`, rendered by emulating `colorScheme: light` in the render script) wired via `<link … media="(prefers-color-scheme: light)">` with the dark PNGs kept as the media-less universal fallback, and split `theme-color` into dark `#0D1A10` / light `#F5F7F4`. Also produced `og-image-light.png` as a **standalone** light-ground card for light-bg placements (emails/docs).
**Rejected:** (a) **a light OG that swaps on shares** — impossible: `og:image` resolves to one fixed image per URL with no per-viewer theme, so the dark card stays canonical (no `?v` bump, not wired into og:image/Helmet/spa.py); the light OG is a repo asset only. (b) **light app/PWA icons** — apple-touch / manifest icons have no per-theme delivery mechanism, so a light version would be an unused file; the dark chip already reads on any home screen. (c) **two separate favicon SVG sources** (dark + light) — the adaptive single SVG is the source of truth; the render script emulates the scheme per target to derive both PNG sets, no duplication.
**Affects:** frontend/public/{favicon.svg (adaptive), favicon-light-16/32/48.png, og-image-light.svg/png}, frontend/scripts/render-brand-assets.mjs (per-target colorScheme), frontend/index.html (media links + theme-color split), docs/frontend.md

## 2026-06-10 — Coaching "Drill" CTAs drill the concept (new `/api/practice/drill`); path demoted to honest secondary
**Area:** frontend · backend · gating · product · **Status:** accepted
**Decision:** The dashboard / landing / mock-post-mortem "Drill weak concept" CTAs were **path-first** (or deep-linked a single question / the bare hub), so *Drill X* opened a path titled for a *different* concept, or dropped you in the full catalog where **Next** left the concept entirely. Built a real concept drill: a Pro+ `GET /api/practice/drill?track=&concept=` (family-aware via `concept_matches_focus`, **unsolved-first** ordering) feeds a `?drill=` context on `QuestionPage` that mirrors the `?path=` nav bar — a scoped *Next in drill* that stays on the concept until done. Every coaching *Drill* CTA now leads with this (`/practice/{track}?drill={concept}`); the matching path is offered only as an **honest secondary** (*Or take the … path →*). Fixed `AppShell` dropping `?drill=` on the hub→question redirect (the literal "drill dumps you into the catalog" bug). Gated the logged-in landing weak-spots to Pro+ (it leaked weak concepts to free users; the dashboard already gated them) and deleted the dead, never-mounted `InsightStrip`.
**Rejected:** (a) just make the existing `?concepts=` sidebar filter scope Next — it only filters the visual list and AppShell dropped the param first; a real scoped walk is the fix. (b) route the coaching drill through the mock **custom-drill** (`focus_concepts`) machinery — that's a timed Pro/Elite *simulation*, the wrong vehicle for a low-friction practice walk. (c) a free drill *mechanic* — operator confirmed the weak-concept drill is and stays a Pro+ feature; the free-visible landing cards were a gating leak, not an entitlement.
**Deferred (not built):** a generic "drill this concept" from concept pills as a free acquisition funnel; practice-solves-based "weak-pattern paths" (flagging which solved path tiles you actually struggled on) — both future; mock debrief/recommendation stays strictly Pro+.
**Affects:** backend/routers/practice.py (new) + main.py + routers/insights.py (`study_plan`/debrief → `?drill=`); frontend QuestionPage.js, AppShell.js, ProgressDashboard.js, LandingPage.js, MockSession.js, App.css; deleted components/InsightStrip.js; docs/features/dashboard.md, docs/frontend.md, docs/backend.md, docs/features/pricing.md; tests test_practice_drill.py + Dashboard/Landing vitest.
**Supersedes:** none — path-first coaching was undocumented drift. Distinct from 2026-06-08 "Landing learning-paths section funnels to /learn" (that governs the logged-out *marketing* paths section, not the personalized coaching drill).

## 2026-06-10 — OG card advertises curation + the mock-exclusive bank; stat row drops "instant feedback"
**Area:** brand · product · **Status:** accepted
**Decision:** Evolved the social-card stat row from "9 tracks · 850+ questions · instant feedback" to **"9 tracks · 850+ curated questions · 1,000+ mock-exclusive"** (user-approved). "Curated" carries the quality/reasoning-depth signal (the "a curriculum, not a question bank" positioning) instead of leaving 850+ to read as raw volume; "1,000+ mock-exclusive" surfaces the Pro/Elite mock-only bank (verified 1,102) as a premium hook on the public card — same durable round-number framing the landing tracks-editorial already uses. Dropped "instant feedback" (the most generic of the four) for room. Re-rendered `og-image.png` and bumped the cache-bust `?v=2 → ?v=3` everywhere.
**Rejected:** (a) **keep "instant feedback"** and cram four stats — the line gets long/cramped at 22px; the curation + premium-bank signals out-rank a generic feedback claim. (b) **"mock-only"** (matches existing site copy) over "mock-exclusive" — operator chose the punchier card wording; the landing page still says "mock-only", a tolerable cross-surface variance. (c) **revert the Sonnet agent's `playwright`→prod-deps + `@playwright/test` bump** — playwright is a build-only asset-render tool, never a runtime dep; the rendered PNG ships as a file, so package.json was restored.
**Affects:** frontend/public/og-image.svg + og-image.png (re-render), frontend/index.html, backend/routers/spa.py, frontend/src/pages/{LandingPage,LearningPath,SampleQuestionPage,TrackHubPage,LearningPathsIndex}.js (?v=3), docs/frontend.md

## 2026-06-10 — Grow the follow-up taxonomy to 8 + accept `_pivot`-less aliases; don't re-author good content (audit E1)
**Area:** content · taxonomy · mock · **Status:** accepted
**Decision:** The mock chain bank had drifted off the documented 7 follow-up dimensions: 17 Data Engineering children used the `_pivot`-less spelling of canonical dimensions, and 2 Statistics children used `abstraction_pivot` (not in the 7). Read the actual questions — **all are high-quality, genuine pivots**. So (per "docs serve the product"): (1) **promoted `abstraction_pivot` to a canonical 8th dimension** (generalise-from-instance / reframe-under-a-different-lens — a reasoning move the 7 can't label), with a reasoning-depth defense in `concept-taxonomy.md` + label/blurb in the frontend map; (2) **accept the `_pivot`-less spellings as aliases** normalised by a new `backend/follow_up_dimensions.py` `canonical_dimension()` (used by the analytics keying so `data_quality` and `data_quality_pivot` count as one — fixing the per-dimension stat split — and by the validator); (3) **made `validate_content.py` actually enforce** canonical-membership + consecutive-differ (the doc claimed enforcement that didn't exist). **No question JSON was edited.**
**Rejected:** (a) **re-author the 17 DE tokens** to the `_pivot` form via the authoring agent — pure-notation churn on good content for no behavioural gain; aliasing in code is cheaper and the frontend already humanises both. (b) **remap `abstraction_pivot` into one of the 7** — none fit (it changes the candidate's *level of abstraction*, not an external property of the problem); forcing a fit would mislabel strong questions. (c) **leave the validator claims aspirational** — they actively lied, which is how the drift went unnoticed.
**Affects:** backend/follow_up_dimensions.py (new), backend/routers/mock.py (analytics canonicalisation), backend/scripts/validate_content.py (+ tests), frontend/src/mockModeConfig.js; docs/concept-taxonomy.md (7→8 + abstraction entry + rules), docs/features/mock.md, docs/specs/mock-benchmark-spec.md; the "docs serve the product" rule added to CLAUDE.md + docs/content-authoring.md; docs/mock-audit-tracker.md (E1)

## 2026-06-10 — Refresh all favicons/icons/OG onto the Forest & Ink mark; durable OG copy; versioned share URL
**Area:** frontend · brand · **Status:** accepted
**Decision:** The favicon ("dt" glyph, old indigo `#5B6AF0`) and the OG card (white "D" + indigo bar, "350+ questions · 22 learning paths · SQL·Python·Pandas·PySpark") both predated the Forest & Ink rebrand and the 9-track footprint — so shares showed a stale, off-theme card. Rebuilt the entire icon system on the canonical **two-diagonal-squares mark** + forest-ink ground `#0D1A10` (mark `#4ADE80`/`#87B09A`), matching the in-app Topbar logo: new `favicon.svg` (rounded chip) + `icon-maskable.svg` (full-bleed) source SVGs, rasterized by a committed Playwright script (`frontend/scripts/render-brand-assets.mjs`) to favicon-16/32/48, apple-touch-icon (180), icon-192/512, and a fresh `og-image.png`. Added `site.webmanifest` + full `<link>`/`theme-color` wiring (none existed before). OG copy is **durable** — "9 tracks · 850+ questions · instant feedback" + the landing H1 — instead of exact counts that drift.
**Rejected:** (a) **just bump the numbers** (350→878, 22→96) in the old indigo art — re-introduces the same staleness next release and keeps the off-theme indigo "D"; the durable round-number framing + theme-correct mark is the root fix. (b) **a dark chip vs. transparent mark for the favicon** — the in-app mark is transparent-on-page, but a tab/app icon needs its own ground; a forest-ink chip guarantees legibility on any browser chrome and gives apple-touch/PWA the required opaque field. (c) **rename the OG file to bust caches** — instead versioned the URL (`og-image.png?v=2`) across index.html, spa.py injection, and all Helmet pages, keeping one canonical filename; share platforms (WhatsApp/FB) still re-scrape on their own TTL, so existing cached previews update only after that or a re-share.
**Affects:** frontend/public/{favicon.svg,icon-maskable.svg,og-image.svg,+7 PNGs,site.webmanifest}, frontend/scripts/render-brand-assets.mjs, frontend/index.html, backend/routers/spa.py, frontend/src/pages/{LandingPage,LearningPath,SampleQuestionPage,TrackHubPage,LearningPathsIndex}.js, docs/frontend.md, docs/design/color-palette.md

## 2026-06-10 — Cap penalty-free mock discards at 3/day; block (don't count) when exceeded
**Area:** mock · gating · **Status:** accepted
**Decision:** A discard within the 2-min window hard-deletes the session, which refunds the rate-limit quota — so an uncapped discard let a user start→discard→repeat to re-roll/preview question sets without spending quota (audit C4). Capped penalty-free discards at `MAX_PENALTY_FREE_DISCARDS_PER_DAY = 3` per UTC day, tracked in a new append-only `mock_discards` table (migration `20260610_000002`, applied to prod). Beyond the cap the discard returns **429 and the session is left active** — the user continues or ends it normally (it then counts as used); a blocked discard is **not** itself counted against them.
**Rejected:** (a) **tombstone — count the discard toward the cap** — punishes the legitimate misclick/wrong-track recovery the discard window exists for; we keep recovery free and only stop the *re-roll* loop. (b) **server "no-activity" check** — runs (`/run-query`/`/run-code`) are ephemeral and not persisted against the session, so the server can't detect "previewed then discarded"; ineffective. (c) **accept + document only** — the operator chose a clean fix now over watching telemetry. 3/day leaves ample room for real misclicks while killing the abuse (after 3, the next start is locked in).
**Affects:** `backend/routers/mock.py` (cap + 429), `backend/db.py` (`mock_discards` + `get_daily_discard_usage`/`record_discard`), `backend/alembic/versions/20260610_000002_*`, `frontend/src/pages/MockSession.js` (429 keeps session active), `backend/tests/test_11_mock.py`, `docs/features/mock.md`, `docs/deployment.md`, `docs/mock-audit-tracker.md` (C4)

## 2026-06-10 — Schema lives in two places; both must be updated on every migration
**Area:** architecture · process · **Status:** accepted
**Decision:** The `_SCHEMA_SQL` string in `backend/db.py` and the Alembic migration chain are independent, never auto-synced. `_SCHEMA_SQL` is the source for local-dev startup (`ensure_schema()`) and the test suite (`ensure_schema_admin()` in `conftest.py`); Alembic is the source for production. Writing a migration without updating `_SCHEMA_SQL` causes tests to fail with `column does not exist` while production is unaffected — the exact failure mode of the 2026-06-10 `plan_override` bug. The rule is now explicit in `CLAUDE.md` § migration checklist (step 2), `docs/deployment.md` § Two-track schema, and `docs/architecture.md` § PostgreSQL schema.
**Rejected:** (a) **making tests run Alembic instead of `_SCHEMA_SQL`** — heavier test setup, slower, more fragile on connection/env config; `_SCHEMA_SQL` + `IF NOT EXISTS` guards is the right fast path for the test harness. (b) **removing `_SCHEMA_SQL` and always calling Alembic** — would force every local dev startup to run Alembic, breaking the clean "app starts itself" model and making local setup more complex. (c) **adding a CI diff-check** — useful future hardening but the immediate fix is the documented discipline.
**Affects:** `CLAUDE.md` (step 2 in migration checklist), `docs/deployment.md` (§ Two-track schema), `docs/architecture.md` (§ PostgreSQL schema intro)

## 2026-06-10 — Admin grant system for beta/trial access (operator-only, lazy expiry, no cron)
**Area:** ops · gating · **Status:** accepted
**Decision:** Added `POST/DELETE /api/admin/grant-plan` (Bearer-token–protected) so the platform operator can give specific users time-limited Pro/Elite access for beta testing or invited cohorts. Override stored as `plan_override` + `plan_override_until` on `users`; resolved lazily via `_effective_plan()` at every auth point — no cron, no background task, expiry is per-request. Migration `20260610_000001` applied to prod immediately.
**Rejected:** (a) **coupon codes / user self-redemption** — adds UI surface, session state, abuse surface, and support overhead; this is operator tooling, not a user-facing flow. (b) **background expiry cron** — operational complexity with zero correctness benefit; lazy evaluation at auth time is correct because every protected route reads the session user through `_effective_plan()`. (c) **manual DB edits** — no audit trail, error-prone, not reproducible.
**Affects:** `backend/db.py` (`_effective_plan`, all user-returning queries), `backend/routers/admin.py`, `backend/config.py` (`ADMIN_SECRET`), `backend/alembic/versions/20260610_000001_*`, `docs/backend.md`, `docs/architecture.md`, `docs/features/pricing.md`, `docs/deployment.md`, `CLAUDE.md`

## 2026-06-09 — Drop the "In progress" rail from the /learn index; the per-tile progress bar is enough
**Area:** frontend · product · **Status:** accepted
**Decision:** `LearningPathsIndex` (`/learn` and `/learn/:topic`) rendered an "In progress" rail at the top listing **every** in-progress path, then re-listed those same paths inside their per-track grouped grids below — so each in-progress path appeared twice, and the page filled with duplicates for any user who had touched several paths. Removed the rail (computation + render + `.learn-index-progress-rail` CSS). Each `PathProgressCard` tile already carries its own progress bar, so the grouped grids are a sufficient single source.
**Rejected:** (a) **cap the rail to top-3** (like the home's `ContinuePathsSection`) — would fix the duplication but still duplicate three tiles, and the capped fast-resume affordance already exists on the logged-in home; a second one on /learn isn't worth the visual cost. (b) **suppress in-progress paths from the grouped grids** so the rail is the only place they appear — breaks the "every path in its track group" mental model and makes a path vanish from its track the moment you start it.
**Affects:** frontend/src/pages/LearningPathsIndex.js, frontend/src/App.css, docs/frontend.md, CLAUDE.md

## 2026-06-09 — Restore the role lens on the logged-in home + make /learn reachable everywhere
**Area:** frontend · product · **Status:** accepted
**Decision:** The dashboard-lean logged-in `/` had dropped the role lens (logged-out only) and buried `/learn` (reachable solely via a paths-empty-state link). Three fixes: (1) a persistent "All paths →" link in the `ContinuePathsSection` header; (2) a shared-Topbar "Paths" nav link between Practice ▾ and Mock — one change that fixes `/learn` reachability on every page for both auth states; (3) made `YourTracksSection` role-aware via a compact pill selector (All · Data Engineer · Data Analyst · Analytics Engineer · Data Scientist) reusing the existing `ROLES` config, filtering/ordering the grid to `ROLES[i].tracks` with each card's progress bar intact.
**Rejected:** (a) a logged-in-only Paths link — would leave the same reachability gap on Mock/Dashboard/learn pages; the shared Topbar is the right altitude. (b) reusing the heavy `lp-role-tabs` underline tablist for the filter — too loud for a utility "Jump back in" context; chose a calm pill segmented control (`aria-pressed`, not tablist, since it re-renders one grid in place rather than swapping panels). (c) duplicating ROLES into a logged-in-specific array — the lens must stay identical to the logged-out role selector, so it shares one source.
**Affects:** frontend/src/components/Topbar.js, frontend/src/pages/LandingPage.js, frontend/src/App.css, frontend/src/pages/LandingPageTiers.test.js

## 2026-06-09 — Collapse the Pandas track's two legacy aliases into one canonical `pandas`
**Area:** content · architecture · frontend · **Status:** accepted
**Decision:** The Pandas track carried two legacy aliases — the frontend slug `python-data` and the backend db-topic/module/dir `python_data` — and the mismatch was a **live bug**: the paths router read solves under `"python-data"` while the practice router wrote `"python_data"`, so Pandas learning-path progress always showed 0 even when the user had solved Pandas practice questions. Standardized on a single canonical token **`pandas`** everywhere (frontend slug + apiPrefix, backend slug/db_topic/router-prefix, `git mv` of the module/router/content-dir, path/sample JSON topics, validator + registries, docs), removed the dashboard's `_remap` alias hack, and migrated every existing DB row — `user_progress`/`user_sample_seen.topic` + `submissions`/`mock_sessions`/`mock_session_questions.track` — `python_data`/`python-data` → `pandas` (Alembic `20260609_000001`, applied to prod). Old URLs keep working via back-compat redirects.
**Rejected:** (a) **Patch only the `_TOPIC_DB` one-liner** (the minimal fix) — leaves the dual-alias trap to bite the next reader; chose the clean root fix. (b) **"No python-data anywhere" incl. dropping the redirects** — would break existing bookmarks/SEO links; the redirect route strings are the one deliberate place the old slug survives (plus the migration's WHERE clause + append-only history). (c) **Skip the data migration** — would orphan real solves (23 prod `user_progress` rows + 78 submissions) and reset Pandas progress to 0.
**Affects:** ~90 files across frontend/backend/content/docs; `git mv` python_data_questions module+router+`content/python_data_questions/` → pandas; `backend/alembic/versions/20260609_000001_*`; CLAUDE.md; docs/deployment.md (migration log).

## 2026-06-09 — Concept tags are hidden during an active mock session (shown in practice + the post-mortem)
**Area:** mock · frontend · product · **Status:** accepted
**Decision:** The active mock question render used to show the question's concept pills (e.g. "WINDOW FUNCTIONS"). Removed them from the live mock session (`MockSession.js`). A mock is an interview *simulation*: naming the pattern telegraphs the approach and turns recognition into recall — the opposite of what the reasoning-premium product is meant to test. Concept tags remain in **practice mode** (where learning/navigation is the point) and in the mock **post-mortem** concept breakdown (so the learning signal survives, after the user has committed).
**Rejected:** (a) keep them everywhere (the status quo) — undercuts the mock's diagnostic value, especially for weaker users who use the tag as an approach hint. (b) hide them in benchmark/Interview-Loop only but keep in custom drills — inconsistent; the whole mock workspace is the "simulation" surface, so one rule (hidden in all mock modes) is cleaner and easier to reason about.
**Affects:** frontend/src/pages/MockSession.js; docs/mock-audit-tracker.md (A5a)

## 2026-06-09 — Company filter is a free practice-only convenience; the mock company filter is deleted, never a tier perk
**Area:** mock · pricing · product · **Status:** accepted
**Decision:** The mock `company_filter` was a phantom — gated to Elite in `compute_mock_access` but never applied to selection, no UI, yet advertised as an Elite perk (landing, `pricing.md`, AppShell upsell, `mock.md`). Deleted it end-to-end (backend field/gate/tests + all advertising + docs). The **practice** SQL company filter (`SidebarNav`, free, all tiers) stays as a light catalog-slice convenience — never gated, never marketed as premium.
**Rejected:** (a) **building** the mock company filter (option 3/1) — a company filter is the canonical grind-market lever (StrataScratch/DataLemur framing) that directly contradicts the reasoning-premium North Star; the per-company pool is too thin to survive mock's no-repeat freshness model; and it's SQL-only so it doesn't generalise across the 9 tracks. (b) **dropping it from practice too** (option 4) — the practice filter is a useful, harmless free convenience that never claims to be the value prop; removing it would discard a working feature and the `companies` data for no gain. (c) **extending it to Pro** — the tier question dissolves: it's free in practice and absent in mock.
**Affects:** backend/unlock.py, backend/routers/mock.py, backend/tests/test_11_mock.py; frontend/src/pages/LandingPage.js, frontend/src/components/AppShell.js; docs/features/mock.md, docs/features/pricing.md, docs/architecture.md, docs/specs/mock-benchmark-spec.md, docs/specs/platform-north-star.md; docs/mock-audit-tracker.md (C1/D1)

## 2026-06-09 — Interview Loop pivot dimension is rendered from a label/blurb map, not the question's `framing` field
**Area:** mock · frontend · **Status:** accepted
**Decision:** The pivot card (and per-dimension analytics) showed the raw `follow_up_dimension` token (e.g. `business_rule_pivot`) with one generic sentence for every pivot, undercutting the Elite Loop's "feels like a real interviewer" promise (audit A3). Added a shared `FOLLOW_UP_DIMENSIONS` map in `mockModeConfig.js` (human label + dimension-specific interviewer blurb for the 7 canonical pivots, `_pivot`-less aliases, and a humanize fallback so no raw token ever renders); pivot card uses label-heading + blurb, post-mortem marks follow-ups with a "↩ Follow-up · {label}" chip (needed C5: persist `is_follow_up`), analytics use the label. Reconciled `docs/features/mock.md` to match (docs serve the product).
**Rejected:** the prior spec's "render the follow-up's `framing` field" — `framing` holds only the question *type* token ("scenario"), not interviewer narrative, so it can't carry the pivot framing; a curated per-dimension map is the right source. Also rejected putting labels server-side — display concern, kept frontend-only.
**Affects:** frontend/src/mockModeConfig.js, MockSession.js, MockHub.js, App.css; backend/db.py + routers/mock.py (C5); docs/features/mock.md; docs/mock-audit-tracker.md (A3/C5/B2/D6, E1)
**Supersedes:** the `framing`-field pivot-card prescription previously in docs/features/mock.md (no dated decision entry; doc-only).

## 2026-06-09 — Free mock benchmark is gated on `difficulty != "easy"`, not a medium/hard blocklist
**Area:** mock · gating · **Status:** accepted
**Decision:** `compute_mock_access` blocked only `("medium","hard")` for Free benchmarks, so `difficulty="mixed"` (which draws medium/hard) slipped through as `can_start: True` — a Free user could start a mixed benchmark and receive medium questions, bypassing the easy-only spec and the Pro upsell (confirmed firsthand in the mock audit, finding A2). Changed the guard to `difficulty != "easy"` (covers mixed and any future value). Frontend needed no change — `getDifficultyButtonState` derives the blocked pill / Start-disable purely from `/access`.
**Rejected:** enumerating `("medium","hard","mixed")` — same effect today but re-opens the hole the moment a new difficulty is added; the `!= "easy"` invariant is the cleaner, future-proof expression of "Free is easy-only."
**Affects:** backend/unlock.py, backend/tests/test_11_mock.py (TC-129b/c), docs/features/mock.md, docs/mock-audit-tracker.md (A2)

## 2026-06-08 — Practice and Paths are framed as two layouts of one shared question bank
**Area:** frontend · **Status:** accepted
**Decision:** The per-track Practice catalog and Learning Paths were presented adjacently (landing §06 tracks + §07 paths; the track hub's progress card + paths section) with no stated relationship, so they read as competing offerings. Added a consistent cross-surface message — *same questions, two layouts, shared progress (solve once anywhere = done everywhere)* — on three surfaces: a compact "Two ways to work {track}" strip on TrackHubPage (between the stat card and the paths section), a connective clause in the landing PathsShowcase sub-copy, and a clarified `/learn` header + "Browse the full catalog →" back-pointer. Canonical caption: "Same questions — solve one in either place and it's marked done in both. You never redo a question."
**Rejected:** (a) **Leave them unframed** — users couldn't tell the two surfaces weren't separate question sets / double work. (b) **Surface the orphan/coverage gap** ("paths don't cover everything") — negative and misleading: orphans are redundant-concept reps, so a paths-only user loses no concept; the framing stays positive (paths = the ordered route, catalog = the full set). (c) **A verbose explainer on every surface** — visual noise; the message lives as one compact strip at the in-track decision point plus light one-line touches on the marketing/index surfaces.
**Affects:** frontend/src/pages/{TrackHubPage,LandingPage,LearningPathsIndex}.js, frontend/src/App.css (`.trackhub-twoways*`), docs/frontend.md (§Practice ↔ Paths messaging).

## 2026-06-08 — Path levels are content-driven; ≥1 foundational (reverses singleton-foundational)
**Area:** content · taxonomy · **Status:** accepted
**Decision:** A learning path's `level` now follows its **actual content** — question difficulty + prerequisite position + concept role — not a fixed "1 foundational + a few intermediate/advanced" template. Per-level counts are whatever the content warrants; the validator now enforces **≥1** foundational per track (was *exactly one*), so a track may have 2–3 entry points. Audited all 86 paths (difficulty-score + concept role): re-leveled 20 (e.g. SQL window-functions int→adv, sql-string-and-date adv→found; Stats CI/errors→adv, probability→found), absorbed 28 of 64 orphans into the path they belong to (skipping trivial/redundant ones with a named near-equivalent), split the 4 worst >15 paths along clean fault lines, and added 5 compact paths for genuinely uncovered concept gaps (survival analysis, algorithmic fairness, ANOVA, lakehouse, SQL data-quality). `display_order` re-sequenced so each track's established entry point leads. 86→96 paths; no question authored or edited (path config only); no question in two paths.
**Rejected:** (a) **Keep the singleton-foundational template** — it forced hard-dominant paths to read "intermediate" and buried all-easy base topics under one arbitrary entry; the label should describe the content, not a quota. (b) **Pad the new compact paths to the ideal 6–12 with mock-only questions** (an auditor's first instinct) — mock questions never appear in the practice catalog, so they can't live in a practice-path; the paths stay at their honest 3–4 practice-Q size, flagged compact, to grow when practice content is added. (c) **Split every >15 path** — only the 4 with clean fault lines were split; the other 4 (16–17, no clean line) were left flagged to bound scope. (d) **Leave orphans out** — violates "absorb unless a near-equivalent exists"; only trivial SQL SELECT/WHERE basics + named-redundant ones were left out.
**Affects:** backend/content/paths/*.json (96), backend/scripts/validate_content.py (Rule 2), CLAUDE.md, docs/content-authoring.md §Paths, docs/backend.md, docs/frontend.md.

## 2026-06-08 — Hard-refuse email sends to RFC-reserved / undeliverable domains
**Area:** ops · security · **Status:** accepted
**Decision:** `email_service` now refuses (before any Resend call) to send to RFC 2606/6761 reserved TLDs (`.test/.example/.invalid/.localhost/.local`), `example.{com,net,org}`, and malformed addresses, across all three senders. Root cause: the load-test harness registers `load-*@internal.test` VUs against `/register`, and a backend holding the **production** `RESEND_API_KEY` (from `backend/.env`) fired ~150 real sends in a day to an undeliverable `.test` domain — burning Resend daily quota and, more seriously, generating hard bounces that damage sender reputation. Prod itself was clean (5 real users, 6 verification emails ever); the sends came from local load runs against localhost.
**Rejected:** *Rely only on running the harness with `RESEND_API_KEY=` unset* — operational, easy to forget, and leaves every other accidental fake-domain send (typos, future tooling) uncaught. We did both: the README now mandates the empty key (primary guard), and the domain block is the durable backstop. *Stop carrying the prod Resend key in local `.env`* (use a Resend test key) is the deeper fix and still worth doing, but it's a larger operational change and doesn't protect prod from a typo'd reserved domain.
**Affects:** `backend/email_service.py`, `backend/tests/test_email_reserved_domains.py`, `backend/loadtest/README.md`, `docs/backend.md` § auth security controls

## 2026-06-08 — Wire FORWARDED_ALLOW_IPS into the Dockerfile (ready the proxy-IP rate-limit fix, default unchanged)
**Area:** ops · security · **Status:** accepted
**Decision:** The prod Dockerfile now starts uvicorn with `--forwarded-allow-ips "${FORWARDED_ALLOW_IPS:-127.0.0.1}"`. This readies the fix for the P2.2 proxy-IP keying finding (the per-IP rate limiter keys on `request.client.host`, which only reflects the real client IP if uvicorn trusts `X-Forwarded-For` from the immediate peer): if prod `client_ip=` logs show all clients collapsing into one proxy-IP bucket, the fix is now a **single env-var set** (`FORWARDED_ALLOW_IPS=<verified Railway hop>`) with no image change. The default `127.0.0.1` reproduces uvicorn's built-in default, so runtime behaviour is **unchanged** until the env var is set.
**Rejected:** (a) **Setting `FORWARDED_ALLOW_IPS="*"` now** — trusts XFF from any peer → clients spoof their IP to evade the limiter or frame another address; the value must be the specific proxy hop, verified first. (b) **Applying a concrete hop value now** — the real prod peer isn't verified yet (needs a prod-log check of `client_ip=`); shipping a guessed value could silently break keying. (c) **Leaving it unwired** (P2.2 state) — the fix would require an image/CMD edit under time pressure; wiring the env now makes it a safe one-flip once verified. (d) **Keying the limiter on XFF in app code instead** — duplicates what uvicorn's `--forwarded-allow-ips` does correctly and securely; no reason to reimplement.
**Affects:** Dockerfile, docs/deployment.md (§ Environment variables + § Rate-limiter operational notes & findings).

## 2026-06-08 — Land worktree tasks via fast-forward to main + push, automated by scripts/land.sh
**Area:** process · **Status:** accepted
**Decision:** A coding task runs in its own worktree; to integrate, run `scripts/land.sh <task-branch>` from the **main** worktree. It syncs local `main` with `origin/main` (ff-only), **fast-forwards `main` to the task branch's commits** (linear history, no merge bubble; merging reads the branch without checking it out, so it works while the branch is still checked out in its worktree), pushes, then removes the worktree + branch. If `main` advanced and ff isn't possible it refuses and prints the rebase-onto-`origin/main` command to run in the task worktree, then you re-run — never a half-merge. One command = the whole close-out. Invariant: `main` ≡ `origin/main`, no stranded work, no leftover worktrees.
**Rejected:** (a) **`--no-ff` merge commit per task** — adds a history bubble per task; the user wanted clean linear landing, not per-task landmarks. (b) **ff-only with no rebase fallback** — silently impossible once `main` advances; the rebase-then-retry path (run in the task worktree, where the branch is checked out) handles that cleanly. (c) **Squash** — drops the individual commits the user wants preserved. (d) **Manual multi-command close-out** — error-prone and the original source of stranded work; collapsing it into one `land` command is the low-overhead cure. (e) **Running `land` from the task worktree** — a worktree can't cleanly remove itself, and `main` is checked out elsewhere; running from the main worktree sidesteps both.
**Affects:** scripts/land.sh (new), CLAUDE.md (Standing instructions — git workflow), user memory (`worktree_workflow.md`).
**Supersedes:** 2026-06-08 — Per-task git worktrees, merged to main + pushed + deleted.

## 2026-06-08 — Rate-limiter graceful degradation + proxy-keying finding (concurrency P2.2)
**Area:** ops · architecture · **Status:** accepted
**Decision:** Added `BaseRateLimiter.check_safe(key, *, fail_open)` so a transient Redis error never becomes a 500: the coarse per-IP limiter and the auth *baseline* limiter fail **open** (allow + WARN), the token-issue limiter fails **closed** (deny + short Retry-After). Confirmed (via new `loadtest/ratelimit.py`, source-binding a non-loopback client past the dev localhost exemption) that the 60/min/IP limiter degrades gracefully under burst (429 + Retry-After + X-RateLimit-* + canonical error). Confirmed the in-memory fallback is single-replica-only and already enforced (`create_rate_limiter` raises in prod without Redis). **Surfaced (not auto-fixed):** uvicorn is started without `--forwarded-allow-ips`, so it trusts `X-Forwarded-For` only from a `127.0.0.1` peer (empirically demonstrated) — per-IP keying is correct only if Railway's proxy reaches the container from `127.0.0.1`, else all clients share one proxy-IP bucket.
**Rejected:** (a) **Blanket fail-open for all limiters** — fail-open on the token-issue limiter would let a Redis blip uncork unbounded magic-link/verification emails (the current 500 actually *blocked* the send); per-limiter policy is the right granularity. (b) **Fail-closed everywhere** — a Redis blip would 503/deny every page; unacceptable for the coarse availability guard. (c) **Setting `FORWARDED_ALLOW_IPS="*"` to fix proxy keying now** — lets any client spoof its IP and evade the limiter; the correct value is the specific proxy hop, which needs the real prod peer verified first (left as a documented action item, not a unilateral deploy/security change).
**Affects:** backend/rate_limiter.py, backend/main.py, backend/routers/auth.py, backend/loadtest/ratelimit.py (new), backend/loadtest/README.md, backend/tests/test_15_rate_limiting.py, docs/backend.md (§ Rate limiting), docs/deployment.md (§ Rate-limiter operational notes & findings).

## 2026-06-08 — Off-loop password hashing under a separate cap (concurrency P2.1)
**Area:** architecture · ops · **Status:** accepted
**Decision:** PBKDF2 (260k iterations, ~22 ms/call) was the last blocking-CPU call left on the single event loop. Auth paths now hash off the loop via `offload.run_blocking_hash` (`db.hash_password` / `db.verify_password_async`), under a **separate** `MAX_CONCURRENT_HASHES` semaphore (default cores − 1). Sync `_hash_password`/`verify_password` stay the implementation (still used by scripts/seeding); register/login/reset-password call the async wrappers. Iteration count unchanged. Guarded by `tests/test_concurrency_smoke.py` (real-PBKDF2 off-loop + bounding + verify round-trip + caps-are-distinct).
**Rejected:** (a) **Reuse the code-execution semaphore (`MAX_CONCURRENT_EXECUTIONS`)** — couples two different resource classes (a 22 ms CPU burst vs. a 512 MB subprocess); an auth burst would starve code execution and vice versa. Separate caps keep them isolated. (b) **Plain `asyncio.to_thread` with no cap** — gets it off the loop but lets a burst saturate every core (PBKDF2 releases the GIL), degrading the loop and code execution; the bounded helper mirrors the existing offload discipline. (c) **Lowering the iteration count** — that's a security parameter, not a perf knob; offloading fixes the loop block without weakening the hash.
**Affects:** backend/offload.py, backend/db.py, backend/routers/auth.py, backend/tests/test_concurrency_smoke.py, docs/backend.md (§ Off-loop password hashing), docs/deployment.md (env table + § Concurrency & scaling model).

## 2026-06-08 — Per-task git worktrees, merged to main + pushed + deleted (reverses "main only")
**Area:** process · **Status:** superseded
**Decision:** Each coding task runs in its **own git worktree** (off `main`), not directly on `main`. When the task is done and committed (one or several distinct logical commits, no squash unless asked): fast-forward local `main` from `origin/main` first, merge **all** the worktree branch's commits into `main`, `git push`, then delete the worktree + its branch. Invariant after every task: `main` ≡ `origin/main`, no lingering worktrees/branches. Trivial meta-changes may still go straight to `main`. The Agent tool `isolation: "worktree"` option is now allowed/encouraged for disjoint parallel slices.
**Rejected:** (a) **The prior "always work directly on `main`, never use worktrees" rule** — it forbade isolation; the user reversed it. The original motivation (stale `claude/*` branches/worktrees diverging from main) is addressed by the mandatory merge-push-delete cleanup step rather than by banning worktrees. (b) **Squash-merge to main** — the user wants the individual logical commits preserved. (c) **Merge without ff-ing main from origin first** — risks divergence/conflicts in the multi-session setup; the ff-first step is part of the rule.
**Affects:** CLAUDE.md (Standing instructions — git workflow), user memory (`worktree_workflow.md`, replacing `no_worktrees.md`).

## 2026-06-08 — Run all code execution off the event loop (kill head-of-line blocking)
**Area:** architecture · ops · **Status:** accepted
**Decision:** Every blocking evaluator now runs **off** the single event loop through `backend/offload.py`: `run_blocking_exec` (subprocess sandboxes — Python/Pandas/statistics) offloads via `asyncio.to_thread` under the global `MAX_CONCURRENT_EXECUTIONS` semaphore; `run_blocking_sql` (DuckDB) offloads *and* serializes behind a process-wide async lock. Applied uniformly across **all 7 code-exec paths** (practice SQL/Python/Pandas, statistics, sample — incl. converting its sync `run-query`/`run-code` endpoints to `async` —, and mock `_evaluate_submission`); statistics/sample/mock previously bypassed the semaphore entirely. DuckDB safety also required snapshotting the loaded-table set at startup so `get_loaded_tables()` (hit by `/health` + every query) no longer runs `SHOW TABLES` on the shared connection. Postgres pool raised 5+10→10+20 (configurable). A load harness (`backend/loadtest/`) measured before→after: head-of-line inflation of `/health` p95 **9.4× → 1.2×**; a single 5 s execution that blocked a concurrent `/health` for **4.7 s → ~0**; 3 concurrent 5 s executions **15.0 s → 5.0 s**; the single-replica knee moved ~16 → ~32 active users (now CPU-bound, not loop-bound). Guarded by `tests/test_concurrency_smoke.py`.
**Rejected:** (a) **Leaving the blocking calls on the loop** (status quo) — one 5–12 s execution froze every user; the existing semaphore was cosmetic because requests serialized on the loop *before* it could parallelize them. (b) **`to_thread` SQL with no lock / no table-cache** — concurrent use of the shared DuckDB connection *segfaults* (verified, exit 139); the lock + startup snapshot are mandatory, not optional. (c) **Concurrent DuckDB cursors** (cursor-only access tested clean at 6.4k concurrent queries) — rejected for a determinism-critical grader; single-writer serialization is conservative and SQL is sub-100 ms so it isn't the throughput bottleneck. Lifting it is a measured-bottleneck-gated Tier-3 option. (d) **Multiple uvicorn workers** — doesn't fix per-request blocking and multiplies DuckDB memory + DB connections; horizontal replicas (Tier 2) are the documented scale path instead.
**Refines:** the §3 "concurrency semaphore" sub-decision of *2026-06-06 — Sandbox security hardening* (the semaphore is now load-bearing rather than cosmetic; that entry's env-scrub and AST-guard decisions stand).
**Affects:** backend/offload.py (new), backend/database.py, backend/db.py, backend/config.py, backend/deps.py, backend/routers/{questions,python_questions,python_data_questions,statistics_questions,sample,mock}.py, backend/loadtest/ (new), backend/tests/test_concurrency_smoke.py (new), docs/backend.md, docs/deployment.md, CLAUDE.md.

## 2026-06-08 — Landing learning-paths section funnels to /learn, not a random leaf
**Area:** frontend · **Status:** accepted
**Decision:** Replaced the landing "random 4 paths + shuffle" block (each card deep-linking to one leaf path) with `PathsShowcaseSection`: a single featured track's graded arc as a connected numbered spine (foundational→advanced made visible, first 5 steps + "+N more") plus a breadth strip of every track with its live path count — all funneling to `/learn`, the curriculum front door. The only leaf deep-link is a per-user "Continue: <path>" resume hook for visitors with an in-progress path. Counts derive live from `/api/paths`.
**Rejected:** (a) **Keep random-4 + shuffle** — shuffle signals the paths are interchangeable, the opposite of a graded curriculum, and 4-of-86 shows neither breadth nor depth. (b) **A flat grid of the 9 foundational paths** — shows entry points but not the within-track arc, which was the actual problem (the progression was invisible). (c) **Per-card progress bars on the marketing surface** — a dead 0/N empty state for logged-out visitors; the surface should show the curriculum, not the user's emptiness.
**Affects:** frontend/src/pages/LandingPage.js, frontend/src/App.css, frontend/src/pages/LandingPageTiers.test.js, CLAUDE.md (landing structure §07 + path footprint), docs/{backend,frontend,content-authoring}.md (86-path footprint corrected from a stale 46).

## 2026-06-08 — Make SQL grading sound under ORDER BY ties and float aggregation
**Area:** architecture · **Status:** accepted
**Decision:** Order-sensitive SQL comparison is now **tie-tolerant**: a result is correct iff it has the same row multiset AND the same sequence of `ORDER BY` *key* values as the reference (rows tied on the key may permute) — `_results_match` / `_parse_order_by_columns` in `evaluator.py`. Separately, the grading DuckDB connection runs **single-threaded** (`SET threads TO 1`) so float aggregation is deterministic. Together these remove a live false-negative: 18 questions whose `ORDER BY` was not a total order, and 2 whose `ROUND`ed aggregate jittered across runs, marked correct answers (incl. the reference graded against itself) wrong a large fraction of the time. Surfaced by the new `test_code_references.py` guard.
**Rejected:** (a) **Full-row positional comparison** (the prior behaviour) — assumed every `ORDER BY` is a total order, nondeterministic on ties. (b) **Sort all rows before comparing** (pre-refactor behaviour) — would re-accept genuinely misordered answers. (c) **Per-question `ORDER BY` tiebreakers** for the 18 — content whack-a-mole that leaves the grader fragile for future questions. (d) **Float comparison tolerance** for the jitter — too loose (would accept near-but-wrong values); single-thread determinism fixes the root cause at negligible cost (≤9k-row datasets).
**Affects:** backend/evaluator.py, backend/database.py, backend/tests/test_sql_grading_tie_tolerance.py, backend/tests/test_sql_grading_determinism.py, docs/backend.md, CLAUDE.md.

## 2026-06-08 — Promote execution-based reference checks into standing CI guards
**Area:** process · **Status:** accepted
**Decision:** Wired the deterministic layer of the offline `audit_code_tracks.py` sweep into CI as pytest guards: `test_code_references.py` (every SQL + Pandas reference executes, is non-degenerate, `solution_*` reproduces `expected_*`, Pandas `schema` matches CSV header order) and `test_generator_reference_budget.py` (every `compute:reference` generator reference finishes within budget). Closes the gap where SQL/Pandas references — which grade *live*, so the content validator has no stored answer to check — could ship crashing/empty/wrong (the largest content-fix class of the 2026-05/06 refactor). Two pre-existing degenerate-empty SQL questions (12030, 13025) are quarantined as non-strict xfail pending an authoring-agent fix.
**Rejected:** (a) **Leaving the checks offline** in `audit_code_tracks.py` (run only during audits) — a new question could silently reintroduce the class. (b) **A Pandas-only guard** — the same hole existed for SQL (its references were never CI-executed either), so the guard spans both code tracks. (c) **An MCQ explanation-vs-key numeric heuristic** (the dropped H6) — false-positive-prone; the Reject-on-sight doc rule carries it instead.
**Affects:** backend/tests/test_code_references.py, backend/tests/test_generator_reference_budget.py, docs/content-authoring.md § Validator coverage state.

## 2026-06-07 — Debias MCQ answer keys on two axes (position + length), in content, gated by a blind re-audit
**Area:** content · **Status:** accepted
**Decision:** Removed two answer-key tells across every MCQ group (practice/mock/sample): correct-option **position** (was up to 100% one letter — fixed to ≤40% via a deterministic permutation + explanation "Option X" letter-remap) and correct-option **length** (was 82–96% "pick the longest" — fixed to ≤45% by trimming over-detailed correct options to mid-pack). Two new ERROR validators (`_validate_answer_position_balance`, `_validate_answer_length_balance`) enforce both forever.
**Rejected:** (a) **Serve-time option shuffle** — 79–97% of explanations reference "Option X" by letter, so runtime shuffling needs fragile prose surgery across 3 submit paths and desyncs on non-canonical phrasing; chose stored re-key. (b) **Wholesale de-letter of explanations** — would blind the `_validate_correct_option_explanation_consistency` key-inversion guard bank-wide; chose mechanical letter-remap that keeps it working. (c) **Forcing length to uniform 25% / lengthening distractors first** — risks answer-uniqueness. Governing rule: **uniqueness beats debiasing** — trim the correct option first (can't create a 2nd correct answer), re-pass every changed question through a GPT-5 blind re-audit, revert any trim-induced flip to audited text, leave-and-flag anything not debiasable safely.
**Affects:** backend/scripts/validate_content.py (2 validators + main()), backend/scripts/rebalance_phase1_positions.py, backend/scripts/audit_blind_answer_openai.py (sample support), docs/content-authoring.md, .github/agents/question-authoring.agent.md, CLAUDE.md.

## 2026-06-05 — Raise SQL guard MAX_JOINS 5 → 9
**Area:** architecture · **Status:** accepted
**Decision:** Raise `sql_guard.MAX_JOINS` from 5 to 9 (allow up to 8 joins anywhere in a query). The join *count* is not the cost driver on the small committed datasets (≤45k rows) — cost is already bounded by the 3s query timeout, the cartesian-join check, and the result caps. A cap of 5 wrongly rejected legitimately-hard analytics questions *and the platform's own reference solutions* (13018/13021/13024), and blocked the natural EXISTS-cohort approach entirely.
**Rejected:** (a) Rewrite the 3 references to ≤4 joins only — leaves users guard-blocked on the natural multi-join solution and distorts 13021's EXISTS lesson. (b) Keep the cap — keeps hard SQL un-authorable above 4 joins. Over-joining is coached via the EXPLAIN-based efficiency note, not blocked.
**Affects:** backend/sql_guard.py; 13018/13024 references also CTE-cleaned (4/3 joins) as quality polish.

## 2026-06-05 — Grade code answers on the full result; display only a preview
**Area:** architecture · **Status:** accepted
**Decision:** Pandas and SQL grading compare the **full** result (capped only by a high safety bound: pandas `_MAX_DATA_RESULT_ROWS=100k`, SQL `MAX_GRADING_ROWS=100k`) and return only a ~200-row preview (`total_rows`/`truncated`) to the client. This decouples grading soundness from payload/render cost, so legitimately large per-row outputs (e.g. dropna over 45k rows) grade correctly and a query/answer that diverges only beyond row 200 is no longer mis-graded.
**Rejected:** (a) Keep capping the *graded* result (pandas 10k hard-error / SQL `head(200)`) — unsound (mis-grades) or unauthorable. (b) Re-scope large-output questions to aggregates — distorts per-row lessons (esp. transform-vs-aggregate). Datetimes are ISO-serialized + date-normalized (not re-scoped) for the same "fix the platform, not the curriculum" reason.
**Affects:** docs/backend.md, docs/tracks/pandas.md, docs/tracks/sql.md; backend evaluator.py / python_evaluator.py / python_sandbox_harness.py.

## 2026-06-05 — Adopt an append-only decision log (this file)
**Area:** process · **Status:** accepted
**Decision:** Capture the *reasoning* layer in a single append-only, topic-tagged, never-expiring `docs/decisions/DECISIONS.md`, consulted on demand via a `CLAUDE.md` trigger and a one-line memory pointer — not auto-loaded every session.
**Rejected:** (a) A daily `history/` log on a rolling 2-week/1-month window — time is the wrong retrieval index, and expiry deletes exactly the *old* decisions that cause re-litigation. (b) Storing the decision archive in Claude memory — its index loads every session, so a growing log would tax every turn whether relevant or not; memory holds only the pointer. (c) Relying on commit messages alone — change-indexed not decision-indexed, can't be marked superseded, and rejected alternatives rarely survive.
**Affects:** CLAUDE.md (standing instruction + doc-mapping row), docs/README.md, memory (`decision_log.md` + index pointer).

## 2026-06-06 — Sandbox security hardening: scrubbed env, AST guard expansion, concurrency cap
**Area:** security · architecture · **Status:** accepted
**Decision:** Three layered hardening decisions shipped together:
1. **Scrubbed subprocess env** (`_sandbox_env`): the user-code subprocess receives a minimal default-deny env (PATH/HOME/locale/TMPDIR/TZ + Python flags). All production secrets are absent by default-deny, not by explicitly excluding named secrets. Rejected: explicit per-secret blocklist — too brittle, new secrets would require code changes.
2. **AST guard: `_BLOCKED_NAMES` via `visit_Name`**: blocking dangerous names at the Name-load AST node closes dynamic-string constructions (`globals()['__builtins__']`) that the prior call-site-only check missed. Also closes the `getattr` dynamic-key bypass (old guard only caught `getattr(x, literal_constant)`, not `getattr(x, dynamic_key)`). The guard is tested by 34 red-team escape attempts (all blocked) + 13 legit snippets (no false positives). Rejected: a runtime allow-only namespace (e.g. `RestrictedPython`) — adds a dependency, harder to understand and audit; our AST check is readable and the env scrub is the real backstop.
3. **Global asyncio concurrency semaphore** (default 10, `MAX_CONCURRENT_EXECUTIONS`): caps concurrent code-execution calls across SQL/Python/Pandas. Cap sits after plan/lock checks and around the DuckDB/subprocess call only. Rejected: per-user semaphore — more complex, and the real bottleneck is system-level (DuckDB process + subprocesses), not per-user fairness.
**What remains (infra-only):** egress block (P0), read-only filesystem (P1), seccomp (P1) — documented in `docs/deployment.md` § Sandbox security hardening.
**Affects:** backend/python_evaluator.py, backend/python_guard.py, backend/main.py, backend/deps.py, backend/routers/questions.py, backend/routers/python_questions.py, backend/routers/python_data_questions.py, Dockerfile, docs/backend.md, docs/deployment.md, CLAUDE.md.

## 2026-06-06 — Sandbox egress/seccomp/read-only done in CODE, not Railway infra
**Area:** security · deployment · **Status:** accepted (supersedes the "infra-only TODO" note in the prior entry)
**Context:** The prior entry deferred egress block / seccomp / read-only-fs as "infra-only" pending Railway config. Research found Railway uses eBPF networking and grants **no NET_ADMIN, no `--security-opt`, no `--read-only`/`--tmpfs`** — the container-level forms are simply unavailable on the platform.
**Decision:** Implement the equivalents in code/image instead:
1. **Egress block + seccomp profile** → a per-process `pyseccomp` filter installed in the sandbox `preexec_fn` (`_install_seccomp_filter`), denylisting the network-syscall family. Unprivileged seccomp works (libseccomp sets NO_NEW_PRIVS on load). Denylist (not allowlist) so pandas/numpy file+compute syscalls are untouched — validated on real Linux (socket BLOCKED, file-read + numpy OK) and in CI. Does NOT block `execve` (subprocess must exec python after preexec).
2. **Read-only app dir** → Dockerfile leaves `/app` root-owned (drop the `chown` to appuser), run as non-root appuser → app readable/executable but not writable; only `/tmp` writable. The in-image equivalent of `--read-only`.
3. **Memory cap** → the ONLY remaining Railway-dashboard item (RLIMIT_AS already caps per-subprocess; container cap is belt-and-suspenders).
**Rejected:** (a) Wait for/contact Railway for egress+seccomp support — slower, uncertain, and the in-process filter is strictly better (per-sandbox, not whole-container). (b) Allowlist seccomp (default-deny) — breaks pandas/numpy (hundreds of syscalls); a network denylist is the right scope. (c) Move code execution to a separate no-egress worker service — larger architecture change, deferred unless the in-process filter proves insufficient.
**Affects:** backend/python_evaluator.py, backend/requirements.txt, Dockerfile, .github/workflows/ci.yml, backend/tests/test_sandbox_seccomp.py, docs/deployment.md, CLAUDE.md.

## 2026-06-06 — Category-3 (resource-exhaustion) hardening: killpg, NPROC/FSIZE, semaphore=cores-2
**Area:** security · architecture · **Status:** accepted
**Context:** Review of the "legit-but-abusive code" vector (infinite loops, memory/fork bombs, output floods) found the AST guard correctly *allows* these (they're resource abuse, not escapes) — so only runtime caps stop them — and two real gaps: (1) the `setsid()` "kills the whole tree" claim was false because `subprocess.run`'s timeout does `proc.kill()` on the single child, orphaning forked grandchildren past the timeout; (2) no `RLIMIT_NPROC`, so a fork bomb (under the assume-guard-bypassed model) was unbounded. Also surfaced: two "timeout" tests (tc093/tc104) used `import time` which the guard blocks first, so they tested guard-rejection, not the timeout.
**Decision:**
1. **Process-GROUP kill on timeout** — `_spawn_harness` switched from `subprocess.run` to `Popen` + `communicate(timeout)`; on timeout `_kill_process_group` does `os.killpg(SIGKILL)`. Makes `setsid()` effective; forked children die with the parent.
2. **`RLIMIT_NPROC` 256 + `RLIMIT_FSIZE` 64 MB** added; **`RLIMIT_CPU` 15→14 s** (just above the 12 s data wall).
3. **Concurrency semaphore default → cores − 2** (was fixed 10) — CPU headroom + bounds peak sandbox memory (concurrency × `RLIMIT_AS`), which the Railway RAM cap is sized above.
4. Fixed tc093/tc104 to real `while True: pass` loops; added `test_sandbox_resource_limits.py` (infinite loop, memory bomb, fork-bomb killpg, output flood, recursion — Linux-gated ones validated in CI + a throwaway production-image run).
**Rejected:** (a) seccomp-block `clone`/`fork` for the fork bomb — breaks numpy/BLAS threads (clone-based); `RLIMIT_NPROC` is the clean per-UID bound. (b) Separate sandbox UID for cleaner `RLIMIT_NPROC` semantics — needs the app to start as root / setuid, which the non-root Railway image avoids; a generous NPROC value with headroom is sufficient. (c) Lower the wall timeouts — risks failing legit slow-but-correct solutions; the group-kill + caps are the right lever, not a tighter clock.
**Railway note:** the container RAM cap is set under Settings → Scale → **Replica Limits** (Memory slider), sized above app baseline + semaphore × 512 MB; do not set it low (DuckDB loads datasets in-memory).
**Affects:** backend/python_sandbox_harness.py, backend/python_evaluator.py, backend/main.py, backend/tests/test_06_python.py, test_07_pandas.py, test_sandbox_resource_limits.py, docs/backend.md, docs/deployment.md, CLAUDE.md.

## 2026-06-08 — Added 3-D reasoning questions in existing families; fixed 2 SQL empty-result defects
**Area:** content · curriculum · **Status:** accepted
**Decision:**
1. **Three new practice questions authored without a new concept family** (22051 Python medium STREAMING/ONLINE REDUCTION, 23037 Python hard GRAPH TRAVERSAL BFS/DFS, 33088 Pandas hard RESHAPING&PIVOT + WINDOW&ROLLING + DATETIME). Each lands in an existing healthy family. Creating a new concept family was rejected because it would trigger the mock-only floor obligation (≥4 questions per family at each applicable tier) and the families already in use carry the correct reasoning-depth framing.
2. **Cascade question (33088) re-skinned to business data** — the original handoff mentioned a "cascade" pattern but the datasets have no finance/markets data. Re-skinned to `orders`/`users` acquisition-channel × monthly-refund-rate panel, which is fully satisfiable on the committed CSVs (5 result rows). The cross-entity propagation logic (pivot panel + `.shift(1)` for sustained breach + co-breach count) is clearly distinct from the two existing single-series anomaly questions (33013 IQR, 33085 rolling mean).
3. **SQL empty-result defects fixed**: Q12030 changed HAVING from `problematic > paid` to `problematic > paid * 0.12` (returns 3 rows: IN, UK, SG); Q13025 changed time windows from 24h/48h to 300d/600d (returns 2 rows: user 312/prod 142, user 488/prod 174). Reasoning unchanged in both cases. Removed both from `_SQL_KNOWN_DEFECTS` in `test_code_references.py`.
4. **Python hard Q23037 (3D BFS)** uses iterative BFS + deque to avoid Python's default recursion-depth limit on 50×50×50 volumes — this is a genuine hard-tier insight beyond the medium-tier "BFS basics." 6-neighbor adjacency in 3D (vs 4-neighbor in 2D island questions) is the load-bearing reasoning surface; two voxels in different z-layers at the same (y,x) position would be disconnected in any 2D slice analysis.
5. **Python medium Q22051 (3D geofence)** single-pass streaming with altitude (z-axis) as the load-bearing breach dimension — a 2D-only check would produce false negatives on the public test cases. Placed in STREAMING/ONLINE REDUCTION because the reasoning is bounded-state single-pass (not sliding-window or indexed-sequence).
**Rejected alternatives:** new concept family for 3D spatial reasoning (mock-only floor obligation, no existing taxonomy need), finance dataset (doesn't exist in the committed CSVs), Q33088 as id 33035 (already occupied by a mock-only question — used 33088 instead).
**Affects:** backend/content/python_questions/medium.json, hard.json, backend/content/python_data_questions/hard.json, backend/content/questions/medium.json, hard.json, backend/tests/test_code_references.py, docs/tracks/python.md, docs/tracks/pandas.md, CLAUDE.md, docs/decisions/DECISIONS.md.

## 2026-06-07 — Pin the SQL grading engine (DuckDB) to the validated version
**Area:** architecture · content-integrity · **Status:** accepted
**Context:** `duckdb` was unpinned in requirements.txt. CI installed the latest (1.5.3), which has a window-function regression that made SQL q13011's reference query return 0 rows ("degenerate reference") — while 1.5.0 returns the intended 83 on the same arch + data (proven by an x86 container matrix: x86/1.5.0=83, x86/1.5.3=0, arm/1.5.0=83). This reddened main for 5+ commits. The decomposition showed the divergence is upstream of the final float comparison (count-conditions-only: 83 on 1.5.0 vs 49 on 1.5.3), so rounding the comparison would NOT fix it — it's a genuine engine-version semantics change.
**Decision:** Pin `duckdb==1.5.0` (the version all ~118 SQL references were authored/validated against). A grading engine is part of the answer key — it must be pinned to the validated version, and a bump must re-run `tests/test_code_references.py` before merging. `tests/test_code_references.py` (added by the SQL-determinism workstream) is the standing guard: it executes every SQL/python/statistics reference and fails on any degenerate/empty one — it is what caught this.
**Rejected:** (a) Stay unpinned and rewrite q13011 to be version-robust — the divergence is upstream window semantics, not the final compare, so per-query rounding doesn't fix it; chasing engine bugs per-question is unbounded. (b) Pin to a newer version — 1.5.3 is the one that regressed; 1.5.0 is the validated baseline. (c) Round the float HAVING comparison anyway — defensible hygiene but it was a red herring here (the decomposition ruled it out); left as a latent quality note, not changed, to avoid content risk.
**Affects:** backend/requirements.txt, docs/backend.md (SQL evaluation path), CLAUDE.md (practice count 875→878 stale-doc reconcile, same close-out).
